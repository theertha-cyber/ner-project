# Retrieval eval fixtures

`corpus.jsonl` and `golden_set.jsonl` are synthetic content authored for this
change (`retrieval-tools-and-eval`). No document, chunk, or query in this
directory originates from a live tenant schema or production data — the
corpus describes a fictional small logistics company ("Fictive Freight
Co.") invented purely to exercise retrieval.

- `corpus.jsonl` — one record per document, each with a list of chunks
  (`chunk_index`, `chunk_text`). `document_id` values are stable strings.
- `golden_set.jsonl` — one record per query: `query_id`, `query`, `relevant`
  (list of `{document_id, chunk_index, grade}`, grade 0-3), optional `notes`.
- `embeddings.json` — precomputed embeddings for every corpus chunk and every
  golden-set query, committed so default eval runs are deterministic and
  offline. Tagged with `embedding_model` — a run whose configured embedding
  model doesn't match fails loudly rather than scoring against stale
  vectors. Currently generated by a deterministic synthetic hashing function
  (`embedding_model: "synthetic-hash-embedding-v1"`), not a real semantic
  embedding model — see the module docstring in
  `scripts/_gen_retrieval_eval_fixtures.py` for why, and regenerate with
  `python scripts/_gen_retrieval_eval_fixtures.py`. Swapping in real
  embeddings later means re-running that generator against a live embedding
  backend and committing the result, which also means retiring the
  synthetic tag.
- `baseline.json` — committed aggregate metrics for the default retrieval
  configuration (`dense_only`, k=5), used by the regression gate. Generated
  with `python -m scripts.run_retrieval_eval --k 5 --write-baseline`.
  Observed run-to-run variance across repeated runs against this fixture set
  is 0.0 (fully deterministic: precomputed embeddings, no live model calls) —
  the gate's default tolerance (0.02) is a margin for when live embeddings
  replace the synthetic ones, not a fit to observed noise.

Regenerating any of these files requires re-running the eval pipeline and
committing the new output as a reviewed change.
