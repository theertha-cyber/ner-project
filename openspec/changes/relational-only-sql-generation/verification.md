# Verification Plan

**Change:** relational-only-sql-generation
**Generated:** 2026-08-24
**Status:** 🟡 Implementation verified — every scenario has a passing acceptance test and the Evidence Log is populated. Outstanding before archive: the golden-set eval (task 9.2), the execution-role provisioning run in any environment with `sql_execution_role_enabled` (task 9.1), and the human Audit Record below.

---

## 1. Spec Alignment

Map every requirement and every scenario in this change to a testable acceptance criterion.
Each row drives one evidence entry in Section 5.

| # | Capability | Requirement | Scenario | Acceptance Criterion | Verification Artifact | Status |
|---|-----------|-------------|----------|---------------------|-----------------------|--------|
| 1 | relational-sql-generation | The generated relational surface is the canonical query model | The prompt presents the relational surface, not the EAV table | Given a tenant with an active `multi` definition `Skill` and an active `single` definition `Email`, when the generation prompt is built, then it names `subject` and `e_skill` as the tables to query and contains no instruction to select from `document_entities` or filter on `entity_type` | `tests/test_chat_api_sql.py::TestSQLPrompt` (task 5.9) | - [x] |
| 2 | relational-sql-generation | The generated relational surface is the canonical query model | A single-valued entity is answered from a subject column | Given an active `single` definition projected as `subject.email`, when a question about email addresses is generated, then the statement reads `subject.email` and passes validation | `tests/test_chat_api_sql.py::TestSQLPrompt` (task 5.9) | - [x] |
| 3 | relational-sql-generation | The generated relational surface is the canonical query model | A multi-valued entity is answered from its child table | Given an active `multi` definition projected as `e_skill`, when a question about subjects having a skill is generated, then the statement reads `e_skill` and joins `subject` on `document_id` when it needs the filename or a single-valued fact | `tests/test_chat_api_sql.py::TestSQLPrompt` (task 5.9) | - [x] |
| 4 | relational-sql-generation | One resolver supplies the tenant's relational surface to the generator | The generator receives the tenant's own tables | Given two tenants with different catalogs, when a question is asked in each, then each prompt contains only that tenant's generated relations and none of the other's | `tests/test_chat_api_sql_retry.py` — two-tenant prompt isolation (task 4.5) | - [x] |
| 5 | relational-sql-generation | One resolver supplies the tenant's relational surface to the generator | An inactive definition is not exposed to the generator | Given a deactivated definition whose table is retained, when the surface is resolved, then the prompt omits the table, validation rejects a statement naming it, and the execution role holds no `SELECT` on it | `tests/test_entity_views_generator.py` + `tests/test_sql_execution_privileges.py::TestGeneratedTableSurface` (tasks 1.6, 2.7) | - [x] |
| 6 | relational-sql-generation | One resolver supplies the tenant's relational surface to the generator | A child table retained from a multi era is not exposed to the generator | Given a definition now `single` whose child table was retained, when the surface is resolved, then the prompt omits that table and lists the definition's `subject` column instead | `tests/test_entity_views_generator.py` + `tests/test_sql_execution_privileges.py::TestGeneratedTableSurface` (tasks 1.6, 2.7) | - [x] |
| 7 | relational-sql-generation | One resolver supplies the tenant's relational surface to the generator | A definition added after the last question appears without a deployment | Given a tenant that gains a new active `multi` definition with an `sql_identifier`, when the next question is asked, then its table appears in the prompt, the whitelist, and the grants | `tests/test_entity_views_generator.py` — catalog change re-resolves (task 1.6) | - [x] |
| 8 | relational-sql-generation | One resolver supplies the tenant's relational surface to the generator | The prompt, the whitelist, and the grants agree | Given any tenant schema, when the prompt-named set, the validated set, and the granted set are computed, then all three come from the same resolver and are equal for the generated relations | `tests/test_chat_api_sql_retry_integration.py` — one-resolver set equality (task 8.4) | - [x] |
| 9 | relational-sql-generation | The generator is given the semantic meaning of each relation and column | A definition's description reaches the prompt | Given an active definition `Skill` with a description, when the prompt is built, then it associates `e_skill` with the name `Skill` and that description | `tests/test_chat_api_sql_retry.py` — relational grounding block (task 4.5) | - [x] |
| 10 | relational-sql-generation | The generator is given the semantic meaning of each relation and column | A typed column advertises its kind | Given an active `single` definition of kind `number` projected as `subject.years_experience`, when the prompt is built, then it declares that column's SQL type and indicates it holds a parsed numeric value | `tests/test_chat_api_sql_retry.py` — relational grounding block (task 4.5) | - [x] |
| 11 | relational-sql-generation | The generator is given the semantic meaning of each relation and column | Value samples are keyed by relation, not by entity type | Given stored entities typed `PER` mapped through `base_label_mapping` onto a definition projected as `subject.name`, when samples are assembled, then those values appear under `subject.name` and not under a bare `PER` | `tests/test_chat_api_sql_retry.py` — `base_label_mapping`-only fixture (task 4.6) | - [x] |
| 12 | relational-sql-generation | The generator is given the semantic meaning of each relation and column | A relation with no sampled values is still listed | Given an active definition whose projected rows yield no usable samples, when the prompt is built, then its relation still appears in the listed surface | `tests/test_chat_api_sql_retry.py` — relational grounding block (task 4.5) | - [x] |
| 13 | relational-sql-generation | The generator is given the semantic meaning of each relation and column | Grounding is fetched once per invocation | Given an invocation requiring three attempts, when the loop completes, then the surface-resolution and value-sample queries ran once, not once per attempt | `tests/test_chat_api_sql_retry.py` — relational grounding block (task 4.5) | - [x] |
| 14 | relational-sql-generation | Base-model tenants resolve to the same relational surface | A base-label definition appears on the surface | Given an active `multi` definition `Person` whose `base_label_mapping` maps `PER`, when the surface is resolved, then its child table appears and the prompt presents it under the definition's own name | `tests/test_chat_api_sql_retry.py` — `base_label_mapping`-only fixture (task 4.6) | - [x] |
| 15 | relational-sql-generation | Base-model tenants resolve to the same relational surface | Base-label values are sampled under the right relation | Given stored entities typed `ORG` belonging to a definition projected as `e_employer`, when samples are assembled, then those values appear under `e_employer` | `tests/test_chat_api_sql_retry.py` — `base_label_mapping`-only fixture (task 4.6) | - [x] |
| 16 | relational-sql-generation | Document-scoped questions stay document-scoped on the relational surface | A subject query is constrained to the scoped documents | Given a question resolved to one document and a statement selecting from `subject`, when the scope is applied, then the executed statement restricts `subject` by a bound `document_id` predicate and the identifiers appear nowhere as literals | `tests/test_chat_api_structured_scope.py` (task 3.4) | - [x] |
| 17 | relational-sql-generation | Document-scoped questions stay document-scoped on the relational surface | A child-table query is constrained to the scoped documents | Given a scoped question and a statement selecting from `e_skill` joined to `subject`, when the scope is applied, then both relations carry the bound predicate | `tests/test_chat_api_structured_scope.py` (task 3.4) | - [x] |
| 18 | relational-sql-generation | Document-scoped questions stay document-scoped on the relational surface | The scope survives aggregation over the relational surface | Given a scoped aggregate over `e_skill` with a `GROUP BY`, when the scope is applied, then the predicate applies underneath the grouping and the aggregate covers only scoped documents | `tests/test_chat_api_structured_scope.py` (task 3.4) | - [x] |
| 19 | relational-sql-generation | Document-scoped questions stay document-scoped on the relational surface | The scope is applied before the row limit truncates | Given a scoped statement carrying a `LIMIT`, when the scope is applied, then the predicate constrains the source rows and the limit applies to in-scope rows only | `tests/test_chat_api_structured_scope.py` (task 3.4) | - [x] |
| 20 | relational-sql-generation | Document-scoped questions stay document-scoped on the relational surface | An unscopeable statement is not a silent tenant-wide answer | Given a document scope and a statement referencing no scopeable relation, when the attempt completes, then it is not classified `success` and is retried with feedback naming the problem | `tests/test_chat_api_structured_scope.py` (task 3.4) | - [x] |
| 21 | relational-sql-generation | Document-scoped questions stay document-scoped on the relational surface | The scope is reapplied to every attempt | Given a scoped question needing two attempts, when the loop completes, then every executed statement carried the bound scope predicate | `tests/test_chat_api_structured_scope.py` (task 3.4) | - [x] |
| 22 | relational-sql-generation | Invalid relational SQL is rejected before execution | A relation not on the surface is rejected | Given a statement selecting from `e_unknown`, when validation inspects it, then the statement is rejected and never executed | `tests/test_sql_table_whitelist.py` (task 2.6) | - [x] |
| 23 | relational-sql-generation | Invalid relational SQL is rejected before execution | A column no relation declares is rejected | Given a statement selecting `subject.salary` where `subject` declares no such column, when validation inspects it, then the statement is rejected and the error names the column | `tests/test_sql_table_whitelist.py` (task 2.6) | - [x] |
| 24 | relational-sql-generation | Invalid relational SQL is rejected before execution | A valid relational statement passes | Given a statement joining `e_skill` to `subject` on `document_id`, selecting declared columns, carrying a row limit, when validation inspects it, then the statement is accepted | `tests/test_sql_table_whitelist.py` (task 2.6) | - [x] |
| 25 | relational-sql-generation | Invalid relational SQL is rejected before execution | Another tenant's generated relation is rejected | Given a statement naming a table that exists only in another tenant's schema, when validation inspects it against the querying tenant's surface, then the statement is rejected | `tests/test_sql_table_whitelist.py` (task 2.6) | - [x] |
| 26 | relational-sql-generation | Relational failures are detected and corrected without entity-type patterns | A wrong relation is corrected on retry | Given a first statement reading a relation not on the surface, when the attempt completes, then it is a validation failure and the retry prompt carries the rejected statement, the reason, and the list of on-surface relations | `tests/test_chat_api_sql_retry.py` — relational defect ladder (task 6.7) | - [x] |
| 27 | relational-sql-generation | Relational failures are detected and corrected without entity-type patterns | A value filed under a different relation is explained in relational terms | Given a first statement filtering `e_skill.normalized_value = 'oracle'` returning zero rows while `oracle` occurs under a definition projected as `e_employer`, when the attempt completes, then it is an empty-with-defect and the feedback names `e_employer` without referencing `entity_type` | `tests/test_chat_api_sql_retry.py` — relational defect ladder (task 6.7) | - [x] |
| 28 | relational-sql-generation | Relational failures are detected and corrected without entity-type patterns | An unexplained empty relational result is not retried | Given a statement that validates, executes, returns zero rows, references only on-surface relations and columns, and triggers no defect signal, when the attempt completes, then it is classified `success` and an empty result is returned without error | `tests/test_chat_api_sql_retry.py` — relational defect ladder (task 6.7) | - [x] |
| 29 | relational-sql-generation | Relational failures are detected and corrected without entity-type patterns | A database error on a relational statement is retried | Given a validated statement whose join predicate raises at execution, when the attempt completes, then it is an execution failure and the retry prompt carries the sanitized error and the previous statement | `tests/test_chat_api_sql_retry.py` — relational defect ladder (task 6.7) | - [x] |
| 30 | relational-sql-generation | Relational failures are detected and corrected without entity-type patterns | A filename filter that matches no document is still detected | Given a statement requiring `subject.filename` to match a literal no document carries and returning zero rows, when the attempt completes, then it is an empty-with-defect and the feedback states that no document carries that filename | `tests/test_chat_api_sql_retry.py` — relational defect ladder (task 6.7) | - [x] |
| 31 | relational-sql-generation | A tenant whose relational surface is unpopulated reports the source as unavailable | An unprojected tenant does not get a confident empty answer | Given a tenant whose `document_entities` holds rows and whose `subject` holds none, when a structured question is asked, then structured retrieval reports a failure, the turn reports the source unavailable, and it does not report that no matching records exist | `tests/test_chat_api_sql_retry.py` — coverage probe (task 7.4) | - [x] |
| 32 | relational-sql-generation | A tenant whose relational surface is unpopulated reports the source as unavailable | An unprojected document under a document scope does not get a confident empty answer | Given a question scoped to a document with entity rows but no `subject` row, when the question is asked, then structured retrieval reports a failure rather than an empty answer | `tests/test_chat_api_sql_retry.py` — coverage probe (task 7.4) | - [x] |
| 33 | relational-sql-generation | A tenant whose relational surface is unpopulated reports the source as unavailable | A populated surface answers normally | Given a tenant whose documents have been projected, when a question legitimately matches nothing, then the empty result is returned as a successful, genuinely empty answer | `tests/test_chat_api_sql_retry.py` — coverage probe (task 7.4) | - [x] |
| 34 | relational-sql-generation | A tenant whose relational surface is unpopulated reports the source as unavailable | Re-extraction is the migration path | Given a tenant reported unprojected, when a new model version is promoted and batch extraction runs over its documents, then the schema is reconciled, the surface is populated, and subsequent questions are answered from it | `tests/test_relational_projection_worker.py` — re-extraction repopulates the surface (task 7.5) | - [x] |
| 35 | chat-api | SQL query generation and validation | Valid SQL query is executed | Given a natural-language question, when generation produces a SELECT over the tenant's relational surface with a LIMIT, then validation passes it, it executes in a read-only transaction, and the rows reach the RAG pipeline | `tests/test_chat_api_sql_retry_integration.py` + `tests/test_chat_api_sql.py` (task 8.3) | - [x] |
| 36 | chat-api | SQL query generation and validation | Malicious SQL is rejected | Given an LLM-generated `DROP TABLE extracted_entities`, when validation inspects it, then it is rejected, logged, the SQL source is skipped, and the response indicates the source was unavailable | `tests/test_chat_api_sql_retry_integration.py` + `tests/test_chat_api_sql.py` (task 8.3) | - [x] |
| 37 | chat-api | SQL query generation and validation | Query with non-whitelisted table is rejected | Given a generated query referencing `pg_authid`, when validation inspects the table name, then the query is rejected | `tests/test_chat_api_sql_retry_integration.py` + `tests/test_chat_api_sql.py` (task 8.3) | - [x] |
| 38 | chat-api | SQL query generation and validation | Query with a column no relation declares is rejected | Given a generated query selecting a column neither the static tables nor the resolved surface declares, when validation inspects it, then the query is rejected and the error names the column | `tests/test_sql_table_whitelist.py` — undeclared column rejected (task 2.6) | - [x] |
| 39 | chat-api | SQL query generation and validation | Query exceeds timeout | Given a valid query running longer than 10 seconds, when it executes, then execution is cancelled and the SQL source is skipped for the turn | `tests/test_chat_api_sql_retry_integration.py` + `tests/test_chat_api_sql.py` (task 8.3) | - [x] |
| 40 | chat-api | SQL query generation and validation | A generated entity table is both granted and whitelisted | Given a tenant with an active `multi` definition whose table exists, when the role is provisioned and the whitelist resolved, then the role holds `SELECT` and validation accepts a query naming it | `tests/test_sql_execution_privileges.py` (task 2.7) | - [x] |
| 41 | chat-api | SQL query generation and validation | Grants, whitelist, and generation context resolve from one source | Given any tenant schema, when the granted, whitelisted, and generator-described sets are computed, then all three come from one resolver and are equal for the generated tables | `tests/test_chat_api_sql_retry_integration.py` — prompt/whitelist/grants equality (task 8.4) | - [x] |
| 42 | chat-api | SQL query generation and validation | An inactive definition's table is excluded from the query surface | Given a deactivated definition whose table is retained, when grants and whitelist are resolved, then neither includes it, the generation context omits it, and a query naming it is rejected | `tests/test_sql_execution_privileges.py::TestGeneratedTableSurface` + `tests/test_entity_views_generator.py` (tasks 1.6, 2.7) | - [x] |
| 43 | chat-api | SQL query generation and validation | A child table retained from a `multi` era is excluded from the query surface | Given a definition now `single` whose child table was retained, when grants and whitelist are resolved, then neither includes it, a query naming it is rejected, and its values remain reachable as a `subject` column | `tests/test_sql_execution_privileges.py::TestGeneratedTableSurface` + `tests/test_entity_views_generator.py` (tasks 1.6, 2.7) | - [x] |
| 44 | chat-api | SQL query generation and validation | A table that leaves the query surface loses its grant | Given a generated table the role holds `SELECT` on, when its definition leaves the surface and the role is provisioned again, then provisioning revokes the schema's table privileges before re-granting and the role no longer holds `SELECT` on it | `tests/test_sql_execution_privileges.py` (task 2.7) | - [x] |
| 45 | chat-api | SQL query generation and validation | Reactivation restores access without recreating data | Given a previously deactivated definition whose table retained its rows, when it is reactivated and the role and whitelist are resolved again, then the role holds `SELECT` and a query naming it is accepted | `tests/test_sql_execution_privileges.py` (task 2.7) | - [x] |
| 46 | chat-api | SQL query generation and validation | A grant for a table that does not yet exist is skipped safely | Given an active definition whose table has not been created, when the role is provisioned, then the `pg_tables` guard skips the grant and provisioning does not raise | `tests/test_sql_execution_privileges.py` (task 2.7) | - [x] |
| 47 | sql-query-recovery | Attempt outcome classification | Validation failure is classified and retried | Given a first query referencing a relation outside the surface and the static whitelist, when validation rejects it, then the attempt is `validation_error`, the query is not executed, and a second attempt is made | `tests/test_chat_api_sql_retry.py` (task 6.7) | - [x] |
| 48 | sql-query-recovery | Attempt outcome classification | A column no relation declares is classified as a validation failure | Given a first query selecting a column no on-surface relation declares, when validation rejects it, then the attempt is `validation_error` and a second attempt is made | `tests/test_chat_api_sql_retry.py` (task 6.7) | - [x] |
| 49 | sql-query-recovery | Attempt outcome classification | Execution failure is classified and retried | Given a first query that validates but raises at execution, when the attempt completes, then it is `execution_error`, a second attempt is made, and a succeeding second attempt returns its rows | `tests/test_chat_api_sql_retry.py` (task 6.7) | - [x] |
| 50 | sql-query-recovery | Attempt outcome classification | Generation failure is classified and retried | Given the generation LLM call raising or returning unusable text, when the attempt completes, then it is `generation_error` and a second attempt is made | `tests/test_chat_api_sql_retry.py` (task 6.7) | - [x] |
| 51 | sql-query-recovery | Zero rows is a legitimate result unless a deterministic defect explains it | Unexplained empty result is not retried | Given a query that validates, executes, returns zero rows, references only on-surface relations and columns, and triggers no defect, when the attempt completes, then it is `success`, no second generation call is made, and an empty result is returned without error | `tests/test_chat_api_sql_retry.py` (task 6.7) | - [x] |
| 52 | sql-query-recovery | Zero rows is a legitimate result unless a deterministic defect explains it | Empty result caused by a value held in another relation is retried | Given a first query filtering `e_skill.normalized_value = 'oracle'` returning zero rows while the value occurs under a definition projected as `e_employer`, when the attempt completes, then it is `empty_with_defect`, a second attempt is made, and its rows are returned on success | `tests/test_chat_api_sql_retry.py` (task 6.7) | - [x] |
| 53 | sql-query-recovery | Zero rows is a legitimate result unless a deterministic defect explains it | A value that occurs nowhere in the tenant's data is not a defect | Given a first query filtering on a value occurring under no relation or column and returning zero rows, when the attempt completes, then it is `success` and the empty result is returned as a genuine answer | `tests/test_chat_api_sql_retry.py` (task 6.7) | - [x] |
| 54 | sql-query-recovery | Zero rows is a legitimate result unless a deterministic defect explains it | An unscopeable document-scoped statement is a defect | Given a supplied document scope and a statement referencing no scopeable relation, when the attempt completes, then it is `empty_with_defect` and a second attempt is made | `tests/test_chat_api_sql_retry.py` (task 6.7) | - [x] |
| 55 | sql-query-recovery | Zero rows is a legitimate result unless a deterministic defect explains it | Non-empty result is never retried | Given a query returning at least one row, when the attempt completes, then it is `success` regardless of any other signal and no further generation call is made | `tests/test_chat_api_sql_retry.py` (task 6.7) | - [x] |
| 56 | sql-query-recovery | Previous-attempt feedback is supplied to the generator | Retry prompt contains the previous SQL and its failure reason | Given a first attempt that failed with a database error, when the second prompt is built, then it contains the first SQL, a sanitized failure description, and an instruction to reconsider assumptions | `tests/test_chat_api_sql_retry.py` (task 6.7) | - [x] |
| 57 | sql-query-recovery | Previous-attempt feedback is supplied to the generator | Bound parameters are stripped from error feedback | Given a database error whose message appends the statement and bound parameters, when it is rendered into feedback, then the appended echo is removed and the retained text is truncated to the configured budget | `tests/test_chat_api_sql_retry.py` (task 6.7) | - [x] |
| 58 | sql-query-recovery | Previous-attempt feedback is supplied to the generator | Empty-result feedback names the relation that holds the value | Given a first attempt returning zero rows because it filtered a value in a relation that does not hold it, when the second prompt is built, then it states the zero-row outcome, names the relation or column that does hold the value, and does not reference `entity_type` | `tests/test_chat_api_sql_retry.py` (task 6.7) | - [x] |
| 59 | sql-query-recovery | Previous-attempt feedback is supplied to the generator | Validation feedback restates the available surface | Given a first attempt rejected for naming a nonexistent relation or column, when the second prompt is built, then it contains the rejection reason and lists the relations and columns available to this tenant | `tests/test_chat_api_sql_retry.py` (task 6.7) | - [x] |
| 60 | sql-query-recovery | Bounded relational surface and value samples in the generation context | The relational surface appears in the generation context | Given a tenant with an active `multi` definition `Skill` and an active `single` definition `Email`, when the prompt is built, then it lists `e_skill` with its columns and `subject` with its `email` column and that column's SQL type | `tests/test_chat_api_sql_retry.py` — surface and samples in context (task 4.5) | - [x] |
| 61 | sql-query-recovery | Bounded relational surface and value samples in the generation context | Sample values appear under their relation | Given a tenant whose skill entities include values such as `python` and `kubernetes`, when the prompt is built, then those values are listed under `e_skill` | `tests/test_chat_api_sql_retry.py` — surface and samples in context (task 4.5) | - [x] |
| 62 | sql-query-recovery | Bounded relational surface and value samples in the generation context | Sample size is bounded per relation and in total | Given a tenant with many relations and many distinct values, when samples are fetched, then no relation or column exceeds the configured per-key sample size and the total does not exceed the configured overall cap | `tests/test_chat_api_sql_retry.py` — surface and samples in context (task 4.5) | - [x] |
| 63 | sql-query-recovery | Bounded relational surface and value samples in the generation context | Relations with no sampled values are still listed | Given a relation whose rows yield no usable sample values, when the prompt is built, then that relation still appears in the listed surface | `tests/test_chat_api_sql_retry.py` — surface and samples in context (task 4.5) | - [x] |
| 64 | sql-query-recovery | Bounded relational surface and value samples in the generation context | Surface and samples are fetched once per invocation | Given an invocation requiring three attempts, when the loop completes, then the surface and sample queries ran once, not once per attempt | `tests/test_chat_api_sql_retry.py` — surface and samples in context (task 4.5) | - [x] |
| 65 | sql-query-recovery | Bounded relational surface and value samples in the generation context | One tenant's surface never appears in another tenant's context | Given two tenants with different catalogs, when a question is asked in each, then each prompt contains only the relations of the tenant whose schema was bound from request context | `tests/test_chat_api_sql_retry.py` — surface and samples in context (task 4.5) | - [x] |
| 66 | entity-view-layer | The query-surface resolver is the one authoritative description of the readable relations | The resolver reports columns and types, not just names | Given a tenant with an active `single` definition of kind `number` and an active `multi` definition, when the surface is resolved, then the result names the `subject` column and declares it `DOUBLE PRECISION`, and names the child table and its fixed column list | `tests/test_entity_views_generator.py` — resolver shape (task 1.6) | - [x] |
| 67 | entity-view-layer | The query-surface resolver is the one authoritative description of the readable relations | The resolver carries the definition's semantics | Given an active definition with a description and examples in the catalog, when the surface is resolved, then the result carries that name, description, and examples alongside its relation or column | `tests/test_entity_views_generator.py` — resolver shape (task 1.6) | - [x] |
| 68 | entity-view-layer | The query-surface resolver is the one authoritative description of the readable relations | The relation set matches what the reconciler maintains | Given any set of definitions, when the surface is resolved and the reconciler's expected table set computed, then the two sets are equal | `tests/test_entity_views_generator.py` — resolver shape (task 1.6) | - [x] |
| 69 | entity-view-layer | The query-surface resolver is the one authoritative description of the readable relations | Off-surface relations are excluded | Given a tenant with an inactive definition, a definition with no `sql_identifier`, and a `single` definition retaining a `multi`-era child table, when the surface is resolved, then none of those tables appear and the `single` definition appears as a `subject` column | `tests/test_entity_views_generator.py` — resolver shape (task 1.6) | - [x] |
| 70 | entity-view-layer | The query-surface resolver is the one authoritative description of the readable relations | One tenant's surface never includes another's relations | Given two tenants with different catalogs, when both schemas are resolved in one call, then each entry contains only its own relations and every requested schema is present carrying at least `subject` | `tests/test_entity_views_generator.py` — resolver shape (task 1.6) | - [x] |
| 71 | entity-view-layer | The query-surface resolver is the one authoritative description of the readable relations | A base-model definition resolves through its label mapping | Given an active definition whose `base_label_mapping` maps a CoNLL label not matching its name, when the surface is resolved, then the definition appears under its own name and the mapped labels are available for associating stored entity data with it | `tests/test_entity_views_generator.py` — resolver shape (task 1.6) | - [x] |
| 72 | entity-view-layer | Provisioning smoke-checks the generated relations | A generated relation is smoke-checked | Given a tenant schema whose surface includes a generated child table, when the smoke check runs under the restricted role, then it attempts a bounded read from that table as well as from each static whitelisted table | `tests/test_sql_execution_privileges.py` (task 2.7) | - [x] |
| 73 | entity-view-layer | Provisioning smoke-checks the generated relations | A missing grant on a generated relation is caught at provisioning time | Given a generated relation the role has no `SELECT` on, when the smoke check runs, then it raises a permission error identifying the relation | `tests/test_sql_execution_privileges.py` (task 2.7) | - [x] |

> **Rule:** Every `#### Scenario:` block in every `specs/**/*.md` file for this change
> MUST appear as a row in this table. A missing scenario is a P1 gap that blocks archive.

---

## 2. Hallucination Risk Register

| # | Risk Area | Potential AI Error | Human Check Required |
|---|-----------|-------------------|----------------------|
| 1 | Single-source surface resolution (design Decision 1) | Introducing a second list of generated relations or columns — a module constant, a hard-coded `subject` column list, or a parallel resolver used only by the prompt — so the prompt, the whitelist, and the grants can drift | Grep `src/` for literal `"subject"`, `"e_"`, and any set of generated table or column names outside `src/shared/entity_views.py`. Confirm `build_role_statements`, `validate_sql`, and the prompt builder each obtain their surface from one `resolve_query_surface` call per question |
| 2 | Base-model label mapping (ADR-008, design Decision 3) | Associating stored entity data with a relation by comparing `entity_type` to the definition `name`, which compiles, passes every fine-tuned fixture, and produces an empty surface association for every base-model tenant | Confirm every association goes through `entity_type_literals` / `build_routing_index`. Add or check a fixture whose only link is `base_label_mapping` and assert its samples land under the right relation |
| 3 | Document-scope column map (design Decision 4) | Rewriting `_DOCUMENT_SCOPE_COLUMNS` as a static dict that lists `subject` but not the generated child tables, or silently returning the statement unchanged when nothing matched | Read the resolved map at a breakpoint or in a test for a tenant with two child tables. Confirm an unscopeable statement under a supplied scope produces a defect, not a `success` — this is the one failure the change exists to close |
| 4 | Defect ladder replacement (design Decision 5) | Deleting a defect detector rather than replacing it, or leaving `entity_type` regexes in place so relational SQL never triggers a defect and every empty result becomes an unretryable `success` | Grep `src/chat_api/services/sql_generator.py` for `entity_type` — remaining hits must be confined to the EAV grounding/oracle reads, never to defect detection or feedback text. Confirm each of the four defect signals in the spec has a code path and a test |
| 5 | Column validation strictness (design Decision 6) | Rejecting a column reference the parser could not attribute to a relation, turning correct queries into validation failures that surface only as "source unavailable" | Review the unattributable-reference branch explicitly. Assert a query using a column alias, a `USING` join, or a computed expression is accepted, not rejected |
| 6 | Coverage probe (design Decision 7) | Treating an empty relational surface as an empty answer, or conversely raising the coverage failure for a tenant that is populated but legitimately matches nothing | Verify both directions: an unprojected tenant produces a failure, and a populated tenant with a genuinely non-matching question produces an empty success. Confirm no EAV fallback query was introduced |
| 7 | Prompt rewrite scope (proposal, design Decision 3) | Retaining EAV instructional fragments — the second `document_entities` join for the subject's name, `entity_type` filtering guidance, one-fact-per-row reasoning — because they were embedded mid-paragraph in the old prompt | Read the generated prompt text end to end for a fixture tenant. Confirm no instruction to query `document_entities`, filter `entity_type`, or self-join entity rows survives, and that aggregation/quantifier guidance was preserved rather than dropped wholesale |

---

## 3. Pattern & ADR Compliance

| ADR | Decision Summary | Constraint on This Change | Verification Step |
|-----|-----------------|--------------------------|-------------------|
| ADR-001-tenant-data-isolation | Separate PostgreSQL schema per tenant; `SET search_path TO tenant_<id>` scopes every query | The surface must resolve per schema from the schema bound out of authenticated request context; no tenant's relations may reach another's prompt, whitelist, or grants | Confirm `schema` is bound once before the attempt loop and never reassigned inside it. Run the two-tenant test (rows 4, 65, 70) and assert no cross-tenant relation name appears. Grep for any surface resolution keyed on a value derived from the user's question |
| ADR-007-chatbot-architecture | Full RAG with structured SQL, semantic search, and NER; SQL passes a validation layer and executes read-only with a timeout; responses carry citations | Validation, the read-only transaction, the 10s timeout, and the least-privilege execution role are unchanged; relational SQL is validated by the same layer, not by a bypass | Confirm every attempt still routes through `validate_sql` and `execute_sql`, that `BEGIN READ ONLY` / `SET LOCAL ROLE` / `COMMIT` are unchanged, and that rows 36 and 39 still pass. Confirm results carry enough columns for citation assembly |
| ADR-008-base-model-as-default | The shared base model is the default; `entity_type` holds CoNLL labels until a tenant fine-tunes | Every association between stored entity data and a relation goes through `base_label_mapping` via `entity_type_literals`; name equality would silently empty every base-model tenant | Run rows 14, 15, and 71 against a fixture whose definitions link only through `base_label_mapping`. Grep for any comparison of `entity_type` to a definition `name` outside `entity_type_literals` |

**Confirmed 2026-08-24 (task 10.4):**

- **ADR-001** — `schema` is bound once in `generate_and_execute` before the coverage probe and the
  attempt loop and is never reassigned inside it; the surface is resolved from that schema alone
  (`_fetch_query_surface`). `TestSurfaceGrounding::test_one_tenants_surface_never_appears_in_anothers_prompt`,
  `TestSecurityInvariants::test_all_attempts_use_request_context_schema`, and the resolver's
  two-schema test cover rows 4, 65, 70. No resolution is keyed on anything derived from the
  question.
- **ADR-007** — every attempt still routes through `validate_sql` then `execute_sql`; `BEGIN READ
  ONLY`, `SET LOCAL ROLE`, `COMMIT` and the 10s `asyncio.timeout` are untouched, asserted by
  `TestSecurityInvariants::test_retries_use_read_only_path` and the timeout tests (rows 36, 39).
  The prompt requires every result row to project `document_id`, which is what citation assembly
  and the graph's secondary scope filter key on.
- **ADR-008** — every entity-data-to-relation association runs through `relation_by_entity_type`,
  which is built from `build_routing_index` over `entity_type_literals`; there is no comparison of
  `entity_type` to a definition `name` anywhere in `sql_generator.py`. `TestBaseModelGrounding`
  covers rows 11, 14, 15 with a `base_label_mapping`-only fixture, including the defect probe.

---

## 4. Evidence Requirements

Evidence that **MUST** be collected and logged in Section 5 before this change is archived.
Do not archive while any item below remains unchecked.

### Functional Evidence

*(One item per row in Section 1 — test output, log excerpt, or trace proving the THEN was observed in a real execution.)*

- [x] Rows 1–3: test output showing the generated prompt names `subject` and `e_skill`, contains no `document_entities` query instruction, and that single-valued and multi-valued questions produce statements against the right relation
- [x] Rows 4, 65, 70: test output for two tenants showing each prompt and each resolved surface contains only its own relations
- [x] Rows 5, 6, 42, 43, 69: test output showing an inactive definition's table and a `single`-flipped retained child table are absent from the prompt, the whitelist, and the grants, and that the `single` definition appears as a `subject` column
- [x] Row 7: test output showing a newly added active `multi` definition appears in the prompt, whitelist, and grants on the next resolution without a code change
- [x] Rows 8, 41: test output asserting equality of the prompt-named, validated, and granted generated-relation sets from one resolver call
- [x] Rows 9, 10, 66, 67: test output showing definition name, description, examples, and declared SQL type reach the prompt and the resolved surface
- [x] Rows 11, 15: test output showing samples are keyed by relation/column, including a `base_label_mapping`-only fixture
- [x] Rows 12, 63: test output showing a relation with no sampled values still appears in the listed surface
- [x] Rows 13, 64: test output for a three-attempt invocation asserting the surface and sample queries ran exactly once
- [x] Rows 14, 71: test output showing a base-label definition appears on the surface under its own name
- [x] Rows 16–19: test output showing the bound `document_id` predicate applied to `subject` and to a child table, surviving aggregation and preceding the row limit, with no scoped identifiers as literals
- [x] Row 20, 54: test output showing an unscopeable statement under a supplied scope is classified as a defect and retried, never `success`
- [x] Row 21: test output for a two-attempt scoped question asserting every executed statement carried the bound predicate
- [x] Rows 22–25, 37, 38, 48: test output showing rejection of an off-surface relation, an undeclared column (with the column named), and another tenant's relation, and acceptance of a valid relational statement
- [x] Rows 26, 47, 59: test output showing a validation failure is retried and the retry prompt carries the reason and the available surface
- [x] Rows 27, 52, 58: test output showing the wrong-relation defect fires, names the holding relation, and contains no `entity_type` reference
- [x] Rows 28, 51, 53: test output showing an unexplained zero-row result and a value-occurs-nowhere result are both `success` with no second generation call
- [x] Rows 29, 49, 56, 57: test output showing an execution error is retried with a sanitized, parameter-stripped, truncated message and the previous SQL
- [x] Row 30: test output showing the filename defect still fires against a `subject.filename` filter
- [x] Rows 31, 32: test output showing an unprojected tenant and an unprojected scoped document both produce a structured-retrieval failure and an unavailable-source turn
- [x] Row 33: test output showing a populated tenant with a genuinely non-matching question returns an empty success
- [x] Row 34: log or test evidence from a re-extraction run showing schema reconciliation, surface population, and a subsequent answer from the relational surface
- [x] Rows 35, 36, 39: test output showing a valid relational SELECT executes read-only, a `DROP TABLE` is rejected and logged with the source reported unavailable, and a >10s query is cancelled with the source skipped
- [x] Rows 40, 44, 45, 46: integration-test output against a real schema showing grant, revoke-before-regrant, reactivation, and the `pg_tables` skip
- [x] Row 50: test output showing a raising or unusable generation call is classified `generation_error` and retried
- [x] Row 55: test output showing a non-empty result is never retried
- [x] Rows 60–62: test output showing the surface with columns and types in the prompt, samples under their relation, and both sample caps respected
- [x] Row 68: test output asserting the resolved relation set equals the reconciler's expected table set
- [x] Rows 72, 73: provisioning-run output showing the smoke check reads every generated relation and raises identifiably when a grant is missing
- [ ] Golden-set eval: `structured_query_success_rate` over `simple_structured`, `exact_entity_lookup`, and `attribute_filtering` before and after the change, with the post-change figure at or above baseline

### Structural Evidence

- [ ] Code review completed — implementation matches design.md decisions (no undocumented deviations)
- [x] All ADR compliance steps in Section 3 confirmed ✓
- [x] No undocumented architectural patterns introduced
- [ ] No AI-invented requirements present in generated code (cross-checked against spec files)
- [x] Scope boundary confirmed: no change to `entity_postprocessor.py`, `entity_normalizer.py`, `relational_projection.py`, `worker.py`, `entity_resolver.py`, the `document_entities` schema, or any migration
- [x] No EAV/relational routing branch exists in the generation path

### Edge Case Evidence

- [x] Risk 1 mitigation confirmed — grep for generated relation/column names outside `entity_views.py` returned nothing, and all three consumers resolve from one call
- [x] Risk 2 mitigation confirmed — every entity-data-to-relation association goes through `entity_type_literals`, verified against a `base_label_mapping`-only fixture
- [x] Risk 3 mitigation confirmed — the resolved scope map covers `subject` and every child table, and an unscopeable scoped statement is a defect
- [x] Risk 4 mitigation confirmed — remaining `entity_type` references in `sql_generator.py` are confined to grounding/oracle reads; all four defect signals have code paths and tests
- [x] Risk 5 mitigation confirmed — unattributable column references are accepted; alias, `USING`, and computed-expression queries verified as passing
- [x] Risk 6 mitigation confirmed — both coverage directions verified, and no EAV fallback query was introduced
- [x] Risk 7 mitigation confirmed — full prompt text read end to end; no EAV instructional fragment survives and the aggregation/quantifier guidance was preserved

---

## 5. Evidence Log

All test runs below were executed on 2026-08-24 against the local Postgres test database
(`venv/Scripts/python -m pytest <file> -q`). Counts are the run's own summary line.

| # | Evidence Type | Description / Link | Scenario(s) Covered | Collected By | Date |
|---|--------------|-------------------|---------------------|--------------|------|
| 1 | Test run | `tests/test_entity_views_generator.py` — 87 passed. `TestQuerySurfaceResolver` asserts declared column types (`DOUBLE PRECISION`/`DATE`/`TEXT`), description and examples on the resolved entry, exclusion of inactive / identifier-less / `single`-retained tables with the `single` definition listed as a `subject` column, two-schema isolation with every requested schema carrying `subject`, `base_label_mapping`-only resolution, equality with `expected_table_names`, and a definition added between two resolutions appearing at the second | 5, 6, 66, 67, 68, 69, 70, 71, 7 | agent | 2026-08-24 |
| 2 | Test run | `tests/test_sql_table_whitelist.py` — 35 passed. `TestRelationalSurfaceValidation` accepts an `e_skill`/`subject` join and a `subject` single-value column read, rejects an off-surface relation, a `single`-retained child table, and another tenant's relation; `TestColumnValidation` rejects `subject.salary`, an undeclared child column, and an undeclared static column, each naming the column; `TestColumnValidationIsPermissiveOnAmbiguity` accepts select aliases, `USING` joins, computed expressions, derived-table aliases, reused aliases, unqualified columns, and dotted string literals | 2, 3, 22, 23, 24, 25, 37, 38 | agent | 2026-08-24 |
| 3 | Test run | `tests/test_sql_execution_privileges.py` — 21 passed against a real schema. `TestGeneratedTableSurface` covers grant/whitelist equality, inactive and `single`-retained exclusion, reactivation, the `pg_tables` skip, and revoke-before-regrant; `TestSmokeCheckCoversGeneratedRelations` reads every relation on the resolved surface under the restricted role and raises `SmokeCheckFailed` naming `e_skill` with "permission denied" when its grant is revoked | 40, 42, 43, 44, 45, 46, 72, 73 | agent | 2026-08-24 |
| 4 | Test run | `tests/test_chat_api_structured_scope.py` — 26 passed. The bound predicate reaches `subject` and `e_skill` (both sides of a join), survives aggregation, precedes the row limit, leaves no identifier literal in the statement, is reapplied to every attempt, and an unscopeable scoped statement is classified `empty_with_defect` with a `scope:` defect and never executed | 16, 17, 18, 19, 20, 21, 54 | agent | 2026-08-24 |
| 5 | Test run | `tests/test_chat_api_sql_retry.py` — 69 passed. Loop bounds and outcome classification, the wrong-relation defect (naming `e_employer`, and `subject.email` for a pivoted column, with `entity_type` absent from the feedback), the filename defect on `subject.filename`, the zero-rows policy, sanitized retry feedback restating the surface, surface/sample grounding with both caps and one fetch per invocation, base-model routing through `base_label_mapping`, the coverage probe in both directions, and the prompt/validated/granted set equality | 4, 9, 10, 11, 12, 13, 14, 15, 26, 27, 28, 29, 30, 31, 32, 33, 41, 47, 48, 49, 50, 51, 52, 53, 55, 56, 57, 58, 59, 60, 61, 62, 63, 64, 65, 8 | agent | 2026-08-24 |
| 6 | Test run | `tests/test_chat_api_sql.py` — 25 passed. `TestSQLPrompt` reads the rendered prompt end to end for a fixture tenant: it names `subject` and `e_skill`, declares `subject.email` and the child column list, carries the `document_id` projection requirement and the preserved reasoning guidance, lists the static tables, and contains neither `document_entities` nor `entity_type` | 1, 35, 36, 37 | agent | 2026-08-24 |
| 7 | Test run | `tests/test_sql_generator_candidate_name.py` — 9 passed, and `tests/test_sql_generator_document_name_fix.py` — 6 passed. The identity-and-evidence directive survives the rewrite in relational terms, the old EAV name-join is gone, and `_fix_document_name_reference` still repairs the static-table path while leaving a relational statement byte-identical | 1, 35 | agent | 2026-08-24 |
| 7b | Test run | `tests/test_sql_filename_defect.py` — 9 passed. The filename literal extractor and the existence probe are unchanged; the feedback now points at the relation or column holding names, and a three-defect render (wrong-relation, scope, validation) contains neither `entity_type` nor `document_entities` | 30, 58, 59 | agent | 2026-08-24 |
| 8 | Test run | `tests/test_chat_api_sql_retry_integration.py` — 13 passed through the real `execute_plan`, `StructuredRetrievalTool`, guardrails and graph nodes. A valid relational SELECT executes read-only and reaches answer generation; `DROP TABLE`, an off-surface relation, a timeout, and an unprojected tenant each produce a failed capability and the controlled fallback rather than an empty answer; the wrong-relation defect consumes a retry and its feedback names `e_employer` with no `entity_type` | 31, 35, 36, 37, 39 | agent | 2026-08-24 |
| 9 | Test run | `tests/test_relational_projection_worker.py::TestReExtractionIsTheMigrationPath` — the documented remedy exercised end to end against a real schema: a tenant left with EAV rows and no relational rows reports a coverage reason, promoting the model version and re-running `run_batch_extraction` reconciles the schema and repopulates `subject` and `e_per`, the coverage reason clears, and the join that could not be answered returns its row | 34 | agent | 2026-08-24 |
| 10 | Test run | `tests/test_retrieval_tools.py`, `tests/test_retrieval_orchestrator.py`, `tests/test_chat_graph_topology.py` — passed unchanged after the `StructuredRetrievalTool.description` reword; `args_schema` and `_scope_to_document_ids` untouched | — (task 8.2) | agent | 2026-08-24 |
| 11 | Static check | `grep -rn "['\"]subject['\"]\|['\"]e_[a-z_]*['\"]" --include=*.py src/ | grep -v entity_views.py` returned nothing, and `TestOneResolverFeedsEveryConsumer::test_no_generated_relation_name_is_hard_coded_outside_entity_views` asserts the same rule in CI | Risk 1 | agent | 2026-08-24 |
| 12 | Static check | `grep -n entity_type src/chat_api/services/sql_generator.py` — every remaining hit is the `document_entities` column set, the frequency-ranked sample query, the re-keying of those samples onto relations, the `SELECT DISTINCT entity_type` oracle read in the wrong-relation probe, or prose. No defect detector and no feedback branch mentions it | Risk 4 | agent | 2026-08-24 |
| 11b | Provisioning run | `scripts/provision_sql_execution_role.py` against the local `ner_dev` database: role `ner_chat_sql` created and granted over 3 tenant schemas, smoke check `OK` on all three; `--check-only` re-run confirms. The first run failed the smoke check on two schemas whose catalogs name generated relations the reconciler has never created — a real defect in the task-2.4 smoke check, now fixed by resolving the relations to read from `pg_tables` first, the same guard each grant already carries | 72, 73, task 9.1 (local environment only) | agent | 2026-08-24 |
| 12b | Suite run | Full suite, `pytest -q --tb=no -rf --continue-on-collection-errors -p no:randomly`: **91 failed, 1763 passed, 29 skipped, 32 errors**, matching the recorded pre-change baseline for this repo (~91 failed / 32 errors on `main`). Zero failures in any file this change touches — `test_entity_views_generator`, `test_sql_table_whitelist`, `test_sql_execution_privileges`, `test_chat_api_structured_scope`, `test_chat_api_sql_retry`, `test_chat_api_sql`, `test_sql_generator_candidate_name`, `test_sql_generator_document_name_fix`, `test_sql_filename_defect`, `test_chat_api_sql_retry_integration`, `test_relational_projection_*`, `test_retrieval_tools`, `test_chat_graph_topology`, `test_chat_api_sql_logging` all report 0. The one chat-adjacent failure, `test_chat_api_rag.py::test_chat_response_sources`, is a pre-existing disclaimer-wording assertion and fails identically in isolation | task 9.3 | agent | 2026-08-24 |
| 13 | Eval | **Not run.** The golden-set eval (`src/shared/retrieval/eval`, `-m eval_gate`) needs a live database, an embedding backend, and an LLM endpoint. `structured_query_success_rate` over `simple_structured`, `exact_entity_lookup`, and `attribute_filtering` must be recorded against the pre-change baseline before this change is archived (task 9.2) | Golden-set eval | — | — |

---

## 6. Audit Record

> ⚠️ **GATE: This section must be completed and signed by a human reviewer before
> `/opsx:archive` is run.** An unsigned or incomplete Audit Record is a hard block on archive.

**Change slug:** relational-only-sql-generation
**Proposal:** `openspec/changes/relational-only-sql-generation/proposal.md`
**Spec files reviewed:**

- specs/relational-sql-generation/spec.md
- specs/chat-api/spec.md
- specs/sql-query-recovery/spec.md
- specs/entity-view-layer/spec.md

### Reviewer Sign-Off

| Check | Status |
|-------|--------|
| Design reviewed against proposal | - [ ] |
| All ADRs in Section 3 verified compliant | - [ ] |
| Spec Alignment table complete (no missing scenarios) | - [ ] |
| Evidence Log populated with real evidence | - [ ] |
| All functional evidence items in Section 4 checked | - [ ] |
| All structural evidence items in Section 4 checked | - [ ] |
| All edge case evidence items in Section 4 checked | - [ ] |

### AI Output Review

| Check | Status |
|-------|--------|
| All generated artifacts reviewed for spec alignment | - [ ] |
| No hallucinated requirements introduced | - [ ] |
| No undocumented patterns used | - [ ] |
| No AI-invented fields, endpoints, or behaviours present | - [ ] |
| Every THEN clause in specs has a corresponding evidence entry | - [ ] |
| Hallucination risk register reviewed and all mitigations confirmed | - [ ] |

**Archive approved by:** ___________________________

**Date:** ___________

**Notes:**
