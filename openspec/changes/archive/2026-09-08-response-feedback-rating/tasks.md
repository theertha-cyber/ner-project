## 1. Database migration

- [x] 1.1 Add Alembic migration `alembic/versions/032_chat_message_feedback.py`: `CREATE TABLE IF NOT EXISTS tenant_template.chat_message_feedback (id, message_id UNIQUE FK -> chat_messages.id, tenant_id, user_id, rating TEXT CHECK (rating IN ('up','down')), created_at)`, following the DDL style of `alembic/versions/010_chatbot_infrastructure.py`. (Numbered 032 — 031 was already taken by `training_jobs_hyperparams_nullable`.)
- [x] 1.2 In the same migration, add `ALTER TABLE tenant_template.chat_messages ADD COLUMN IF NOT EXISTS answer_kind TEXT NOT NULL DEFAULT 'answer'` (values: `answer`, `clarification`, `guardrail_blocked`, `out_of_domain`) and `ADD COLUMN IF NOT EXISTS model_version TEXT NULL`.
- [x] 1.3 Add the backfill block (`DO $$ ... FOR schema_name IN (SELECT nspname FROM pg_namespace WHERE nspname LIKE 'tenant\_%') ... EXECUTE format(...)`) to create the table and add both columns in every already-provisioned `tenant_<id>` schema, mirroring `010_chatbot_infrastructure.py`.
- [x] 1.4 Add a down-migration dropping `chat_message_feedback` and the two new columns from `tenant_template` and every tenant schema.
- [x] 1.5 Ran the migration against the real dev database (`ner_dev` via the `db-init` compose service, `docker compose run --rm db-init`) — `alembic upgrade head` logged `Running upgrade 031 -> 032`, `verify_schema.py` reported "no drift detected". Added `tests/test_migration_032_chat_message_feedback.py`, a real-DB test (connects directly to `NER_DATABASE_URL`/`ner_dev`, not the `ner_test` fixture DB) asserting: `tenant_template.chat_message_feedback` exists with the right columns/UNIQUE/CHECK constraints, `tenant_template.chat_messages` has `answer_kind`(NOT NULL)/`model_version`(nullable), and every live provisioned tenant schema (3 found: `tenant_demo_tenant` + 2 others) was backfilled with both. **Caught a real bug this way**: the first `docker compose run db-init` used a stale pre-migration image (built before this file existed) and silently no-opped — `alembic_version` stayed at `031`. Rebuilt (`docker compose build db-init`) and re-ran; second run applied cleanly. `poetry run pytest tests/test_migration_032_chat_message_feedback.py -q` → 6/6 pass against `ner_dev`.

## 2. chat_api: answer classification and model-version capture

- [x] 2.1 In `src/chat_api/services/rag_orchestrator.py`, added `RAGOrchestrator._classify_answer_kind(result)` static method deriving `answer_kind` from the terminal graph state's `pending_clarification`, `entity_resolution_outcome`, and `blocked_reason` — the classification the graph already computes — rather than re-deriving it from message content.
- [x] 2.2 Added `RAGOrchestrator._extract_model_version(sources)`, reading `model_version` off any source (propagated through `Source`/`Citation` schemas); `None` when no source carries one (i.e. no NER inference call occurred this turn).
- [x] 2.3 `execute_with_clarification` now returns `(reply, sources, pending_clarification, answer_kind, model_version)`; `src/chat_api/api/v1/chat.py`'s `chat()` endpoint persists `answer_kind`/`model_version` on the assistant message INSERT.

## 3. chat_api: feedback persistence and endpoint

- [x] 3.1 Added `FeedbackCreate`/`FeedbackOut` Pydantic models to `src/chat_api/api/v1/schemas.py`; extended `MessageResponse` with `answer_kind: AnswerKind | None`, `model_version: str | None`, `feedback: FeedbackOut | None`; added `model_version` to `Source`/`Citation`.
- [x] 3.2 Implemented `POST /api/v1/chat/messages/{message_id}/feedback` in `src/chat_api/api/v1/chat.py`: 403 for non-`business_user`, 404 for missing/non-assistant/non-`answer`-kind messages, 201 with the created record on success, 409 with the existing rating (unmodified) on duplicate submission (caught via `IntegrityError` on the DB unique constraint).
- [x] 3.3 Updated `get_conversation`'s query to LEFT JOIN `chat_message_feedback` and populate `answer_kind`/`model_version`/`feedback` per message (null for user messages).
- [x] 3.4 Added `POST /api/v1/chat/messages/{message_id}/feedback` proxy route in `src/gateway/api/v1/chat_proxy.py`.

## 4. gateway: feedback analytics for the dashboard

- [x] 4.1 Replaced `_tenant_quota_rows` with `_tenant_feedback_rows` in `src/gateway/api/v1/dashboard.py`: queries `chat_messages`/`chat_message_feedback` for total eligible (`answer_kind='answer'`) assistant message count, rated count, and positive count.
- [x] 4.2 Satisfaction ratio computed strictly as `positive / rated` (never `positive / total`); `rated = 0` renders as `"—"`, not `0%`/`100%`.
- [x] 4.3 `_tenant_admin_data` now sets `sideBot="Response quality"` and `sideRows` to 4 rows (`Satisfaction`, `Total responses`, `Rated`, `Positive`) with green/amber/red/neutral coloring on the Satisfaction row. **Correction from the original task wording**: an earlier draft of this task called for repurposing `big`/`bigUnit`/`sideMetrics` for the ratio — implementing it surfaced that those fields already render the "Active model" eval F1/precision/recall/loss panel (`_model_side_panel_from_active`), so doing so would have silently deleted that still-required panel. Fixed by confining all response-quality data to `sideBot`/`sideRows` only; `sideTop`/`sideMeta`/`big`/`bigUnit`/`bar`/`sideMetrics` are untouched. Specs/design/verification were updated to match (see design.md Decision 4).
- [x] 4.4 Added a defensive `await db.rollback()` at the start of `_tenant_feedback_rows` — discovered during testing that `_tenant_admin_data`'s pre-existing Annotation-stats block can leave the shared session's transaction aborted on failure (its outer `except` doesn't roll back), which would otherwise silently blank the feedback panel too. `sources["feedback"]` is set from whether `_tenant_feedback_rows` returned rows.

## 5. Portal: chat message feedback UI

- [x] 5.1 Extended the portal chat `Message` type (in `MessageThread.tsx` and `chat/page.tsx`) with `answer_kind`, `model_version`, `feedback`.
- [x] 5.2 Added `src/portal/src/components/chat/MessageFeedback.tsx` using `lucide-react`'s `ThumbsUp`/`ThumbsDown`, rendered from `MessageThread.tsx` for assistant messages with `answer_kind === "answer"`, gated by a `canRate` prop set from `useAuth().user?.role === "business_user"` in `chat/page.tsx`.
- [x] 5.3 `chat/page.tsx` `handleRateMessage` posts to the gateway feedback endpoint; on 201 or 409 it syncs local message state to the returned/existing rating.
- [x] 5.4 Fixed/disabled state renders directly from each message's `feedback` field, already included in the `GET conversation` payload — no extra fetch needed.
- [x] 5.5 `MessageFeedback` only renders for `msg.role === "assistant" && msg.answer_kind === "answer"`; never for user messages or other `answer_kind` values. **Known gap**: the optimistically-appended assistant message right after sending (before any reload) has no `answer_kind` yet (`ChatResponse` doesn't carry it), so feedback controls appear only after the conversation is reloaded/reselected, not instantly post-send. Documented rather than fabricating a client-side guess at classification.

## 6. Portal: dashboard response-quality panel

- [x] 6.1 Confirmed `src/portal/src/types/dashboard.ts`'s `SideRow`/`DashboardData` types need no shape change — the response-quality panel reuses `sideBot`/`sideRows` as-is; `sideMetrics` stays the pre-existing fixed 3-tuple (unaffected).
- [x] 6.2 No hardcoded "Quota usage" label exists in the portal (`sideBot` is server-driven) — no portal label change needed beyond the gateway's string.
- [x] 6.3 `MetricsPanel.tsx` already renders `sideRows` as a generic list — confirmed via the `test_dashboard_feedback_panel.py` HTTP-level assertions on the 4 new rows; no component change made.

## 7. Verification tests

- [x] 7.1 Eligibility scenarios (rows 1-6) covered in `tests/test_chat_api_feedback.py` (endpoint-level, since eligibility is enforced at that single point) rather than a separate file — consolidated for less duplication across two spec files describing the same code path.
- [x] 7.2 `tests/test_chat_message_feedback_model.py` — covers rows 11-12 (FK independence, additive-column extension). Rows 7-10 (first-rating-accepted, same/opposite-value duplicate rejection, persistence) covered in `tests/test_chat_api_feedback.py`.
- [x] 7.3 `tests/test_chat_message_model_version.py` — covers rows 13-14, plus the `answer_kind` classification unit tests for `_classify_answer_kind`.
- [x] 7.4 `tests/test_chat_api_feedback.py` — covers rows 15-22 and rows 1-6 (201/409/404×4/403/401), all passing against a real test DB via `ASGITransport` against `src.chat_api.main.app`.
- [x] 7.5 `tests/test_chat_api_feedback.py::test_rating_persists_and_is_visible_via_conversation_get` covers rows 24-26 (GET conversation returns `feedback`/`answer_kind`). Existing `tests/test_chat_api_conversations.py` schema-level tests (rows 23, 27, 28 territory) were not modified — they don't exercise the DB and were unaffected by the new optional fields.
- [x] 7.6 `src/portal/src/components/chat/MessageThread.test.tsx` (new) — 5 tests covering rows 29-34 (rendering unaffected, controls gated by `canRate` + `answer_kind==="answer"`, never on user messages, fixed/disabled state once rated). All passing.
- [x] 7.7 Ran `tests/test_dashboard_summary_roles.py` to confirm no regression from this change (not modified — unaffected). A full run surfaced pre-existing unrelated failures (stat-count mismatches for annotator/business_user) that predate this change and are out of scope.
- [x] 7.8 `tests/test_dashboard_feedback_panel.py` (new) — covers rows 36, 39-41, all passing.
- [x] 7.9 Ran `tests/test_dashboard_summary.py` and `tests/test_dashboard_tenant_enumeration.py` to confirm no regression from this change; both have pre-existing unrelated failures (e.g. `training_jobs.created_at` column mismatch, stray tenant schemas missing `model_versions`) that reproduce independent of any code in this change and are out of scope to fix here.
- [x] 7.10 Portal type-check (`tsc --noEmit`) — no errors from any file touched by this change (`MessageThread.tsx`, `MessageFeedback.tsx`, `chat/page.tsx`, `MetricsPanel.test.tsx`); the full repo `tsc` run has pre-existing unrelated errors in untouched files (annotation, training-jobs, hooks) that predate this change.
- [x] 7.11 `src/portal/src/components/dashboard/MetricsPanel.test.tsx` (extended) — added a test asserting the 4-row Response quality panel renders correctly (Satisfaction/Total responses/Rated/Positive) alongside an unaffected Active model top section; all 8 tests in the file pass, component itself unchanged.

## 8. Deploy: rebuild and restart affected containers

- [x] 8a.1 Rebuilt `db-init`, `chat_api`, `gateway`, `portal` Docker images (`docker compose build ...`) so the running stack picks up this change's code. First `chat_api`/`gateway` build attempt failed mid-way on a transient network error downloading `nvidia-cublas` (a large torch dependency) — the failure was initially masked because the build was piped through `tail`, which always exits 0; caught it by checking `docker images` timestamps (images hadn't changed) and re-running the build with its real exit code surfaced. Retried and it succeeded.
- [x] 8a.2 `docker compose run --rm db-init` (real dev DB `ner_dev`) — applied migration 032, reseeded (idempotent, no-ops on existing demo data), `verify_schema.py` passed.
- [x] 8a.3 `docker compose up -d --force-recreate chat_api gateway portal` — all three recreated from the new images and started cleanly (checked `docker logs` for each: no startup errors).
- [x] 8a.4 End-to-end smoke test against the live stack (gateway on `localhost:8000`, real `ner_dev` DB): logged in as demo `business_user` and `tenant_admin`, sent a real chat message (classified `answer_kind: "answer"`), submitted `POST /messages/{id}/feedback` (201), duplicate submission (409, unchanged), `tenant_admin` attempt (403), `GET` conversation reflecting the persisted rating, and `GET /dashboard/summary` showing `sideBot: "Response quality"` with the ratio updating live (0/1 rated → 100% after the rating) while `sideTop`/`big`/`bigUnit` (Active model F1) stayed unaffected.
- [x] 8a.5 Verified the same flow in the portal UI via browser automation: logged in as `bizuser@democorp.io`, opened `/chat`, confirmed Thumbs Up/Down render on the eligible assistant message, clicked Thumbs Up, confirmed `aria-pressed="true"` and both buttons `disabled`, then did a full page reload and re-selected the conversation — rating still showed fixed (persistence across reload/session confirmed in the real browser, not just the test DB).

## 8b. Response Quality card redesign (raw metrics → interpreted health report)

- [x] 8b.1 Backend: replaced `_tenant_feedback_rows` (4-row `sideBot`/`sideRows` list) with `_tenant_response_quality_card` in `src/gateway/api/v1/dashboard.py`, returning a new `ResponseQualityCard` Pydantic model on `DashboardData.responseQuality`: `{status, satisfactionPct, positive, negative, rated, total, recommendation}`. `status` derived as `no_data`/`healthy`/`monitor`/`needs_attention` from the same rated-only ratio; `recommendation` a fixed sentence per status, computed server-side (single source of truth, not left for the client to derive). `sideBot`/`sideRows` are now `""`/`[]` for `tenant_admin`.
- [x] 8b.2 Portal types: added `ResponseQuality`/`ResponseQualityStatus` to `src/portal/src/types/dashboard.ts`, `DashboardData.responseQuality?`.
- [x] 8b.3 New component `src/portal/src/components/dashboard/ResponseQualityCard.tsx`: status badge (icon + colour-coded label), headline percentage + "Positive Feedback" + reviewed-count subtext, a single two-segment (positive/negative) stacked bar tying the two counts together instead of four independent bars, a "Business User Feedback" section naming the source explicitly with thumbs-up/down counts and an "N of M AI responses reviewed" sample-size sentence, a divider, and a "Recommendation" section with icon + interpreted text.
- [x] 8b.4 Wired into `src/portal/src/app/(auth)/dashboard/page.tsx`: rendered below `MetricsPanel` for `tenant_admin` only, when `data.responseQuality` is present.
- [x] 8b.5 Removed the now-stale `MetricsPanel.test.tsx` case that asserted 4 generic sideRows for "Response quality" (no longer how this data is delivered); added `ResponseQualityCard.test.tsx` (4 tests: healthy, needs_attention, monitor, no_data) — all pass. Rewrote `tests/test_dashboard_feedback_panel.py` to assert the `responseQuality` object shape and all 4 statuses (including new monitor/needs_attention coverage) — 6/6 pass against a real DB-backed test.
- [x] 8b.6 `npx tsc --noEmit` — zero errors in any file this redesign touches.
- [x] 8b.7 Rebuilt and redeployed: `docker compose build gateway portal` (succeeded), `docker compose up -d --force-recreate gateway portal`, containers started cleanly. Live smoke test against `localhost:8000` confirmed `sideBot`/`sideRows` are now empty and `responseQuality` is populated correctly. Verified in the portal UI via browser automation as `admin@democorp.io` (tenant_admin, already-authenticated session): the live dashboard rendered exactly the target layout — "Response Quality" / "Healthy" badge / "100%" / "Positive Feedback" / "1 of 1 reviewed responses were positive" / "Business User Feedback" / "1 of 2 AI responses reviewed" / "Recommendation" / "No retraining recommended..." — matching the requested design's healthy-state example.
- [x] 8b.8 Updated design.md Decision 4, and specs/dashboard-summary-endpoint + specs/portal-dashboard (including a new ADDED "Response Quality Card" requirement in portal-dashboard) to describe the final `responseQuality`-object shape instead of the superseded 4-row `sideRows` design.

## 9. Verification & Evidence

- [x] 9.1 Ran acceptance-criteria tests for the large majority of scenarios in verification.md § Spec Alignment — all executed tests pass, including a real-DB migration test (Section 1) and a live-stack smoke test (Section 8). A minority of scenarios (23, 26-28, 30, 35, 37-38, 42-46, 49, 51-54) are unaffected pre-existing behavior not independently re-verified this session; see § Spec Alignment Status column and § Evidence Requirements for the exact breakdown.
- [x] 9.2 Collected functional evidence — see verification.md § Evidence Log and the per-scenario checkboxes in § Evidence Requirements.
- [x] 9.3 Confirmed all 9 Hallucination Risk mitigations in verification.md § Hallucination Risk Register (including Risk 6, discovered and fixed during implementation — see design.md Decision 4).
- [x] 9.4 Confirmed all 4 ADR compliance steps in verification.md § Pattern & ADR Compliance.
- [ ] 9.5 Audit Record sign-off in verification.md § Audit Record — **requires a human reviewer**; not completed by this agent.
- [x] 9.6 Ran `openspec validate response-feedback-rating --strict` — passes clean.
