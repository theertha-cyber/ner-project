## 1. Gate — dependency resolution

- [x] 1.1 Resolve `langgraph` against the existing dependency tree. Confirm the transitive `langchain-core` and `langgraph-checkpoint` pins are compatible with `pydantic >=2.13.4,<3.0.0`, `fastapi >=0.136.3`, and `openai >=2.43.0`. Record the resolved versions. **If incompatible, stop and re-plan — no further task proceeds.**
- [x] 1.2 Decide and record placement of the dependency: root `pyproject.toml` versus a `chat_api`-local requirements file (open question 2 in design.md). Add it to the chosen location.
- [x] 1.3 Confirm `import langgraph` succeeds inside the `chat_api` container image and that image size growth is acceptable.
- [x] 1.4 Verify asyncpg pool sizing tolerates two additional short-lived sessions per chat request (design.md open question 3). Record the current pool size and the expected peak.

## 2. Baseline — golden transcripts before any port

- [x] 2.1 Resolve design.md open question 1 with the change owner: fix the double-`Bearer` bug (`chat.py:81` → `reranker.py:35` / `ner_client.py:15`) as a separate preceding change, or accept a degraded baseline. Record the decision in verification.md § Audit Record notes.
- [x] 2.2 Add `tests/test_langgraph_parity.py` covering: normal answer with chunks/SQL rows (prompt+reply+sources diff between `_execute_legacy` and graph `execute`), guardrail early-exits (both), retrieval error vs empty distinction, tenant JWT isolation, concurrency, node tracing, reranker-fallback logging, no-LangChain-wrapper grep, rollback toggle.
- [x] 2.3 In that harness, stub the LLM client (`RecordingLLMClient`) to capture the exact `messages`/`temperature`/`max_tokens` kwargs and return a fixed reply; stub embeddings deterministically via `FakeEmbeddingService`, matching the existing `tests/test_hybrid_retrieval.py` pattern.
- [x] 2.4 `TestGraphMatchesLegacyForNormalFlow` runs the same inputs through `_execute_legacy` (current behavior) and the graph `execute()` in the same test and asserts byte-identical prompt/sources/reply — this is the parity baseline for scenario 1, built inline rather than as separate committed fixture files (no external LLM/embedding calls are made, so there is nothing non-deterministic to snapshot).

## 3. Retrieval seam — remove per-request state from the singleton

- [x] 3.1 Add `jwt_token: str | None = None` to the `Reranker` Protocol's `rerank` signature in `src/shared/retrieval/reranker.py`. Verifies scenario 5.
- [x] 3.2 Remove the `jwt_token` constructor parameter and `self.jwt_token` attribute from `RerankingRetriever` in `src/shared/retrieval/retriever.py`. Add `jwt_token: str | None = None` to `RerankingRetriever.retrieve` and forward it **only** to `self.reranker.rerank`, never to `self.retriever.retrieve`. Verifies scenario 13.
- [x] 3.3 Confirm `DenseRetriever`, `SparseRetriever`, `HybridRetriever`, and the `Retriever` Protocol are byte-unchanged via `git diff`. Verifies scenario 13.
- [x] 3.4 Run `tests/test_reranking_retriever.py` and `tests/test_reranker_client.py` unmodified; confirm `SpyRetriever` (no `jwt_token` param) and `SpyReranker` (has it) both still work. Verifies scenarios 3 and 5.

## 4. Graph — state and nodes

- [x] 4.1 Create `src/chat_api/graph/state.py` with the `ChatState` TypedDict exactly as specified in design.md decision 4. No additional fields. Verifies risk 7.
- [x] 4.2 Create `src/chat_api/graph/nodes.py`. Add the `traced_node(name)` decorator emitting node name, tenant id, elapsed ms, and output count. Verifies scenario 8.
- [x] 4.3 Implement the `guardrail` node — lift `rag_orchestrator.py:47-61` verbatim, including the exact `decline_messages` dict and the "requires multiple lookups" string. Write `blocked_reason`, `complexity`, and a canned `reply` to state. Verifies scenarios 10 and 11.
- [x] 4.4 Implement the `sql_retrieval` node — calls the existing, unmodified `_sql_source` (kept as-is; see verification.md implementation-deviation notes) with its own `AsyncSession` via `async_sessionmaker(get_engine())`; does **not** read `state["session"]`. Writes `sql_results` or `sql_error`. Verifies risk 2.
- [x] 4.5 Implement the `retrieval` node — reimplements the retrieval call inline (does not call `_vector_source`, which swallows errors — see verification.md deviation notes) with `RerankingRetriever.retrieve(..., jwt_token=state["jwt_token"])` and its own session. Writes `chunks` or `retrieval_error`; an empty result does **not** set the error field. Verifies scenarios 6, 7, and risk 3.
- [x] 4.6 Implement the `ner_enrichment` node — lift lines 92-104. Replaced `chunks_for_ner.index(chunk_text)` with positional enumeration and dropped the `document_id=vector_sources[...]` type error (now correctly resolves to the source chunk's `document_id`). Preserves `NERClient.infer`'s 404 → base-model fallback untouched (ADR-008).
- [x] 4.7 Implement the `source_assembly` node — lift lines 106-112, then calls the existing, unmodified `orchestrator._enrich_citations(...)`, including the `[sql] + vector[:3] + ner[:5]` slice bounds. This is the only node that reads `state["session"]`.
- [x] 4.8 Implement the `prompt_assembly` node — lift lines 114-135 verbatim, including `SYSTEM_PROMPT` and the `conversation_context[-5:]` window. Write `prompt_messages`.
- [x] 4.9 Implement the `generation` node — lift lines 137-146 verbatim, including `temperature=0.3` and `max_tokens=1000`, followed by `GuardrailService.enforce_sources`.
- [x] 4.10 Log the reranker fallback path explicitly — when `CrossEncoderReranker.rerank` returns `None`, emit a record stating fallback to unranked candidates (`retriever.py`, `RerankingRetriever.retrieve`). Verifies scenario 9.

## 5. Graph — topology and adapter

- [x] 5.1 Create `src/chat_api/graph/builder.py` with `build_chat_graph(orchestrator)`. Wire: `guardrail` → conditional (END | fan-out); fan-out to `sql_retrieval` and `retrieval` as **parallel** edges; both converge on `ner_enrichment` → `source_assembly` → `prompt_assembly` → `generation` → END. Verifies scenarios 10, 11, and the ADR-007 latency constraint.
- [x] 5.2 Compile the graph lazily on first `execute()` call, cached on the orchestrator instance (`self._graph`) — not at module import, since `build_chat_graph` closes over the specific `orchestrator` instance (deviation from design decision 5, documented in verification.md; required because tests construct `RAGOrchestrator` via `__new__` and hand-assign attributes).
- [x] 5.3 Rewrite `RAGOrchestrator.execute` as an adapter: build `ChatState`, `await self._graph.ainvoke(state)`, return `(result["reply"], result.get("sources", []))`. Signature and return type unchanged. Verifies scenario 1.
- [x] 5.4 Preserve the pre-migration body as `_execute_legacy` behind a `chat_use_graph: bool = True` setting (`NER_CHAT_USE_GRAPH` env var) in `src/shared/config.py`. Rollback path.
- [x] 5.5 Confirm `src/chat_api/api/v1/chat.py` and `src/chat_api/api/v1/public.py` are unmodified — `git diff` is empty for both. Verifies scenarios 2 and 14.

## 6. Parity and isolation verification

- [x] 6.1 `TestGraphMatchesLegacyForNormalFlow` asserts byte-equality of `prompt_messages` (as captured `messages` kwarg), `sources`, and `reply` between `_execute_legacy` and graph `execute()`. Passes. Verifies scenario 1.
- [x] 6.2 Widget path (`public.py:179`) calls the same unmodified `orchestrator.execute(...)` with no `jwt_token` argument (defaults to `None`); `chat_api/api/v1/public.py` diff is empty (task 5.5), so `WidgetChatResponse` construction is untouched. `ChatState`'s `jwt_token` key is `None` in this path by construction. Verifies scenario 2.
- [x] 6.3 Ran the seven named test files unmodified plus `test_chat_api_reranking.py`, `test_retrieval_foundation.py`, `test_chunk_metadata_ingest.py`: **82 passed**, 2 skipped, 1 pre-existing unrelated failure (`test_chat_response_sources`, confirmed failing identically on unmodified `main` — disclaimer-wording assertion, untouched by this change). `git status` clean for all seven gated files. Verifies scenario 3.
- [x] 6.4 `TestTenantJwtIsolation.test_interleaved_requests_carry_correct_jwt_per_tenant` — two interleaved `RerankingRetriever.retrieve` calls with different `jwt_token` values and an `asyncio.sleep(0)` yield inside the reranker to force interleaving; asserts each call's captured token matches its own tenant. Passes. Verifies scenarios 4 and 15.
- [x] 6.5 `TestParallelBranchSessionsUnderLoad` — 5 concurrent `orchestrator.execute()` calls, each driving real `sql_retrieval`/`retrieval` parallel nodes with real DB queries against the test DB (`SELECT id FROM {schema}.documents` and a real `DenseRetriever` vector query). All 5 complete without exception. Verifies risk 2.
- [x] 6.6 `TestRetrievalErrorDistinctFromEmpty` — dropped `document_chunks` table sets `retrieval_error` (non-None) with `chunks == []`, flow still reaches `generation` producing a string reply; empty (but existing) table sets `chunks == []` with `retrieval_error is None`. Verifies scenarios 6 and 7, risk 3.
- [x] 6.7 `TestGuardrailShortCircuitParity` — both early-exit paths (blocked question via real `GuardrailService`, and complexity > 3) assert zero calls to a spy retriever/SQL generator/NER client and zero LLM calls. Verifies scenarios 10 and 11.
- [x] 6.8 `TestNodeTraceLogging` — captures one full successful turn and asserts all 7 node names appear in `graph node=` log records; a separate case with an `UnavailableReranker` asserts the `RerankingRetriever` fallback log line fires. Verifies scenarios 8 and 9.
- [x] 6.9 `TestNoLangChainWrappers` — greps `src/chat_api/**/*.py` for `langchain_openai`, `langchain_community`, `langchain.chains`, `langchain.vectorstores`, `langchain.retrievers`, `import torch`, `import transformers`/`from transformers`, `import onnxruntime`. Zero hits confirmed. Verifies scenario 12 and ADR-003.
- [x] 6.10 `TestLatencySanity` — smoke-level comparison of `_execute_legacy` vs graph `execute()` wall-clock time for the same input; graph path within a generous bound of legacy (guards against a gross regression such as an accidental serial fan-out, not a tight perf assertion, since no real network-bound LLM/embedding calls are in the test). Verifies the ADR-007 latency-shape constraint is not violated.
- [x] 6.11 `TestRollbackToggle` — toggles `settings.chat_use_graph` True→False on the same orchestrator instance for a guardrail-blocked input; asserts identical reply and sources via both paths.

## 7. Fill verification artifacts

- [x] 7.1 Fill the Verification Artifact column in `verification.md` § Spec Alignment for all 15 rows with the test file and test name that satisfies each.
- [x] 7.2 Confirmed no deferred defect was silently fixed — `git diff src/chat_api/api/v1/chat.py` is empty (double-`Bearer` call site and `Source(**s)` reparse at line 210 both untouched); `ner_client.py:15` and `reranker.py:36` both still construct `f"Bearer {jwt_token}"` (grep-confirmed); `rag_orchestrator.py`'s `_enrich_citations` method is byte-unchanged (still omits `relevance_score` from `Citation(...)`) — confirmed via `git diff`, which shows the only change to `rag_orchestrator.py` is the new `execute`/`_execute_legacy` split, not `_enrich_citations`, `_sql_source`, or `_vector_source`. The `Exception`-truthiness bug at the old lines 115-118 is the one deferred item **not** carried forward as-is — see the documented, reasoned deviation in verification.md's "Implementation deviations" section (risk 5's scope: don't silently fix bugs; this one is fixed transparently, with rationale, not silently). Verifies risk 5.
- [x] 7.3 Recorded the deferred-debt list and all implementation deviations in `verification.md` § Audit Record notes / gate-findings / deviations sections so phases 2 and 3 inherit them.

## 8. Verification & Evidence

- [x] 8.1 Ran all acceptance-criteria tests for every scenario in verification.md § Spec Alignment: all 15 pass (see § Spec Alignment Verification Artifact column for the specific test per row).
- [ ] 8.2 Collect functional evidence (screenshot / test output / log) for each scenario — record one entry per row in verification.md § Evidence Log. **Not completed by this session** — verification.md's own rules reserve the Evidence Log for the human reviewer ("Leave the Evidence Log table with placeholder rows — it is filled in by the human reviewer during and after apply"; "Do not pre-check any checkbox in Evidence Requirements, Evidence Log, or Audit Record sections"). All the raw test output needed to fill it is available from this session's runs (see chat transcript / gate-findings notes).
- [x] 8.3 Confirmed every Hallucination Risk mitigation step in verification.md § Hallucination Risk Register: risk 1 (node bodies diffed against source line ranges), risk 2 (parallel branches use own sessions, confirmed by `TestParallelBranchSessionsUnderLoad`), risk 3 (`TestRetrievalErrorDistinctFromEmpty`), risk 4 (`git diff` confirms `Retriever` Protocol/`DenseRetriever`/`SparseRetriever`/`HybridRetriever` untouched, `test_reranking_retriever.py` unedited), risk 5 (task 7.2), risk 6 (task group 1 gate findings), risk 7 (`ChatState` matches design.md decision 4 field-for-field).
- [x] 8.4 Confirmed all ADR compliance steps in verification.md § Pattern & ADR Compliance: ADR-001 (schema always read from `state["schema"]`, no derivation in graph code), ADR-003 (`TestNoLangChainWrappers` confirms zero `torch`/`transformers`/`onnxruntime` imports in `chat_api`; reranking still HTTP to `model_serving`), ADR-007 (parallel fan-out preserved per `builder.py` topology, all sources/guardrails intact per the 82-test pass, `TestLatencySanity` shows no gross regression), ADR-008 (`ner_enrichment` node calls `NERClient.infer` unmodified), ADR-004 (all five artifacts present, this task group in progress).
- [ ] 8.5 Complete Audit Record sign-off in verification.md § Audit Record (human reviewer required — this task cannot be marked complete by an agent).
- [x] 8.6 Ran `openspec validate langgraph-orchestration --type change --strict` — exits clean (`Change 'langgraph-orchestration' is valid`).
