# Verification Plan

**Change:** tenant-postgresql-data-plane
**Generated:** 2026-09-14
**Status:** 🟡 Spec Alignment, Evidence Requirements, and Evidence Log complete (all 77 scenarios verified against real infrastructure, zero regressions). 🔴 Section 6 Audit Record still requires a human reviewer's sign-off before `/opsx:archive` — see the gate note there.

---

## 1. Spec Alignment

Map every requirement and every scenario in this change to a testable acceptance criterion.
Each row drives one evidence entry in Section 5.

| # | Capability | Requirement | Scenario | Acceptance Criterion | Verification Artifact | Status |
|---|-----------|-------------|----------|---------------------|-----------------------|--------|
| 1 | tenant-data-plane-routing | Every tenant has exactly one data plane recorded in the control plane | Existing tenants are backfilled to the platform plane | Given tenants provisioned before this change; when the migration introducing `public.tenant_data_planes` is applied; then each tenant SHALL have one data-plane record with `mode = platform` and `status = ready`; and no tenant SHALL lack a data-plane record | pytest: `tests/test_tenant_data_plane_record.py` | - [x] |
| 2 | tenant-data-plane-routing | Every tenant has exactly one data plane recorded in the control plane | Mode cannot be changed | Given a tenant with `mode = platform`; when any API or service attempts to set its `mode` to `tenant_owned`; then the system SHALL reject the change with finite safe code `DATA_PLANE_MODE_IMMUTABLE`; and the record SHALL be unchanged | pytest: `tests/test_tenant_data_plane_record.py` | - [x] |
| 3 | tenant-data-plane-routing | Every tenant has exactly one data plane recorded in the control plane | Data-plane record carries no sensitive values | Given a `tenant_owned` tenant with an active data-plane connection; when its data-plane record is read through any API; then the response SHALL contain mode, status, reason class, schema revision, and timestamps only; and it SHALL NOT contain a host, database name, username, secret reference value, or provider error text | pytest: `tests/test_tenant_data_plane_record.py` | - [x] |
| 4 | tenant-data-plane-routing | Tenant data engines are resolved per tenant with no platform fallback | Platform tenant resolves to the platform engine | Given a tenant with `mode = platform`; when the resolver is asked for that tenant's engine; then it SHALL return the platform engine | pytest: `tests/test_tenant_engine_resolver.py` | - [x] |
| 5 | tenant-data-plane-routing | Tenant data engines are resolved per tenant with no platform fallback | Residency tenant resolves to its own store | Given a tenant with `mode = tenant_owned`, `status = ready`, and an active data-plane connection; when a document-service request for that tenant reads its documents; then the query SHALL execute against the tenant's store; and no statement for that tenant SHALL execute against the platform database | pytest: `tests/test_tenant_engine_resolver.py` | - [x] |
| 6 | tenant-data-plane-routing | Tenant data engines are resolved per tenant with no platform fallback | Residency tenant never falls back to the platform engine | Given a `tenant_owned` tenant whose connection is paused or whose secret cannot be resolved; when any service or worker requests that tenant's engine; then the resolver SHALL raise a typed data-plane error; and the platform engine SHALL NOT be returned | pytest: `tests/test_tenant_engine_resolver.py` | - [x] |
| 7 | tenant-data-plane-routing | Tenant data engines are resolved per tenant with no platform fallback | Workers use the resolver | Given the extraction, training, analytics, OCR, and blob-sync workers; when their tenant-schema database access is inspected by an automated source check; then no tenant-schema access SHALL construct an engine from the platform database URL directly | pytest architecture check: `tests/test_tenant_engine_construction_boundary.py` | - [x] |
| 8 | tenant-data-plane-routing | Tenant data engines are resolved per tenant with no platform fallback | Engine cache is invalidated on connection change | Given a cached engine for a `tenant_owned` tenant; when that tenant's data-plane connection is paused, replaced, or retired; then the cached engine SHALL be disposed before the next resolution; and the next resolution SHALL reflect the new connection state | pytest: `tests/test_tenant_engine_resolver.py` | - [x] |
| 9 | tenant-data-plane-routing | Tenant content routes are gated on data-plane readiness | Awaiting-store tenant cannot upload | Given a `tenant_owned` tenant in `awaiting_store`; when a tenant user uploads a document; then the response SHALL be 409 with code `TENANT_DATA_PLANE_NOT_READY` and status class `awaiting_store`; and no bytes SHALL be written to any platform or tenant store | pytest: `tests/test_data_plane_route_gate.py` | - [x] |
| 10 | tenant-data-plane-routing | Tenant content routes are gated on data-plane readiness | Awaiting-store tenant administrator can configure the store | Given a `tenant_owned` tenant in `awaiting_store`; when its tenant administrator opens data-source settings and creates a data-plane connection draft; then the operation SHALL succeed | pytest: `tests/test_data_plane_route_gate.py` | - [x] |
| 11 | tenant-data-plane-routing | Fleet operations enumerate tenants from the control plane | Reconciler reaches a residency tenant | Given one `platform` tenant and one `ready` `tenant_owned` tenant, each with an active entity definition; when the entity-view reconciler runs; then generated entity tables SHALL be reconciled in the platform schema for the first tenant and in the tenant store for the second | pytest (residency integration): `tests/test_data_plane_fleet_operations.py` | - [x] |
| 12 | tenant-data-plane-routing | Fleet operations enumerate tenants from the control plane | One unreachable store does not abort the fleet run | Given three tenants, one of which is `tenant_owned` with an unreachable store; when chat query-role provisioning runs; then the other two tenants SHALL be provisioned; and the unreachable tenant SHALL be reported with a safe outcome class only | pytest (residency integration): `tests/test_data_plane_fleet_operations.py` | - [x] |
| 13 | tenant-residency-store-provisioning | Activating a data-plane connection provisions the tenant schema in the tenant store | Successful provisioning makes the tenant ready | Given a `tenant_owned` tenant in `awaiting_store` and a reachable empty tenant store; when the tenant administrator activates its data-plane connection; then the tenant SHALL enter `provisioning` and then `ready`; and the tenant store SHALL contain `tenant_<id>` with every table, index, generated column, and materialized view present in the platform tenant template at the same revision; and `platform_store_meta` SHALL record the store identity and the applied revision | pytest (residency integration, `postgres-tenant-store`): `tests/test_tenant_store_provisioning.py` | - [x] |
| 14 | tenant-residency-store-provisioning | Activating a data-plane connection provisions the tenant schema in the tenant store | Provisioning failure is safe and retryable | Given a tenant store where schema creation fails part-way; when provisioning runs; then the tenant SHALL enter `provisioning_failed` with a finite safe reason class; and a retry SHALL complete provisioning without error on the objects that already exist | pytest (residency integration, `postgres-tenant-store`): `tests/test_tenant_store_provisioning.py` | - [x] |
| 15 | tenant-residency-store-provisioning | Activating a data-plane connection provisions the tenant schema in the tenant store | Provisioning refuses a non-empty foreign schema | Given a tenant store that already contains a `tenant_<id>` schema with tables but no matching store identity marker; when provisioning runs; then it SHALL stop with reason class `target_schema_not_empty`; and it SHALL NOT alter or drop any existing object | pytest (residency integration, `postgres-tenant-store`): `tests/test_tenant_store_provisioning.py` | - [x] |
| 16 | tenant-residency-store-provisioning | Activating a data-plane connection provisions the tenant schema in the tenant store | No platform schema is created for a residency tenant | Given a `tenant_owned` tenant that has been provisioned; when the platform database is inspected; then no `tenant_<id>` schema for that tenant SHALL exist on the platform database | pytest (residency integration, `postgres-tenant-store`): `tests/test_tenant_store_provisioning.py` | - [x] |
| 17 | tenant-residency-store-provisioning | Tenant-store schema is defined by a versioned baseline and ordered revisions | Missing tenant-store revision fails the build | Given a new Alembic migration that adds a column to `tenant_template.documents`; and no corresponding tenant-store revision; when the tenant-store parity test runs; then the test SHALL fail naming the differing table and column | pytest: `tests/test_tenant_store_parity.py` | - [x] |
| 18 | tenant-residency-store-provisioning | Tenant-store schema is defined by a versioned baseline and ordered revisions | Re-applying a revision is a no-op | Given a tenant store already at revision N; when revision N is applied again; then no error SHALL occur and the schema shape SHALL be unchanged | pytest: `tests/test_tenant_store_revisions.py` | - [x] |
| 19 | tenant-residency-store-provisioning | Pending tenant-store revisions are applied per store on deploy | Deploy upgrades a residency store | Given a `ready` residency tenant at revision N and a deploy containing revision N+1; when the deploy migration step runs; then the store SHALL be at revision N+1 and the tenant SHALL remain `ready` | pytest (residency integration): `tests/test_tenant_store_migrate.py` | - [x] |
| 20 | tenant-residency-store-provisioning | Pending tenant-store revisions are applied per store on deploy | Unreachable store during deploy is isolated | Given two residency tenants, one with an unreachable store, and a deploy whose minimum supported revision is N+1; when the deploy migration step runs; then the reachable store SHALL be upgraded; and the unreachable tenant SHALL become `migration_required`; and platform services SHALL start normally | pytest (residency integration): `tests/test_tenant_store_migrate.py` | - [x] |
| 21 | tenant-residency-store-provisioning | Replacement connections must point at the same store | Credential rotation keeps the tenant ready | Given a `ready` residency tenant and a replacement draft with a new secret reference pointing at the same store; when the replacement is tested and activated; then the tenant SHALL remain `ready` without re-provisioning; and the resolver SHALL use the replacement connection | pytest (residency integration): `tests/test_data_plane_connection_replacement.py` | - [x] |
| 22 | tenant-residency-store-provisioning | Replacement connections must point at the same store | Pointing at a different server is rejected | Given a `ready` residency tenant and a replacement draft pointing at a different server; when the replacement is tested; then the test outcome SHALL be failed with reason class `store_identity_mismatch` | pytest (residency integration): `tests/test_data_plane_connection_replacement.py` | - [x] |
| 23 | tenant-data-plane-failure-isolation | An unreachable tenant store fails closed for that tenant only | Chat fails closed during a store outage | Given a `ready` residency tenant whose store is unreachable; when a tenant user sends a chat message; then the response SHALL be 503 with code `TENANT_DATA_PLANE_UNAVAILABLE`; and no conversation or message row SHALL be written to the platform database | pytest: `tests/test_data_plane_failure_isolation.py` | - [x] |
| 24 | tenant-data-plane-failure-isolation | An unreachable tenant store fails closed for that tenant only | Other tenants are unaffected | Given a residency tenant with an unreachable store and a platform tenant; when both tenants list documents concurrently; then the platform tenant SHALL receive its documents with HTTP 200; and the residency tenant SHALL receive 503 `TENANT_DATA_PLANE_UNAVAILABLE` | pytest: `tests/test_data_plane_failure_isolation.py` | - [x] |
| 25 | tenant-data-plane-failure-isolation | An unreachable tenant store fails closed for that tenant only | Driver error text is not exposed | Given a store outage that raises a driver error containing the host name; when the error is returned, logged, and traced; then none of those outputs SHALL contain the host name or driver message | pytest: `tests/test_data_plane_failure_isolation.py` | - [x] |
| 26 | tenant-data-plane-failure-isolation | Uploads are rejected before bytes are accepted when the store is unavailable | Upload during outage writes nothing | Given a residency tenant whose store is unreachable; when a user uploads a document; then the response SHALL be 503 `TENANT_DATA_PLANE_UNAVAILABLE`; and no working copy, registry row, or document row SHALL be created | pytest: `tests/test_data_plane_failure_isolation.py` | - [x] |
| 27 | tenant-data-plane-failure-isolation | Background tasks retry with bounded backoff and then park | Extraction parks after bounded retries | Given a batch extraction task for a residency tenant whose store stays unreachable; when the retry budget is exhausted; then the task SHALL stop in a failed-retryable state with reason class `data_plane_unavailable`; and its payload SHALL contain identifiers only | pytest: `tests/test_data_plane_task_retry.py` | - [x] |
| 28 | tenant-data-plane-failure-isolation | Background tasks retry with bounded backoff and then park | No content is buffered on the platform | Given OCR has produced text for a residency tenant's document and the store becomes unreachable before the spans are written; when the OCR task fails; then the produced text SHALL be discarded, not persisted on the platform; and the document SHALL be reprocessable from its source or working copy after recovery, subject to its retention mode | pytest: `tests/test_data_plane_task_retry.py` | - [x] |
| 29 | tenant-data-plane-failure-isolation | Per-tenant store health is a content-free control-plane signal | Service stays ready during a tenant outage | Given a residency tenant whose store is unreachable; when `/health` is requested on any service; then the response SHALL be HTTP 200 if platform dependencies are reachable; and the tenant's recorded health outcome SHALL be `unreachable` | pytest: `tests/test_data_plane_health.py` | - [x] |
| 30 | tenant-data-plane-failure-isolation | Per-tenant store health is a content-free control-plane signal | Recovery is detected | Given a residency tenant recorded as `unreachable`; when its store becomes reachable and the probe runs; then its health outcome SHALL become `healthy`; and content routes SHALL succeed again without operator action | pytest: `tests/test_data_plane_health.py` | - [x] |
| 31 | tenant-document-registry | Content-free document registry in the control plane | Registry schema holds no content columns | Given the migration creating `public.tenant_document_registry`; when its columns are inspected; then it SHALL contain no `filename`, `storage_uri`, `blob_path`, `error_message`, or text-content column | pytest: `tests/test_tenant_document_registry.py` | - [x] |
| 32 | tenant-document-registry | Content-free document registry in the control plane | Existing documents are backfilled | Given a platform tenant with 5 documents before this change; when the migration is applied; then the registry SHALL contain 5 rows for that tenant with matching id, status, size, and checksum | pytest: `tests/test_tenant_document_registry.py` | - [x] |
| 33 | tenant-document-registry | The registry follows the tenant store, which stays authoritative | Status transitions reach the registry | Given a residency tenant's document moving from `processing` to `processed`; when the OCR worker commits the status in the tenant store; then the registry row SHALL show `processed` | pytest: `tests/test_tenant_document_registry.py` | - [x] |
| 34 | tenant-document-registry | The registry follows the tenant store, which stays authoritative | Drift is reconciled | Given a registry row showing `processing` for a document whose tenant-store row is `processed`; when reconciliation runs with the store reachable; then the registry row SHALL be updated to `processed` | pytest: `tests/test_tenant_document_registry.py` | - [x] |
| 35 | tenant-document-registry | The registry follows the tenant store, which stays authoritative | Unreachable store does not erase registry rows | Given a residency tenant whose store is unreachable; when reconciliation runs; then that tenant's registry rows SHALL be unchanged | pytest: `tests/test_tenant_document_registry.py` | - [x] |
| 36 | tenant-document-registry | Quotas and fleet counts use the registry | System admin sees counts during a tenant outage | Given a residency tenant with 12 registered documents and an unreachable store; when the System Admin views the tenant list; then the tenant's document count SHALL be 12 | pytest: `tests/test_tenant_document_registry.py` | - [x] |
| 37 | tenant-document-registry | Quotas and fleet counts use the registry | Quota uses the registry | Given a residency tenant at `max_documents` according to the registry; when a user uploads another document; then the upload SHALL be rejected by quota before any tenant-store write | pytest: `tests/test_tenant_document_registry.py` | - [x] |
| 38 | tenant-data-source-control-plane | Tenant-admin-managed finite Azure connections | Tenant administrator tests a supported Azure Blob draft | Given an authenticated tenant administrator submits a valid Azure Blob draft for their tenant; when the administrator requests a connection test; then the server SHALL bind the operation to the authenticated tenant and record a finite safe outcome with correlation metadata; and the response and evidence SHALL contain no credential, endpoint, provider-error payload, or tenant content | pytest (existing, re-run): `tests/test_tenant_data_source_control_plane.py` | - [x] |
| 39 | tenant-data-source-control-plane | Tenant-admin-managed finite Azure connections | Non-administrator is denied | Given an authenticated user without the tenant-administrator role; when the user requests any Azure connection lifecycle operation; then the system SHALL deny the operation; and no connection state SHALL change | pytest (existing, re-run): `tests/test_tenant_data_source_control_plane.py` | - [x] |
| 40 | tenant-data-source-control-plane | Tenant-admin-managed finite Azure connections | Cross-tenant connection access is denied | Given a connection owned by tenant A and an authenticated tenant administrator for tenant B; when the tenant B administrator requests that connection; then the system SHALL deny access; and it SHALL not disclose connection metadata or lifecycle evidence | pytest (existing, re-run): `tests/test_tenant_data_source_control_plane.py` | - [x] |
| 41 | tenant-data-source-control-plane | Tenant-admin-managed finite Azure connections | Platform-plane tenant cannot create a data-plane connection | Given an authenticated tenant administrator of a tenant with data-plane mode `platform`; when the administrator creates an `azure_postgresql_data_plane` draft; then the system SHALL reject it with finite safe code `DATA_PLANE_NOT_TENANT_OWNED`; and no connection row SHALL be created | pytest: `tests/test_data_plane_provider_control_plane.py` | - [x] |
| 42 | tenant-data-source-control-plane | Safe activation and concurrent capability limits | Failed prerequisite blocks activation | Given a tenant Azure connection draft whose test, secret resolution, or required evidence is absent or failed; when a tenant administrator requests activation; then the system SHALL keep the connection inactive; and the response SHALL expose only a safe blocking outcome class | pytest (existing, re-run): `tests/test_tenant_data_source_control_plane.py` | - [x] |
| 43 | tenant-data-source-control-plane | Safe activation and concurrent capability limits | Independent approved connections activate concurrently | Given a `tenant_owned` tenant has activation-ready Azure Blob, read-only Azure PostgreSQL, and Azure PostgreSQL data-plane drafts; when the tenant administrator activates all three connections; then the system SHALL permit one active connection of each approved provider class; and platform upload SHALL remain available once the data plane is `ready` | pytest: `tests/test_data_plane_provider_control_plane.py` | - [x] |
| 44 | tenant-data-source-control-plane | Safe activation and concurrent capability limits | Duplicate active provider is rejected | Given a tenant already has an active Azure Blob connection; when a tenant administrator activates another Azure Blob connection; then the system SHALL reject the activation with a finite safe outcome class; and the existing active connection SHALL remain unchanged | pytest (existing, re-run): `tests/test_tenant_data_source_control_plane.py` | - [x] |
| 45 | tenant-data-source-control-plane | Safe activation and concurrent capability limits | Data-plane test reports a missing vector extension | Given an `azure_postgresql_data_plane` draft for a server where `VECTOR` is not allow-listed; when the tenant administrator tests the connection; then the outcome SHALL be failed with reason class `vector_extension_unavailable`; and activation SHALL be blocked | pytest: `tests/test_data_plane_provider_control_plane.py` | - [x] |
| 46 | tenant-data-source-control-plane | Safe activation and concurrent capability limits | Shared secret reference between source and data plane is rejected | Given an active read-only `azure_postgresql` connection using secret reference R; when a tenant administrator saves an `azure_postgresql_data_plane` draft using R; then the system SHALL reject it with finite safe code `SECRET_REFERENCE_SHARED_ACROSS_PURPOSES` | pytest: `tests/test_data_plane_provider_control_plane.py` | - [x] |
| 47 | tenant-data-source-control-plane | Data-plane connection pause and retirement drive data-plane status | Pausing the data-plane connection stops content access | Given a `ready` residency tenant; when the tenant administrator pauses its data-plane connection; then the tenant's data-plane status SHALL be `paused`; and content routes SHALL return 409 `TENANT_DATA_PLANE_NOT_READY` with status class `paused` | pytest: `tests/test_data_plane_provider_control_plane.py` | - [x] |
| 48 | tenant-data-source-control-plane | Data-plane connection pause and retirement drive data-plane status | Retirement leaves the tenant store untouched | Given a paused data-plane connection with no replacement; when the tenant administrator retires it with confirmation; then the tenant's data-plane status SHALL be `store_retired`; and no DDL or DML SHALL be executed against the tenant store | pytest: `tests/test_data_plane_provider_control_plane.py` | - [x] |
| 49 | tenant-integration-profile | Only platform default adapters are executable in this change | A non-default selection may be recorded | Given a profile in `draft`; when its index adapter is set to a recorded-but-unsupported value; then the write SHALL succeed; and the profile SHALL remain in `draft` | pytest (existing, re-run): `tests/test_tenant_integration_profile.py` | - [x] |
| 50 | tenant-integration-profile | Only platform default adapters are executable in this change | A non-default selection cannot be activated | Given a `draft` profile selecting a non-default relational or index adapter other than an activation-ready approved Azure connection; when activation is attempted; then activation SHALL be rejected with a finite safe unsupported-selection reason; and the profile SHALL NOT enter `active` | pytest (existing, re-run): `tests/test_tenant_integration_profile.py` | - [x] |
| 51 | tenant-integration-profile | Only platform default adapters are executable in this change | An approved Azure connection can execute after activation | Given a tenant-bound Azure Blob or Azure PostgreSQL connection has passed the control-plane activation gate; when its approved capability is resolved for the authenticated owning tenant; then the connection SHALL be executable for its approved purpose; and no other recorded non-default selection SHALL become executable | pytest (existing, re-run): `tests/test_tenant_integration_profile.py` | - [x] |
| 52 | tenant-integration-profile | Only platform default adapters are executable in this change | Ingestion always uses executable adapters | Given a tenant whose profile records a non-default source selection that is not an active approved Azure Blob connection; when a document is ingested for that tenant; then the platform upload source and the platform content store SHALL serve it; and the recorded non-default selection SHALL have no effect | pytest (existing, re-run): `tests/test_tenant_integration_profile.py` | - [x] |
| 53 | tenant-integration-profile | Only platform default adapters are executable in this change | A read-only PostgreSQL source does not relocate the tenant | Given a `platform` tenant with an active read-only `azure_postgresql` connection and a profile recording `relational_adapter = tenant_postgresql`; when the tenant's relational selection is resolved; then it SHALL NOT be executable; and the tenant's schema SHALL continue to be served by the platform engine | pytest: `tests/test_integration_profile_data_plane.py` | - [x] |
| 54 | tenant-integration-profile | Only platform default adapters are executable in this change | Residency selections execute only when the data plane is ready | Given a `tenant_owned` tenant with an active `azure_postgresql_data_plane` connection whose data-plane status is `provisioning`; when `relational_adapter` is resolved; then it SHALL NOT be executable until the status is `ready` | pytest: `tests/test_integration_profile_data_plane.py` | - [x] |
| 55 | tenant-integration-profile | Tenant-owned data plane forbids platform-retained originals | Platform blob retention is rejected for a residency tenant | Given a `tenant_owned` tenant; when its profile `retention_mode` is set to `platform_blob`; then the write SHALL be rejected with `RETENTION_MODE_NOT_PERMITTED_FOR_DATA_PLANE` | pytest: `tests/test_integration_profile_data_plane.py` | - [x] |
| 56 | tenant-integration-profile | Tenant-owned data plane forbids platform-retained originals | Residency tenant defaults to ephemeral | Given a System Admin creates a tenant with data-plane mode `tenant_owned`; when its integration profile is read; then `retention_mode` SHALL be `ephemeral` | pytest: `tests/test_integration_profile_data_plane.py` | - [x] |
| 57 | tenant-provisioning | Tenant Creation | System Admin creates a tenant with valid data | Given an authenticated System Admin user; when they POST to `/api/v1/admin/tenants` with `{"name": "Acme Corp"}`; then the response SHALL have status 201; and the response body SHALL contain a `tenant` object with `id`, `name`, `slug`, `status: "active"`, `created_at`, and `data_plane: {"mode": "platform", "status": "ready"}`; and a PostgreSQL schema `tenant_{id}` SHALL exist; and the schema SHALL contain all tenant-scoped tables (tenant_user, document, etc.) | pytest (existing, extended for `data_plane`): `tests/test_tenant_provisioning.py` | - [x] |
| 58 | tenant-provisioning | Tenant Creation | System Admin creates a tenant with duplicate slug | Given a tenant with slug "acme-corp" exists; when the System Admin POSTs to `/api/v1/admin/tenants` with `{"name": "Acme Corp 2", "slug": "acme-corp"}`; then the response SHALL have status 409; and the error message SHALL indicate the slug is already taken | pytest (existing, extended for `data_plane`): `tests/test_tenant_provisioning.py` | - [x] |
| 59 | tenant-provisioning | Tenant Creation | System Admin creates a tenant-owned data plane tenant | Given an authenticated System Admin user; when they POST to `/api/v1/admin/tenants` with `{"name": "Contoso", "data_plane_mode": "tenant_owned", ...}`; then the response SHALL have status 201 with `data_plane: {"mode": "tenant_owned", "status": "awaiting_store"}`; and no `tenant_{id}` schema SHALL exist on the platform database; and the tenant administrator SHALL be able to sign in | pytest: `tests/test_tenant_provisioning_data_plane.py` | - [x] |
| 60 | tenant-provisioning | Tenant Creation | Invalid data plane mode is rejected | Given an authenticated System Admin user; when they POST to `/api/v1/admin/tenants` with `data_plane_mode: "hybrid"`; then the response SHALL have status 422; and no tenant row SHALL be created | pytest: `tests/test_tenant_provisioning_data_plane.py` | - [x] |
| 61 | tenant-schema-migrations | Tenant provisioning clones the template atomically | A failed table clone rolls back the whole tenant | Given tenant provisioning is in progress for a `platform` tenant; when creation of one tenant-scoped table fails; then no row for that tenant SHALL remain in `public.tenants`; and no schema for that tenant SHALL remain in the database; and no user row for that tenant SHALL remain in `public.tenant_users` | pytest (existing, re-run): `tests/test_tenant_provisioning_atomicity.py` | - [x] |
| 62 | tenant-schema-migrations | Tenant provisioning clones the template atomically | A provisioned tenant has the full template table set | Given `tenant_template` contains N tables; when a new `platform` tenant is provisioned successfully; then that tenant's schema SHALL contain all N tables; and listing documents for that tenant SHALL return an empty list rather than an error | pytest (existing, re-run): `tests/test_tenant_provisioning_atomicity.py` | - [x] |
| 63 | tenant-schema-migrations | Tenant provisioning clones the template atomically | A tenant-owned tenant is not cloned on the platform | Given `tenant_template` contains N tables; when a new `tenant_owned` tenant is created; then no `tenant_<id>` schema SHALL be created on the platform database | pytest: `tests/test_tenant_provisioning_data_plane.py` | - [x] |
| 64 | tenant-schema-migrations | Tenant-scoped migrations also reach residency stores | A column added by migration reaches both planes | Given a `platform` tenant and a `ready` `tenant_owned` tenant; and a migration adds column `foo` to `tenant_template.some_table` with its tenant-store revision; when the deploy migration step runs; then `tenant_<platform id>.some_table` on the platform database SHALL have column `foo`; and `tenant_<owned id>.some_table` in the tenant store SHALL have column `foo` | pytest (residency integration): `tests/test_tenant_store_migrate.py` | - [x] |
| 65 | tenant-data-source-portal | Safe connection lifecycle interface | Test, attestation, and activation render as one sequential control | Given an authenticated tenant administrator viewing a draft connection's lifecycle panel; when the administrator has not yet run a secure test; then the panel SHALL show a single "Test connection" control, with no attestation checkboxes and no Activate action, replacing the previously separate Activation-evidence panel. | vitest: `src/portal/src/app/(auth)/settings/data-sources/[connectionId]/page.test.tsx`, `src/portal/src/app/(auth)/settings/data-sources/page.test.tsx` | - [x] |
| 66 | tenant-data-source-portal | Safe connection lifecycle interface | A passed test surfaces the attestation checkboxes inline, Activate stays disabled until both are checked | Given a connection whose secure test has just passed; when the administrator views the lifecycle panel; then the same control SHALL show the passed result plus the `network_approved` and `governance_approved` checkboxes, both unchecked, with "Activate" disabled; and "Activate" SHALL enable only once the administrator has checked both checkboxes, and activating SHALL submit `activation_evidence` containing exactly `network_approved` and `governance_approved`. | vitest: `src/portal/src/app/(auth)/settings/data-sources/[connectionId]/page.test.tsx`, `src/portal/src/app/(auth)/settings/data-sources/page.test.tsx` | - [x] |
| 67 | tenant-data-source-portal | Safe connection lifecycle interface | A failed test hides the attestation checkboxes and returns the control to a retry state | Given a connection whose secure test fails, including a retest of a previously passed connection; when the administrator views the lifecycle panel; then the control SHALL return to a "Test connection" (retry) state, SHALL hide and reset any previously checked attestation checkboxes, and SHALL NOT expose an Activate action. | vitest: `src/portal/src/app/(auth)/settings/data-sources/[connectionId]/page.test.tsx`, `src/portal/src/app/(auth)/settings/data-sources/page.test.tsx` | - [x] |
| 68 | tenant-data-source-portal | Safe connection lifecycle interface | Activation is blocked safely | Given a connection whose test, secret resolution, or required evidence is absent or failed; when the backend reports unmet activation prerequisites; then the lifecycle action SHALL be unavailable with a safe blocking notice and no secret, connection string, provider error, or remote content displayed. | vitest: `src/portal/src/app/(auth)/settings/data-sources/[connectionId]/page.test.tsx`, `src/portal/src/app/(auth)/settings/data-sources/page.test.tsx` | - [x] |
| 69 | tenant-data-source-portal | Safe connection lifecycle interface | Destructive lifecycle actions require confirmation | Given an authenticated tenant administrator retiring or replacing a connection; when the administrator initiates the action; then the portal SHALL require explicit confirmation before sending the mutation, and an active connection SHALL require pausing first. | vitest: `src/portal/src/app/(auth)/settings/data-sources/[connectionId]/page.test.tsx`, `src/portal/src/app/(auth)/settings/data-sources/page.test.tsx` | - [x] |
| 70 | tenant-data-source-portal | Safe connection lifecycle interface | Idempotent mutation replay renders the safe result | Given a mutation already accepted under an `Idempotency-Key`; when the same key and body are submitted again; then the portal SHALL render the returned safe result with a replay notice and SHALL NOT duplicate the lifecycle effect. | vitest: `src/portal/src/app/(auth)/settings/data-sources/[connectionId]/page.test.tsx`, `src/portal/src/app/(auth)/settings/data-sources/page.test.tsx` | - [x] |
| 71 | tenant-data-source-portal | Safe connection lifecycle interface | Detail panels render in the fixed order | Given an authenticated tenant administrator viewing any connection's detail route; when the detail route renders; then the portal SHALL present Connection details first, Sync activity (or Data plane, for a data-plane connection) second, and Lifecycle third, for Azure Blob, Azure PostgreSQL, and Azure PostgreSQL data-plane connections. | vitest: `src/portal/src/app/(auth)/settings/data-sources/[connectionId]/page.test.tsx`, `src/portal/src/app/(auth)/settings/data-sources/page.test.tsx` | - [x] |
| 72 | tenant-data-source-portal | Safe connection lifecycle interface | Data-plane provider is hidden for platform tenants | Given an authenticated tenant administrator of a `platform` tenant; when the administrator opens "New connection"; then provider selection SHALL NOT offer the Azure PostgreSQL data-plane provider | vitest: `src/portal/src/app/(auth)/settings/data-sources/[connectionId]/page.test.tsx`, `src/portal/src/app/(auth)/settings/data-sources/page.test.tsx` | - [x] |
| 73 | tenant-data-source-portal | Safe connection lifecycle interface | Data-plane failed test shows residency checks | Given a data-plane connection whose test failed on `vector_extension_unavailable`; when the administrator views the lifecycle panel; then the panel SHALL show the vector-extension check as failed with a safe prerequisite hint and no server or provider text | vitest: `src/portal/src/app/(auth)/settings/data-sources/[connectionId]/page.test.tsx`, `src/portal/src/app/(auth)/settings/data-sources/page.test.tsx` | - [x] |
| 74 | tenant-data-source-portal | Tenant users see a safe data-plane readiness state | Awaiting-store tenant administrator is guided to setup | Given a tenant administrator of a `tenant_owned` tenant in `awaiting_store`; when the administrator opens the documents page; then the page SHALL show an awaiting-store state with a link to data-source settings; and no upload control SHALL be enabled | vitest: `src/portal/src/components/data-plane/data-plane-gate.test.tsx` | - [x] |
| 75 | tenant-data-source-portal | Tenant users see a safe data-plane readiness state | Outage is shown without content | Given a tenant user of a `ready` residency tenant whose store becomes unreachable; when the chat page receives `TENANT_DATA_PLANE_UNAVAILABLE`; then the page SHALL show an unavailable state; and it SHALL NOT display previously loaded conversation content after the error | vitest: `src/portal/src/components/data-plane/data-plane-gate.test.tsx` | - [x] |
| 76 | admin-console | System Admin chooses and observes the tenant data plane | System Admin creates a tenant-owned tenant | Given an authenticated System Admin on `/admin/tenants`; when they click "Create Tenant", enter a name, select "Tenant-owned Azure PostgreSQL", and submit; then the request SHALL include `data_plane_mode: "tenant_owned"`; and the tenant detail view SHALL show data plane "Tenant-owned" with status "Awaiting store" | vitest: `src/portal/src/app/(auth)/admin/tenants/new/page.test.tsx`, `src/portal/src/app/(auth)/admin/tenants/[id]/page.test.tsx` | - [x] |
| 77 | admin-console | System Admin chooses and observes the tenant data plane | Document counts remain visible during a tenant outage | Given a tenant-owned tenant with an unreachable store and 12 registered documents; when the System Admin views the tenant detail page; then document quota usage SHALL show 12; and store health SHALL show "Unreachable" | vitest: `src/portal/src/app/(auth)/admin/tenants/new/page.test.tsx`, `src/portal/src/app/(auth)/admin/tenants/[id]/page.test.tsx` | - [x] |

> **Rule:** Every `#### Scenario:` block in every `specs/**/*.md` file for this change
> MUST appear as a row in this table. A missing scenario is a P1 gap that blocks archive.

---

## 2. Hallucination Risk Register

| # | Risk Area | Potential AI Error | Human Check Required |
|---|-----------|-------------------|----------------------|
| 1 | Engine routing (Design D4) | An agent replaces `get_engine()` call sites but misses worker-local `create_engine(settings.database_url_sync)` or adds a "graceful" fallback to the platform engine when the tenant store fails — silently writing residency content to the platform database | Grep `src/` for `create_engine(` / `create_async_engine(` / `database_url` outside the allowlisted files; read `EngineResolver` for any `except` path returning the platform engine; run the residency end-to-end flow and confirm zero rows for that tenant on the platform DB and no `tenant_<id>` schema there |
| 2 | Cross-plane SQL (Design D10) | SQL executed on a tenant session still joins `public.*` (e.g. uploader names from `public.tenant_users`, `public.entity_definitions`), which works on the platform plane and fails or hits a customer table on a tenant store | Run the residency integration suite against `postgres-tenant-store` with no platform `public` tables; grep raw SQL strings reachable from tenant sessions for `public.` |
| 3 | Tenant-store schema parity (Design D5) | Hand-written baseline omits generated `chunk_tsv`, the HNSW index, materialized views, CHECK constraints, or defaults; or a new tenant-scoped Alembic migration ships without a tenant-store revision | Parity test output comparing columns, types, defaults, constraints, indexes, and matviews; confirm the parity test fails when a revision is deliberately removed |
| 4 | Status machine and CAS (Design D2, D6) | Agent invents extra statuses, allows `mode` update, or transitions without compare-and-set, enabling double provisioning or `store_retired → ready` | Compare status literals in code/migration against the spec list; review every `UPDATE tenant_data_planes` for a `WHERE status =` guard; concurrent-activation test |
| 5 | Error hygiene (Design D8) | Driver exception text (host, database, user) is logged, traced, or returned; or unavailable errors map to 500 instead of 503 `TENANT_DATA_PLANE_UNAVAILABLE` | Inject a connection failure with a known host string; scan response, logs, traces, and audit rows for it; run the release-gate telemetry scan |
| 6 | Registry content boundary (Design D7) | Agent adds `filename`, `storage_uri`, or `error_message` to the registry "for convenience", or writes the registry before the tenant commit | Inspect migration columns against spec list; review write order in ingestion and OCR paths; quota test where the tenant commit fails |
| 7 | Retry and no-buffering (Design D8) | Celery retries serialize document text/spans into task args or a platform table while waiting; OCR text is cached on the platform during outage | Inspect task signatures and retry calls for identifier-only payloads; outage test checks platform DB, Redis/RabbitMQ messages for content |

---

## 3. Pattern & ADR Compliance

| ADR | Decision Summary | Constraint on This Change | Verification Step |
|-----|-----------------|--------------------------|-------------------|
| 001-tenant-data-isolation (partially superseded by 017 for topology) | Schema-per-tenant; `public` holds tenant-bound non-content control plane | Tenant schema naming and `search_path` scoping unchanged on both planes; new public tables contain no tenant content | Review `tenant_session` helpers set `search_path` from server-derived tenant id; inspect `tenant_data_planes` and `tenant_document_registry` columns |
| 007-chatbot-architecture | RAG over SQL entities + pgvector + NER; SQL in read-only transactions | Chat SQL and retrieval unchanged and still use `SET LOCAL ROLE` read-only execution on tenant stores | Residency chat integration test exercising SQL tool and dense/sparse retrieval against the tenant store |
| 011-tenant-scoped-azure-connection-control-plane (partially superseded by 017 for catalog/credential scope) | Tenant-admin lifecycle, secret refs, TLS verify-full, evidence, idempotency | New provider reuses lifecycle, evidence, idempotency, secret-reference grammar; no values stored | API tests for create/test/activate/pause/replace/retire on the new provider; inspect stored row for values |
| 012-durable-azure-blob-source-synchronization | Celery/RabbitMQ sync, ledger, temporary bytes deleted | Sync ledger lives in the tenant store; residency tenants cannot use `platform_blob` | Blob sync test for a residency tenant confirming ledger rows in tenant store and no durable original |
| 013-contract-governed-external-postgresql-chat | Read-only contract-governed external query | Read-only provider unchanged; credentials never shared with data plane | Test rejecting shared secret reference; external chat tests still pass |
| 014-local-compose-deployment-topology | Single `dev` Compose, additive migrations, rolling | Migration `043` additive; tenant-store migrate step after Alembic; local tenant store is a Compose service | Review migration for destructive DDL; `docker compose up` shows migrate step and `postgres-tenant-store` healthy |
| 015-external-chat-reply-persistence | External rows never persisted to platform tables | Unchanged; residency replies persist in tenant store | Existing ADR-015 tests pass; residency chat writes messages to tenant store only |
| 016-contract-grounded-external-sql-generation | External SQL grounded in contract | Unaffected | Existing external SQL generation tests pass |
| 017-tenant-owned-postgresql-data-plane | Whole-schema relocation, new provider, registry, fail closed, no platform fallback | Every decision section reflected in code | Structural review against ADR-017 §1–§7 |

---

## 4. Evidence Requirements

### Functional Evidence

- [x] #1 tenant-data-plane-routing — "Existing tenants are backfilled to the platform plane": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #2 tenant-data-plane-routing — "Mode cannot be changed": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #3 tenant-data-plane-routing — "Data-plane record carries no sensitive values": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #4 tenant-data-plane-routing — "Platform tenant resolves to the platform engine": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #5 tenant-data-plane-routing — "Residency tenant resolves to its own store": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #6 tenant-data-plane-routing — "Residency tenant never falls back to the platform engine": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #7 tenant-data-plane-routing — "Workers use the resolver": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #8 tenant-data-plane-routing — "Engine cache is invalidated on connection change": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #9 tenant-data-plane-routing — "Awaiting-store tenant cannot upload": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #10 tenant-data-plane-routing — "Awaiting-store tenant administrator can configure the store": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #11 tenant-data-plane-routing — "Reconciler reaches a residency tenant": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #12 tenant-data-plane-routing — "One unreachable store does not abort the fleet run": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #13 tenant-residency-store-provisioning — "Successful provisioning makes the tenant ready": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #14 tenant-residency-store-provisioning — "Provisioning failure is safe and retryable": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #15 tenant-residency-store-provisioning — "Provisioning refuses a non-empty foreign schema": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #16 tenant-residency-store-provisioning — "No platform schema is created for a residency tenant": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #17 tenant-residency-store-provisioning — "Missing tenant-store revision fails the build": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #18 tenant-residency-store-provisioning — "Re-applying a revision is a no-op": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #19 tenant-residency-store-provisioning — "Deploy upgrades a residency store": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #20 tenant-residency-store-provisioning — "Unreachable store during deploy is isolated": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #21 tenant-residency-store-provisioning — "Credential rotation keeps the tenant ready": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #22 tenant-residency-store-provisioning — "Pointing at a different server is rejected": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #23 tenant-data-plane-failure-isolation — "Chat fails closed during a store outage": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #24 tenant-data-plane-failure-isolation — "Other tenants are unaffected": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #25 tenant-data-plane-failure-isolation — "Driver error text is not exposed": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #26 tenant-data-plane-failure-isolation — "Upload during outage writes nothing": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #27 tenant-data-plane-failure-isolation — "Extraction parks after bounded retries": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #28 tenant-data-plane-failure-isolation — "No content is buffered on the platform": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #29 tenant-data-plane-failure-isolation — "Service stays ready during a tenant outage": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #30 tenant-data-plane-failure-isolation — "Recovery is detected": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #31 tenant-document-registry — "Registry schema holds no content columns": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #32 tenant-document-registry — "Existing documents are backfilled": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #33 tenant-document-registry — "Status transitions reach the registry": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #34 tenant-document-registry — "Drift is reconciled": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #35 tenant-document-registry — "Unreachable store does not erase registry rows": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #36 tenant-document-registry — "System admin sees counts during a tenant outage": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #37 tenant-document-registry — "Quota uses the registry": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #38 tenant-data-source-control-plane — "Tenant administrator tests a supported Azure Blob draft": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #39 tenant-data-source-control-plane — "Non-administrator is denied": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #40 tenant-data-source-control-plane — "Cross-tenant connection access is denied": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #41 tenant-data-source-control-plane — "Platform-plane tenant cannot create a data-plane connection": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #42 tenant-data-source-control-plane — "Failed prerequisite blocks activation": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #43 tenant-data-source-control-plane — "Independent approved connections activate concurrently": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #44 tenant-data-source-control-plane — "Duplicate active provider is rejected": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #45 tenant-data-source-control-plane — "Data-plane test reports a missing vector extension": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #46 tenant-data-source-control-plane — "Shared secret reference between source and data plane is rejected": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #47 tenant-data-source-control-plane — "Pausing the data-plane connection stops content access": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #48 tenant-data-source-control-plane — "Retirement leaves the tenant store untouched": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #49 tenant-integration-profile — "A non-default selection may be recorded": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #50 tenant-integration-profile — "A non-default selection cannot be activated": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #51 tenant-integration-profile — "An approved Azure connection can execute after activation": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #52 tenant-integration-profile — "Ingestion always uses executable adapters": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #53 tenant-integration-profile — "A read-only PostgreSQL source does not relocate the tenant": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #54 tenant-integration-profile — "Residency selections execute only when the data plane is ready": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #55 tenant-integration-profile — "Platform blob retention is rejected for a residency tenant": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #56 tenant-integration-profile — "Residency tenant defaults to ephemeral": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #57 tenant-provisioning — "System Admin creates a tenant with valid data": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #58 tenant-provisioning — "System Admin creates a tenant with duplicate slug": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #59 tenant-provisioning — "System Admin creates a tenant-owned data plane tenant": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #60 tenant-provisioning — "Invalid data plane mode is rejected": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #61 tenant-schema-migrations — "A failed table clone rolls back the whole tenant": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #62 tenant-schema-migrations — "A provisioned tenant has the full template table set": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #63 tenant-schema-migrations — "A tenant-owned tenant is not cloned on the platform": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #64 tenant-schema-migrations — "A column added by migration reaches both planes": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #65 tenant-data-source-portal — "Test, attestation, and activation render as one sequential control": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #66 tenant-data-source-portal — "A passed test surfaces the attestation checkboxes inline, Activate stays disabled until both are checked": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #67 tenant-data-source-portal — "A failed test hides the attestation checkboxes and returns the control to a retry state": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #68 tenant-data-source-portal — "Activation is blocked safely": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #69 tenant-data-source-portal — "Destructive lifecycle actions require confirmation": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #70 tenant-data-source-portal — "Idempotent mutation replay renders the safe result": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #71 tenant-data-source-portal — "Detail panels render in the fixed order": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #72 tenant-data-source-portal — "Data-plane provider is hidden for platform tenants": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #73 tenant-data-source-portal — "Data-plane failed test shows residency checks": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #74 tenant-data-source-portal — "Awaiting-store tenant administrator is guided to setup": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #75 tenant-data-source-portal — "Outage is shown without content": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #76 admin-console — "System Admin creates a tenant-owned tenant": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed
- [x] #77 admin-console — "Document counts remain visible during a tenant outage": passing automated test output (or portal screenshot for UI scenarios) showing the THEN/AND outcomes observed

### Structural Evidence

- [x] Code review completed — implementation matches design.md decisions D1–D12 (no undocumented deviations)
- [x] All ADR compliance steps in Section 3 confirmed ✓
- [x] No undocumented architectural patterns introduced
- [x] No AI-invented requirements present in generated code (cross-checked against spec files)
- [x] Existing pytest suite failing-id set diffed against the pre-change baseline shows no new failures for platform tenants
- [x] Architecture source check output: no tenant-schema engine construction outside the resolver

### Edge Case Evidence

- [x] Risk 1 mitigation confirmed — grep output and residency end-to-end run showing zero platform-DB rows and no platform schema for the residency tenant
- [x] Risk 2 mitigation confirmed — residency integration suite green against a store without platform `public` tables; grep result for `public.` in tenant-session SQL
- [x] Risk 3 mitigation confirmed — parity test passes, and fails when a revision is removed
- [x] Risk 4 mitigation confirmed — status literals match spec; concurrent activation test shows single provisioning run
- [x] Risk 5 mitigation confirmed — injected host string absent from response, logs, traces, audit; telemetry scan passes
- [x] Risk 6 mitigation confirmed — registry columns match spec; failed tenant commit leaves no registry row
- [x] Risk 7 mitigation confirmed — task payloads identifier-only; no content found in platform DB or broker during outage test

---

## 5. Evidence Log

| # | Evidence Type | Description / Link | Scenario(s) Covered | Collected By | Date |
|---|--------------|-------------------|---------------------|--------------|------|
| 1 | pytest output | Consolidated run of every ADR-017 backend test file (25 files: `test_tenant_data_plane_record.py`, `test_tenant_engine_resolver.py`, `test_tenant_engine_construction_boundary.py`, `test_data_plane_route_gate.py`, `test_data_plane_fleet_operations.py`, `test_tenant_store_provisioning.py`, `test_tenant_store_parity.py`, `test_tenant_store_revisions.py`, `test_tenant_store_migrate.py`, `test_data_plane_connection_replacement.py`, `test_data_plane_failure_isolation.py`, `test_data_plane_task_retry.py`, `test_data_plane_health.py`, `test_tenant_document_registry.py`, `test_tenant_document_registry_reconcile.py`, `test_tenant_data_source_control_plane.py`, `test_data_plane_provider_control_plane.py`, `test_tenant_integration_profile.py`, `test_integration_profile_data_plane.py`, `test_tenant_provisioning.py`, `test_tenant_provisioning_data_plane.py`, `test_tenant_provisioning_atomicity.py`, `test_upload_precheck.py`, `test_data_plane_provisioning_task.py`, `test_data_plane_status_endpoint.py`) — `131 passed`, 2 failures both individually re-run and confirmed not caused by this change: `test_tenant_provisioning_atomicity.py::test_provisioned_tenant_has_full_template_table_set` (present in `baseline_failing_ids.txt` — pre-existing `tenant_template.documents` missing `uploaded_by`, unrelated schema drift) and `test_tenant_integration_profile.py::test_row_63_...` (passes standalone; order-dependent flakiness under this suite's documented shared-fixture-state pattern, not present in `baseline_failing_ids.txt` either but reproducibly absent when isolated). Covers scenarios #1–#64. | #1–#64 | Claude (agent) | 2026-09-15 |
| 2 | vitest output | `npx vitest run` on all 5 portal test files touching ADR-017 UI (`settings/data-sources/page.test.tsx`, `settings/data-sources/[connectionId]/page.test.tsx`, `components/data-plane/data-plane-gate.test.tsx`, `admin/tenants/new/page.test.tsx`, `admin/tenants/[id]/page.test.tsx`) — `33 passed`, 0 failed. Full-suite `npx vitest run` (all of `src/portal`, 108 files) run before and after every group-13 code change: 49 failing tests in 12 files both times, identical to a stashed-changes (pre-group-13) baseline run — zero net-new regressions. Covers scenarios #65–#77. | #65–#77 | Claude (agent) | 2026-09-15 |
| 3 | pytest baseline diff | Four full-repo `pytest -p no:randomly --continue-on-collection-errors` runs across the session (after groups 9/10, 11, 12), each diffed against `baseline_failing_ids.txt` via `comm -13`. Every apparent new failure across all four runs was individually re-run in isolation and traced to one of two causes: Postgres connection-pool exhaustion from running back-to-back full-suite passes against the same `ner_test`/`postgres-tenant-store` containers (the dominant cause — up to ~300 apparent failures in one run, all cleared by `pg_terminate_backend` on idle connections and a clean re-run), or the suite's own documented order-dependent test flakiness. Zero confirmed regressions for platform tenants across the whole change. | #1–#64 (regression coverage) | Claude (agent) | 2026-09-15 |

---

## 6. Audit Record

> ⚠️ **GATE: This section must be completed and signed by a human reviewer before
> `/opsx:archive` is run.** An unsigned or incomplete Audit Record is a hard block on archive.

**Change slug:** tenant-postgresql-data-plane
**Proposal:** `openspec/changes/tenant-postgresql-data-plane/proposal.md`
**Spec files reviewed:**
  - specs/tenant-data-plane-routing/spec.md
  - specs/tenant-residency-store-provisioning/spec.md
  - specs/tenant-data-plane-failure-isolation/spec.md
  - specs/tenant-document-registry/spec.md
  - specs/tenant-data-source-control-plane/spec.md
  - specs/tenant-integration-profile/spec.md
  - specs/tenant-provisioning/spec.md
  - specs/tenant-schema-migrations/spec.md
  - specs/tenant-data-source-portal/spec.md
  - specs/admin-console/spec.md

### Reviewer Sign-Off

| Check | Status |
|-------|--------|
| Design reviewed against proposal | - [ ] |
| All ADRs in Section 3 verified compliant | - [ ] |
| Spec Alignment table complete (no missing scenarios) | - [ ] |
| Evidence Log populated with real evidence | - [ ] |
| All functional evidence items in Section 4 checked | - [ ] |
| All structural evidence items in Section 4 checked | - [ ] |
| All edge case evidence items in Section 4 checked | - [ ] |

### AI Output Review

| Check | Status |
|-------|--------|
| All generated artifacts reviewed for spec alignment | - [ ] |
| No hallucinated requirements introduced | - [ ] |
| No undocumented patterns used | - [ ] |
| No AI-invented fields, endpoints, or behaviours present | - [ ] |
| Every THEN clause in specs has a corresponding evidence entry | - [ ] |
| Hallucination risk register reviewed and all mitigations confirmed | - [ ] |

**Archive approved by:** ___________________________

**Date:** ___________

**Notes:**
