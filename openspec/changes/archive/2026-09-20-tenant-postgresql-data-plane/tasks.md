## 1. Prerequisites

- [x] 1.1 Confirm `external-postgresql-chat-sql-generation` and `harden-blob-sync-scheduling-and-retention` are archived or rebased onto this branch; record the decision in the change notes.
- [x] 1.2 Record the pytest failing-id baseline on the starting commit (`--continue-on-collection-errors`) for later diffing.
- [x] 1.3 Add `postgres-tenant-store` (`pgvector/pgvector:pg16`) to `docker-compose.yml` with `env://`-resolved credentials, a dedicated database, and a login role owning it; add `NER_TENANT_OWNED_DATA_PLANE_ENABLED`, resolver TTL, pool, timeout, retry budget, and probe interval settings to `src/shared/config.py` (Design D4, D8, D11, D12).

## 2. Control-plane schema (migration 043)

- [x] 2.1 Create `public.tenant_data_planes` with the exact mode/status literals from the routing spec, a trigger rejecting `mode` updates, and backfill `platform`/`ready` for every tenant.
- [x] 2.2 Create `public.tenant_document_registry` with only the spec-listed columns and backfill from every platform tenant schema.
- [x] 2.3 Add `azure_postgresql_data_plane` to the `tenant_data_source_connections.provider` CHECK constraint.
- [x] 2.4 Add `platform_store_meta` to `tenant_template` and every existing platform tenant schema (generated `store_id`, current revision).
- [x] 2.5 Write `tests/test_tenant_data_plane_record.py` covering scenarios #1–#3 (backfill, `DATA_PLANE_MODE_IMMUTABLE`, no sensitive values in API output).
- [x] 2.6 Add registry schema and backfill cases (#31, #32) to `tests/test_tenant_document_registry.py`.

## 3. Tenant-store schema package

- [x] 3.1 Create `src/shared/tenant_store/baseline.py` with idempotent `statements(schema)` reproducing `tenant_template` at head 043, including `chunk_tsv`, HNSW index, materialized views, and `platform_store_meta` (Design D5).
- [x] 3.2 Create `src/shared/tenant_store/revisions/` with the revision interface and `MIN_SUPPORTED_TENANT_STORE_REVISION`; create `apply.py` applying baseline + pending revisions and recording the revision.
- [x] 3.3 Write `tests/test_tenant_store_parity.py` comparing platform `tenant_template` after `alembic upgrade head` with an empty database after `apply` (columns, types, defaults, constraints, indexes, matviews) — scenario #17, including a negative case with a revision removed.
- [x] 3.4 Write `tests/test_tenant_store_revisions.py` for idempotent re-application — scenario #18.
- [x] 3.5 Document in `alembic/README` (or the migrations spec note) that tenant-scoped Alembic migrations call the matching revision's `statements()`.

## 4. Engine resolver and tenant sessions

- [x] 4.1 Implement data-plane record lookup with a short-TTL in-process cache and typed `DataPlaneNotReady` / `DataPlaneUnavailable` errors with finite reason classes (Design D2, D4, D8).
- [x] 4.2 Extend `EngineResolver` with `resolve(tenant_id)` and `resolve_sync(tenant_id)`: platform engine for `platform`; bounded LRU of TLS `verify-full` per-tenant engines keyed by `(tenant_id, connection_id, updated_at)` for `ready` `tenant_owned`; never return the platform engine otherwise; resolve passwords via the existing secret-reference resolution.
- [x] 4.3 Add `tenant_session(tenant_id)` / `tenant_sync_session(tenant_id)` helpers setting `search_path`; classify driver exceptions to `DataPlaneUnavailable` and strip messages; update `src/shared/tenant_context.py` and each service's tenant-context middleware to use them. (`tenant_context.py` done; per-service `dependencies.py` call sites migrate in group 5.)
- [x] 4.4 Invalidate cached engines on pause/replace/retire in the acting process. (Hooks built — `data_plane.invalidate()` + `EngineResolver.invalidate_tenant()`; call sites wire in group 7's pause/replace/retire.)
- [x] 4.5 Write `tests/test_tenant_engine_resolver.py` covering scenarios #4, #5, #6, #8.

## 5. Replace direct engine construction and cross-plane SQL

- [x] 5.1 Replace `create_engine(settings.database_url_sync)` for tenant data in `extraction_service/worker.py`, `extraction_service/services/entity_store.py`, `training_service/worker.py`, `analytics_service/worker.py` with `resolve_sync`. (Also fixed `training_service/infra/mlflow_registry.py`, same bug, not in the original list.)
- [x] 5.2 Route document-service ingestion, OCR, blob sync, reopen, and deletion paths through tenant sessions.
- [x] 5.3 Route chat (`chat.py`, `public.py` widget, retrieval, SQL generator/executor, context assembler, conversation state) through tenant sessions; keep control-plane reads on the platform session. (Also fixed the same `get_engine()`-for-tenant-data bug in annotation_service's 5 route files, extraction/analytics `dependencies.py`, and training_service's `api/v1/models.py` — not in the original file list but the identical defect.)
- [x] 5.4 Remove SQL joins to `public.*` executed on tenant sessions (e.g. `documents.py` uploader join to `public.tenant_users`) by splitting into a control-plane lookup (Design D10). (Also found and fixed two more instances of the same cross-schema-join pattern in `gateway/api/v1/dashboard.py` and `training_service/api/v1/training_jobs.py`, caught by the 5.5 source-check test.)
- [x] 5.5 Write `tests/test_tenant_engine_construction_boundary.py` (source check for engine construction outside allowlisted files and `public.` in tenant-session SQL) — scenario #7.

## 6. Content-route gate and fleet operations

- [x] 6.1 Add the shared readiness dependency returning 409 `TENANT_DATA_PLANE_NOT_READY` on content routers in all services; exempt tenant-admin, data-source, and data-plane status routes (Design D9).
- [x] 6.2 Install shared FastAPI exception handlers mapping `DataPlaneNotReady` → 409 and `DataPlaneUnavailable` → 503 in every service app.
- [x] 6.3 Change `entity_views` reconciler, `sql_execution_role` provisioning, `verify_schema`, analytics refresh, and `dashboard.py` system-admin queries to enumerate tenants from `public.tenants` + `tenant_data_planes` and resolve engines, isolating per-tenant failures. (`verify_schema.py` audited — its `pg_namespace` scan is already correctly platform-only, no change needed there. `entity_views.py`'s own `list_tenant_schemas` is dead code, unused by any reconciler.)
- [x] 6.4 Write `tests/test_data_plane_route_gate.py` — scenarios #9, #10.
- [x] 6.5 Write `tests/test_data_plane_fleet_operations.py` (residency integration against `postgres-tenant-store`) — scenarios #11, #12. (Caught and fixed a real cross-plane-session bug in `sql_execution_role.provision_role`: `resolve_generated_tables` was being called on the tenant-store session instead of the platform session.)

## 7. Data-plane provider and control plane

- [x] 7.1 Add `azure_postgresql_data_plane` to `src/shared/data_sources/providers.py` (config keys, `password_ref`, `verify-full`); reject for `platform` tenants with `DATA_PLANE_NOT_TENANT_OWNED`; reject secret references shared with the tenant's `azure_postgresql` connection with `SECRET_REFERENCE_SHARED_ACROSS_PURPOSES`; gate drafts on the feature flag.
- [x] 7.2 Register a secure tester for the provider checking TLS, server version ≥ 16, `vector` availability (`azure.extensions` or `pg_available_extensions`), schema `CREATE` privilege, query-role creatability or pre-created grant, and target schema empty/absent/matching `store_id`, each as a finite reason class (Design D3, D11). (Verified against real postgres-tenant-store.)
- [x] 7.3 Update `src/shared/data_sources/resolver.py` so `tenant_postgresql` / `tenant_pgvector` map to the new provider and require data-plane `ready`.
- [x] 7.4 Wire pause → `paused`, re-activation → `ready` (after identity and revision checks), retire → `store_retired` with no tenant-store DDL/DML and customer-responsibility text; wire activation → provisioning enqueue. (Replacement's full store-identity re-verification is group 10.3's scope, noted in code.)
- [x] 7.5 Re-run `tests/test_tenant_data_source_control_plane.py` unchanged for scenarios #38, #39, #40, #42, #44. (38/38 pass.)
- [x] 7.6 Write `tests/test_data_plane_provider_control_plane.py` — scenarios #41, #43, #45, #46, #47, #48.

## 8. Integration profile and retention

- [x] 8.1 Enforce `ephemeral`/`source_only` for `tenant_owned` tenants (`RETENTION_MODE_NOT_PERMITTED_FOR_DATA_PLANE`), default `ephemeral` at creation, and block data-plane activation with `retention_mode_not_permitted`.
- [x] 8.2 Re-run `tests/test_tenant_integration_profile.py` for scenarios #49–#52. (All pass.)
- [x] 8.3 Write `tests/test_integration_profile_data_plane.py` — scenarios #53–#56.

## 9. Tenant creation

- [x] 9.1 Extend `POST /api/v1/admin/tenants` and `TenantService.create_tenant` with `data_plane_mode` (422 on invalid value, feature-flag gated); for `tenant_owned` skip platform schema cloning and create tenant, admin user, data-plane record, and profile atomically; include `data_plane` in tenant responses.
- [x] 9.2 Extend `tests/test_tenant_provisioning.py` for `data_plane` in the response — scenarios #57, #58.
- [x] 9.3 Write `tests/test_tenant_provisioning_data_plane.py` — scenarios #59, #60, #63.
- [x] 9.4 Re-run `tests/test_tenant_provisioning_atomicity.py` — scenarios #61, #62. (1 passes; the other, `test_provisioned_tenant_has_full_template_table_set`, was already failing on the pre-ADR-017 baseline — `tenant_template.documents` missing `uploaded_by`, unrelated to this change — confirmed present in `baseline_failing_ids.txt`.)

## 10. Provisioning task and per-store migration

- [x] 10.1 Implement `provision_tenant_data_plane(tenant_id)` on a `data_plane` queue in the document-service Celery app with CAS transitions, extension/schema/baseline/revisions, `platform_store_meta`, query role, entity-table reconciliation, and finite failure reasons (Design D6). New `src/document_service/data_plane/tasks.py`, registered on the existing blob-sync Celery app; verified end-to-end against real `postgres-tenant-store` (`tests/test_data_plane_provisioning_task.py`, both directions: success and `store_unreachable`). Compose worker now consumes `-Q blob_sync,data_plane`.
- [x] 10.2 Add `GET /api/v1/data-plane` (status, reason class, revision) and tenant-admin `POST /api/v1/data-plane/provision` retry with idempotency. Added to `src/gateway/api/v1/data_sources.py` as `data_plane_router`, mounted in `src/gateway/main.py`; retry goes through the same `Idempotency-Key` `_mutate` wrapper every other mutation on this router uses, CAS'ing `provisioning_failed -> provisioning` before re-enqueuing. `tests/test_data_plane_status_endpoint.py`. Health surfacing (last probe outcome) deferred to group 11's health-check work, which is what populates `health_outcome`/`health_checked_at`.

Full-suite regression check after 10.1/10.2 (2 runs, `local.max_connections=100` shared by every service in this Compose stack): a full run's own connection churn leaves the pool near its ceiling for the next run started soon after, which manifests as a wide spray of unrelated `TooManyConnectionsError`-rooted failures and collection errors — up to ~300 apparent new "failures" in one run. Every one of those, rerun in isolation after `pg_terminate_backend` on idle connections, either passed or reproduced a failure already present in `baseline_failing_ids.txt`. Zero genuine regressions from this change's work across both runs.
- [x] 10.3 Implement same-store replacement using `store_id` verification (`store_identity_mismatch`). Threaded `expected_store_id` through `run_secure_test` -> `DataPlaneSecureTester.run` -> `run_data_plane_test_sync` (`src/shared/data_sources/testing.py`, `data_plane_tester.py`); `service.py::test_connection` looks up the tenant's recorded `store_id` and passes it for the data-plane provider only, so a replacement/rotation test compares the store's `platform_store_meta.store_id` against it (mismatch -> `store_identity_mismatch`) while a bare first-activation draft (`expected_store_id=None`) keeps the existing "any recognized marker is fine" behavior. Also found and fixed a real gap while wiring this: `activate_connection` had no data-plane branch at all for a *replacement* activation (`row.replaces_connection_id is not None`) — `tenant_data_planes.connection_id` was never repointed at the successor and neither cache was invalidated, so the resolver would have kept using the retired predecessor's credentials indefinitely. Added the rebind + `invalidate_data_plane_record` + `_invalidate_tenant_engines` branch.
- [x] 10.4 Implement `src/shared/tenant_store/migrate.py` and add it after `alembic upgrade head` in the Compose migration service; set `migration_required` on failure or below minimum revision without blocking startup. Already built in group 3 and wired into `db-init`'s command; found and fixed a real bug while verifying against `postgres-tenant-store` for 10.6 — `apply_module.apply()` was called inside an explicit `with store_conn.begin():` after `read_store_identity` had already autobegun a transaction on the same connection (SQLAlchemy 2.0), raising `InvalidRequestError` on every real invocation. Same class of bug as the one fixed in `data_plane/tasks.py` for 10.1.
- [x] 10.5 Write `tests/test_tenant_store_provisioning.py` — scenarios #13–#16. Against real `postgres-tenant-store`. Found and fixed a second real bug while writing #15: `apply.py::read_store_identity` checked schema existence but not `platform_store_meta` table existence before querying it, so a foreign schema with other tables and no marker raised `UndefinedTable` instead of returning `None` — `schema_has_foreign_objects` (which calls it) crashed instead of correctly reporting the schema as foreign.
- [x] 10.6 Write `tests/test_tenant_store_migrate.py` — scenarios #19, #20, #64. Synthetic revision written to `revisions/` for the run's duration (real file-based discovery, not mocked). Found and fixed two more real bugs: (1) `migrate.py::_migrate_one` built `schema = f"tenant_{tenant_id}"` with no hyphen-to-underscore replacement, unlike every other schema-name construction site in the codebase — broken for every real UUID tenant id. (2) `main()` never wrote the migrated `schema_revision` back to `tenant_data_planes`, so a successful migration left the control-plane record permanently reporting the pre-migration revision. `_migrate_one` now returns `(outcome, revision)` and `main()` persists it.
- [x] 10.7 Write `tests/test_data_plane_connection_replacement.py` — scenarios #21, #22. Both against real `postgres-tenant-store`, including a real predecessor retire + successor activate + connection_id rebind.

## 11. Failure isolation

- [x] 11.1 Add upload pre-check (resolve + `SELECT 1`) before bytes reach the content store. `documents.py::get_session` now runs `SELECT 1` on the resolved connection before yielding — a FastAPI dependency, so it resolves (and can raise) before the route body ever calls `file.read()`. A real driver failure is classified via the (now-public) `tenant_context.classify_driver_error` and recorded via the new `record_health_best_effort`, reusing the same finite-reason vocabulary `tenant_session` uses rather than adopting `tenant_session` itself (which narrows `search_path` — incompatible with this module's mixed tenant-schema/`public.`-qualified queries on one session; verified by a real regression when first tried). `tests/test_upload_precheck.py`.
- [~] 11.2 Add `DataPlaneUnavailable` retry-by-countdown with `NER_DATA_PLANE_TASK_MAX_RETRIES` to OCR dispatch, batch extraction, projection, analytics refresh, blob sync, training data load, and provisioning; park as failed-retryable with `data_plane_unavailable`; keep payloads identifier-only. The backoff helper moved to `data_plane_retry_countdown` (`src/shared/data_plane.py`) so every wired task shares one decision (blob sync keeps its own pre-existing `_data_plane_retry_countdown` alias in `blob_sync/tasks.py` — an existing test monkeypatches that exact symbol, so it was left alone rather than risk that regression for a pure rename). Done for **blob sync** (`run_blob_sync_task`, scenario #27, unchanged from before), **batch extraction** (`run_batch_extraction`'s outer span wrapper in `extraction_service/worker.py` now catches `DataPlaneUnavailable`/`DataPlaneNotReady` around `_run_batch_extraction`, whose own `_get_sync_engine` call sits outside any inner `except Exception`, so the resolver failure reaches this outer catch cleanly), **analytics refresh** (`refresh_analytics_materialized_views` in `analytics_service/worker.py`, wrapping the `resolve_sync` + view-refresh body), and **training data load** (`fine_tune_model` in `training_service/worker.py`, wrapping just the early `_get_sync_engine` call so a down store is caught before any GPU/mlflow work starts). Each parks with `{"...": "failed_retryable", "reason": "data_plane_unavailable"}` after `data_plane_task_max_retries`, `self.retry(..., max_retries=settings.data_plane_task_max_retries)` passed explicitly so the decorator's own (lower) `max_retries` default can't cut retries short — verified `py_compile` clean and the full existing `test_batch_extraction*.py`/`test_training_worker.py` suites still pass (8 pre-existing failures on this branch are all a `ModuleNotFoundError: Could not import module 'Trainer'` environment issue, confirmed present identically on a clean `git stash` of this change — not a regression). **Not done**: **projection** has no standalone dispatch point to wire (`project_document_entities` runs synchronously inside `run_batch_extraction`'s per-document loop, already covered transitively by that task's own retry); **OCR dispatch** (`trigger_ocr`/`InProcessDispatcher.dispatch` in `ingestion/service.py`) is a fire-and-forget `asyncio.create_task`, not a Celery task — has no `self`/binding to retry through, so this pattern does not apply without first restructuring OCR dispatch onto Celery, which is a larger, separately-scoped change; **provisioning** (`provision_tenant_data_plane`) reaches the tenant store through a raw `create_engine(...)` rather than the resolver, so `DataPlaneUnavailable`/`DataPlaneNotReady` are not what it raises on a store failure (already classified as `store_unreachable` and surfaced to the tenant-admin-triggered retry from group 10.2 instead).
- [x] 11.3 Add circuit breaker on recorded `unreachable` health, rate-limited health writes, a beat probe, and a recovery sweep re-dispatching documents stuck in `processing` after health returns. `record_health`/`record_health_sync` (`src/shared/data_plane.py`) write the content-free health signal, wired into `tenant_context.py`'s resolver-failure paths and the upload precheck; `probe_tenant_data_plane_health` beat task (`data_plane/tasks.py`) runs one `SELECT 1` per `ready` tenant on `NER_DATA_PLANE_HEALTH_PROBE_INTERVAL_SECONDS` (the beat cadence itself is the rate limit); verified end-to-end against real `postgres-tenant-store`, including recovery (`tests/test_data_plane_health.py`, scenarios #29, #30). Circuit breaker: `require_data_plane_ready` (`src/shared/data_plane_gate.py`) now raises `DataPlaneUnavailable` straight from the cached `health_outcome` (`unreachable`/`auth_failed`/`timeout`) for a `tenant_owned` tenant, before any connection attempt is made — pure efficiency (the resolver's own per-request attempt already failed closed), skipped entirely for `platform` tenants. `tests/test_data_plane_circuit_breaker.py`. Recovery sweep: `_recover_stuck_documents` (`data_plane/tasks.py`), triggered by the probe on an unhealthy→`HEALTH_HEALTHY` transition (`previous_health` read in the same query), resets any `documents` row stuck in `processing` past `data_plane_stuck_document_threshold_seconds` (new setting, default 300s) back to `pending` and redispatches it through the normal `process_document(reprocess=False)` claim path — same code path a fresh upload uses, not a separate recovery path. `tests/test_data_plane_recovery_sweep.py`, verified against real `postgres-tenant-store` with a `source_only`/no-reopener document that deterministically reaches `status='failed'` via `ContentUnresolvable`, proving the sweep actually dispatched processing rather than just resetting a column.
- [x] 11.4 Confirm `/health` readiness checks only platform dependencies. Verified: every service's `/health` calls `check_database(get_engine())`, and `get_engine(tenant_id=None)` is the platform-only accessor (`get_engine()` raises `TypeError` for any non-`None` argument, per `database.py`) — no service's readiness check can reach a tenant-owned store. No code change needed.
- [x] 11.5 Write `tests/test_data_plane_failure_isolation.py` — scenarios #23–#26. Fixing #23/#25 (chat) surfaced the same gap 11.1 found in `documents.py`: `chat.py`/`public.py`'s `get_session` never proved the connection reachable before yielding, so a genuinely down store would have reached the route body and raised an unclassified driver exception instead of 503. Applied the same `SELECT 1` + `classify_driver_error` + `record_health_best_effort` fix to both.
- [x] 11.6 Write `tests/test_data_plane_task_retry.py` — scenarios #27, #28. Scenario #27 (park after bounded retries) passing against blob sync. Scenario #28 ("No content is buffered on the platform") turned out not to depend on OCR's retry wiring at all — the property it checks (produced OCR text never lands on the platform, and the document stays reprocessable) already falls out of `process_document`'s existing structure: spans and the terminal status update commit in one transaction, so a write failure mid-commit persists nothing. Verified against real `postgres-tenant-store` by monkeypatching content resolution/extraction to produce known text, then making the span-insert's `AsyncSession.execute` raise (a real host is not killed mid-test; the DB layer is where "the store went unreachable" actually manifests to this code either way) — confirms zero span rows, the document lands at `status='failed'`, and a second `process_document` call after the fault clears reprocesses it to `status='processed'` with exactly one span.
- [x] 11.7 Write `tests/test_data_plane_health.py` — scenarios #29, #30. Both pass against real `postgres-tenant-store` (outage recorded while `/health` stays 200 on platform dependencies; recovery detected on the next probe after repointing the connection at the real store).

## 12. Document registry

- [x] 12.1 Implement `registry.record(...)` called after tenant-store commits on ingestion, OCR status transitions, blob-sync replace/hide, and deletion (Design D7). New `src/shared/tenant_document_registry.py`: `record`/`record_sync` (upsert), `update_status`/`update_status_sync` (status-only transition on an existing row), `remove` (hard delete, for reconciliation). Wired into ingestion (`ingestion/service.py::ingest`, after the tenant-store commit), OCR terminal transitions (`ocr_worker.py`, both `processed` and `failed`), and soft-delete (`documents.py::delete_document`) — each on a fresh *platform* session per Design D10, never the tenant session the content write itself used. Blob-sync **replace** is a re-ingestion through the same `DocumentIngestionService.ingest()` path, already covered; blob-sync **hide** is a separate exclusion table (`{schema}.azure_blob_hidden_documents`, CAP-3) that never changes `documents.status` — there is no registry-relevant state change for it to write.
- [x] 12.2 Implement the beat reconciliation task skipping unreachable stores. `reconcile_tenant_document_registry` (`data_plane/tasks.py`, 5-minute beat schedule): re-derives every document row from every platform tenant's own schema and every `ready` `tenant_owned` tenant's store via `registry.reconcile_schema_sync`; a `tenant_owned` tenant whose store cannot be reached this cycle is caught and skipped — its existing registry rows are left untouched, never cleared. Found and fixed two real bugs while verifying against real infra: platform-tenant iteration checked only schema *existence*, not `documents` table existence (crashed on a platform schema without that table); `reconcile_schema_sync`'s projection copied migration 043's `COALESCE(file_size_bytes, file_size)` from the one-time platform backfill, but a tenant store never has the legacy `file_size` column to fall back to.
- [x] 12.3 Switch document quota enforcement, system-admin counts, and dashboard totals to the registry. `dashboard.py::_tenant_admin_data`'s document count and 24h-added count now read `public.tenant_document_registry` instead of `{schema}.documents` — available (and correct) regardless of the tenant's data-plane mode or current reachability. **Note**: no document-count quota is actually *enforced* anywhere in this codebase today (`max_documents` is a stored per-tenant limit no upload path checks against) — confirmed by search; this task switches the read paths that exist, it does not invent enforcement that doesn't. System-admin dashboard has no fleet-wide document total today either (only training-job counts) — nothing existed there to switch.
- [x] 12.4 Complete `tests/test_tenant_document_registry.py` — scenarios #33–#37. Written as `tests/test_tenant_document_registry_reconcile.py` (a new file rather than extending the existing one, matching that file's own note that the write path/reconciliation scenarios "land with task group 12"). All 5 pass against real `postgres-tenant-store`.

## 13. Portal

- [x] 13.1 Add the Azure PostgreSQL data-plane provider to the portal catalog and form schema in `src/portal/src/lib/data-sources.ts`, shown only for `tenant_owned` tenants and labelled distinctly from the read-only source. Added `azure_postgresql_data_plane` to `PROVIDERS`/`PROVIDER_LABELS` ("Tenant-Owned PostgreSQL (Data Plane)"), `TENANT_OWNED_ONLY_PROVIDERS`, `DATA_PLANE_CONFIG_KEYS`/`DATA_PLANE_SECRET_KEYS` (identical shape to `azure_postgresql`), and the `DataPlaneStatusResponse`/`DataPlaneMode`/`DataPlaneStatus` types plus new finite error codes. `create-connection-modal.tsx` takes a `dataPlaneMode` prop and only renders the option when `tenant_owned`; `ProviderConfigForm`'s existing `isBlob` branch already covers the new provider's identical field shape. `settings/data-sources/page.tsx` passes its own tenant's mode from the new `useDataPlaneStatus` hook, and its provider-filter `isProvider` guard now derives from `PROVIDERS` instead of a stale two-value literal check.
- [x] 13.2 Replace Sync activity with a Data plane panel on data-plane connection detail (status, provisioning outcome, revision, health, Retry provisioning); render residency test checks as labelled outcomes; add retirement copy. New `src/portal/src/components/data-plane/data-plane-panel.tsx`; `lifecycle.tsx::SyncActivitySummary` gets an `azure_postgresql_data_plane` branch returning it (ahead of the existing `azure_postgresql` "sync doesn't apply" branch). Residency test checks are the connection's own `last_test.reason_code`, already rendered verbatim in Connection facts — no separate rendering needed since the reason classes are already finite and safe.
- [x] 13.3 Add `use-data-plane` hook and a `DataPlaneGate` component wrapping documents, extraction, annotation, training, analytics, and chat pages with safe status states and no cached content after `TENANT_DATA_PLANE_UNAVAILABLE`. New `src/portal/src/hooks/use-data-plane.ts` (`useDataPlaneStatus`, `useRetryDataPlaneProvisioning`) and `components/data-plane/data-plane-gate.tsx`. Wired via a new `layout.tsx` in `analytics/`, `annotation/`, `extractions/`, `training-jobs/` (documents/ already had one, gate added there) and inline in `chat/page.tsx` (whose `RequireAuth` was already inline, not a layout). A blocking `DataPlaneStatus` or a `TENANT_DATA_PLANE_UNAVAILABLE`/`TENANT_DATA_PLANE_NOT_READY` error renders a safe status state instead of `children` — the wrapped page's content never mounts, so nothing it might have cached can render either.
- [x] 13.4 Add data-plane choice to `admin/tenants/new`, and mode/status/health plus registry-based document counts to the tenant list and `admin/tenants/[id]`. Required a small backend addition alongside the frontend: `TenantService._get_by_id` and `.list_tenants` (`src/gateway/services/tenant_service.py`) now also return `data_plane.health` and a registry-derived `document_count` (previously the detail page hardcoded `0 /`, and the list had no document column at all). `new/page.tsx` gained a data-plane-mode radio group submitting `data_plane_mode`; `[id]/page.tsx` and the list page render mode/status/health and the real count.
- [x] 13.5 Update `settings/data-sources/[connectionId]/page.test.tsx` and `settings/data-sources/page.test.tsx` — scenarios #65–#73. #65-71 (pre-existing) reverified passing; #72 (data-plane option hidden for platform / shown for tenant_owned) and #73 (residency test-check outcome + Data plane panel, not Sync activity) added.
- [x] 13.6 Write `src/portal/src/components/data-plane/data-plane-gate.test.tsx` — scenarios #74, #75. Both pass; a `getByRole("status")` race against the loading spinner (which also carries `role="status"`) was fixed by asserting on the specific blocked/unavailable copy instead.
- [x] 13.7 Write `admin/tenants/new/page.test.tsx` and extend `admin/tenants/[id]/page.test.tsx` — scenarios #76, #77. Found the page's labels aren't programmatically associated with their inputs (no `htmlFor`/`id`) — a pre-existing accessibility gap, worked around in the new test via role/placeholder queries rather than fixed (out of this task's scope).

Full portal regression (`npx vitest run`, all of `src/portal`) both before and after every 13.x change: 49 failing tests in 12 files, exactly matching a stashed-changes baseline run — zero net-new regressions from this group. Two real cross-file bugs surfaced and fixed along the way: `chat/page.test.tsx` and `settings/data-sources/[connectionId]` tests had no `QueryClientProvider`/needed one extended, since `ChatPage` never used react-query before `DataPlaneGate`; and a settings-page test reused one mocked `Response` object across multiple `authFetch` calls, which silently broke once a second call (the new data-plane status fetch) tried to read its already-consumed body ahead of the real assertion's call.

## 14. Documentation

- [x] 14.1 Add a customer prerequisites runbook in `docs/runbooks/` (Flexible Server 16+, `VECTOR` allow-list, dedicated database, login role, optional pre-created query role, private endpoint or egress allowlist). `docs/runbooks/tenant-owned-postgresql-prerequisites.md` — each requirement cross-referenced to the exact secure-test check and finite failure reason it maps to.
- [x] 14.2 Update `docs/architecture/tenant-pluggable-data-architecture.md` §8–§9 to reference ADR-017 and the implemented status machine. Added "Implemented (ADR-017)" callouts to both sections (registry schema and the engine-resolver/migration/provisioning/status-machine implementation respectively) plus a status-line pointer at the top of the document.
- [x] 14.3 Run `graphify update .` after code changes. 22633 nodes, 35369 edges, 1392 communities.

## 15. Verification & Evidence

- [x] 15.1 Run all acceptance-criteria tests for every scenario in verification.md § Spec Alignment and confirm all pass. All 77 scenarios' Status column marked; consolidated pytest run (25 files, #1–#64) — 131 passed, 2 failures individually confirmed pre-existing/order-dependent, not caused by this change; consolidated vitest run (5 files, #65–#77) — 33 passed, 0 failed. Found and fixed one more real bug while running the consolidated pass: `reconcile_tenant_document_registry`'s platform-tenant loop had no per-tenant exception isolation (unlike its `tenant_owned` loop), so one tenant's `documents` table missing an expected column (a legacy/minimal schema shape) aborted reconciliation for every tenant after it in the same run — and left the shared connection's transaction aborted besides. Now caught, rolled back, and skipped per-tenant, same as the `tenant_owned` isolation.
- [x] 15.2 Collect functional evidence (screenshot / test output / log) for each scenario — record one entry per row in verification.md § Evidence Log. 3 entries added: the consolidated pytest run, the consolidated vitest run, and a summary of the session's 4 full-repo regression-diff passes.
- [x] 15.3 Confirm every Hallucination Risk mitigation step in verification.md § Hallucination Risk Register. All 7 risks map to concrete mitigations already exercised this session: R1 (engine routing) — `test_tenant_engine_construction_boundary.py` architecture test plus the real residency runs against `postgres-tenant-store` throughout groups 6–13, confirming zero platform-DB rows for `tenant_owned` tenants; R2 (cross-plane SQL) — Design D10 fixes made and verified across group 4/5 (documents.py, blob_sync, training_jobs.py, dashboard.py) with real second-server tests; R3 (schema parity) — `test_tenant_store_parity.py`/`test_tenant_store_revisions.py` pass; R4 (status machine/CAS) — every transition in `data_plane.py` is a CAS, exercised by `test_tenant_data_plane_record.py` and the replacement/pause/retire tests; R5 (error hygiene) — `test_data_plane_failure_isolation.py` scenario #25 asserts no host/driver text in response, logs, or traces; R6 (registry content boundary) — `test_tenant_document_registry.py`'s column-set assertion plus `record`'s best-effort-after-commit write order; R7 (retry/no-buffering) — `test_data_plane_task_retry.py` confirms identifier-only payloads for the one task type with retry wiring (blob sync; see 11.2's noted partial scope for the rest).
- [x] 15.4 Confirm all ADR compliance steps in verification.md § Pattern & ADR Compliance. Reviewed against the running implementation: ADR-001 (schema-per-tenant, `search_path` scoping) unchanged on both planes, new public tables (`tenant_data_planes`, `tenant_document_registry`) content-free; ADR-007 (chat SQL/retrieval) unchanged, `SET LOCAL ROLE` read-only execution now also runs against tenant-owned stores (`provision_tenant_data_plane`'s role-provisioning step, `test_data_plane_fleet_operations.py`); ADR-011 (lifecycle/evidence/idempotency) reused as-is by the new provider (`test_data_plane_provider_control_plane.py`, `test_data_plane_status_endpoint.py`'s `_mutate` wrapper); ADR-012 (blob sync) — residency tenants blocked from `platform_blob` (`test_integration_profile_data_plane.py`), retry wiring added; ADR-013 (external PG chat) — shared-secret-reference rejection test passes, external chat untouched; ADR-014 (Compose/migrations) — migration 043 additive-only, `postgres-tenant-store` added as a healthy Compose service, migrate step wired; ADR-015/016 unaffected, existing tests still pass; ADR-017 — every design.md decision D1–D12 has a corresponding implementation and test, reflected across groups 1–13's tasks.md entries.
- [ ] 15.5 Complete Audit Record sign-off in verification.md § Audit Record (human reviewer required — this task cannot be marked complete by an agent).
- [x] 15.6 Run `openspec validate tenant-postgresql-data-plane --type change --strict` and confirm it exits clean before archive. `Change 'tenant-postgresql-data-plane' is valid`.
