## 1. Prerequisites

- [x] 1.1 Confirm change 5 (`confidence-routed-review`) has landed — this change writes the consumed-span record change 5 defines and reads, and surfaces the accumulation figure change 5 computes. **Confirmed:** archived at `openspec/changes/archive/2026-09-08-confidence-routed-review`; `span_training_consumption` created by migration `040` with no writer.
- [x] 1.2 Decide who may request a retrain (Tenant Admin, System Admin, or both), consistent with the existing role model. Approval is already System Admin only, so the request may reasonably be broader. **Decision: Tenant Admin only** — matches `POST /api/v1/training-jobs`, which is already `require_tenant_admin`. Keeping the requester and the approver distinct preserves the two-party gate.
- [x] 1.3 Decide whether a retrain may be requested when accumulation is zero. Blocking prevents a pointless run; allowing preserves retraining after a hyperparameter change rather than a data change. **Decision: allow, with a warning in the response** — the surface presents evidence, it does not gate.
- [x] 1.4 Decide how prominently the promotion surface conveys that metrics from runs over materially different datasets are not directly comparable, and what counts as "materially different". **Decision: a relative difference of 25% or more in trained-on span count**, reported as a `comparable` boolean plus a note in words. The 25% is a presentation constant local to this surface — it is not a readiness or training threshold and does not touch ADR-010.
- [x] 1.5 Confirm the per-entity-type breakdown is sufficient context for the retrain decision, or determine what else the decision actually requires. **Decision: per-type breakdown, serving version, and an in-flight-run indicator.** design.md's Risks section notes a figure is stale while a run executes; saying so is a fact, not a judgement, and it is what makes the number readable.

## 2. Record Consumed Spans

- [x] 2.1 Record the set of confirmed spans consumed by a training run, keyed by tenant and by the model version the run produced, writing into the record change 5 defines. Do NOT duplicate span rows (design.md Decision 1). Implemented in `src/training_service/services/consumed_spans.py`; writes span ids into `{schema}.span_training_consumption`, copying no span row.
- [x] 2.2 Place the write in the run-completion path only. It MUST NOT fire at submission, at approval, or on failure (verification.md Risk 2). The only call site is the success branch of `worker.fine_tune_model`, in the same transaction as the model-version `completed` flip. The span set is captured at dataset-build time and held in memory until then, so a run trains on and records exactly the spans that existed when its export was taken.
- [x] 2.3 Confirm accumulation computed by change 5 resets to zero against the newly produced version once the record is written, and is unchanged while a run is in flight. Confirmed by `test_accumulation_resets_after_run` and `test_accumulation_unchanged_during_run`. **This required a fix to change 5's reader**: `accumulation.current_model_version` read `model_versions.version`, the legacy `002` column that `018` made nullable and that the training worker has not written since — so it returned NULL and reported every trained tenant as base-model. Now `COALESCE(version_number, version)`, agreeing with `extraction_service`'s worker.
- [x] 2.4 Add `tests/test_consumed_spans.py` with `test_completed_run_records_consumed_spans`, `test_accumulation_resets_after_run`, `test_rejected_job_records_nothing`, `test_failed_run_records_nothing`, and `test_accumulation_unchanged_during_run`, covering Spec Alignment rows 1-5. All five present and passing, plus `test_second_run_over_same_spans_does_not_raise` for the primary-key collision a second run over the same corpus would otherwise hit.

## 3. Retraining Decision Surface

- [x] 3.1 Add an endpoint returning the accumulation figure for the tenant's currently serving model version, broken down per entity type, with the serving version identifier. `GET /api/v1/retraining-decision` in `src/annotation_service/api/v1/retraining_decision.py`. The per-type breakdown was added to `accumulation_report` rather than recomputed, so both surfaces read one query.
- [x] 3.2 Return a distinct no-trained-model state for a tenant served by the base model, rather than an accumulation figure of zero (design.md Decision 5, ADR-008). `has_trained_model: false` with `state` and `state_detail`; `spans_accumulated` and `by_entity_type` are absent from the response rather than zero.
- [x] 3.3 Label accumulation distinctly from dataset readiness and do not compare it against the per-entity-type readiness threshold. Introduce no new threshold constant (ADR-010). `kind: accumulation_since_training` plus a note saying in words what the figure is not. Enforced structurally by `test_surface_introduces_no_readiness_threshold`, which parses the module for any readiness import, threshold name, or multi-digit literal.
- [x] 3.4 Add `tests/test_retraining_decision.py` with `test_surface_reports_accumulation_and_version`, `test_accumulation_broken_down_per_entity_type`, `test_no_trained_model_shown_distinctly`, and `test_accumulation_not_presented_as_readiness`, covering Spec Alignment rows 6-9. All four present and passing, plus the in-flight indicator from task 1.5 and the structural threshold scan.

## 4. Manual Retrain Request

- [x] 4.1 Add a retrain request endpoint that creates an ordinary training job through the existing submission path, landing in `pending_approval`. It MUST NOT enqueue a Celery task, MUST NOT create the job already queued, and MUST NOT bypass approval (design.md Decision 2, ADR-006). `POST /api/v1/training-retrain-requests` calls `training_jobs.create_training_job` — the same function, not a copy. A `send_task` spy is empty across the request and fires exactly once at approval.
- [x] 4.2 Confirm the request carries no hyperparameters and that hyperparameters continue to be set at approval time (ADR-009). The body is `TrainingJobCreate`, which has no fields and forbids extras, so a supplied `learning_rate` is a 422 rather than a value silently dropped. Verified by `test_request_carries_no_hyperparameters`, `test_supplied_hyperparameters_are_refused`, and a source scan.
- [x] 4.3 Apply the requester-role decision from task 1.2 and the zero-accumulation decision from task 1.3. Tenant Admin only, inherited from `create_training_job` and verified by `test_requester_must_be_tenant_admin`. Zero accumulation is allowed and flagged with an `X-Retrain-Warning` header, verified by `test_zero_accumulation_is_allowed_with_a_warning`.
- [x] 4.4 Confirm the existing `training-approval` approve and reject paths handle a retrain-requested job identically to any other job, with no branching on how the job originated. `test_approval_handles_it_like_any_other_job` approves one ordinary and one retrain-requested job and compares the resulting rows field by field. `training_jobs.py` is unmodified by this change — the request lives in its own module precisely so that file need not be opened.
- [x] 4.5 Add `tests/test_retrain_request.py` with `test_request_creates_pending_approval_job`, `test_requested_retrain_follows_approval_flow`, `test_requested_retrain_can_be_rejected`, and `test_request_carries_no_hyperparameters`, covering Spec Alignment rows 10-13. All four present and passing, plus the role, zero-accumulation, no-branching and structural checks above.

## 5. No Automatic Initiation

- [x] 5.1 Verify by inspection that no code path reachable from the accumulation figure, a threshold comparison, a schedule, or a training run completion creates, enqueues, or schedules a training job. Verified, and mechanised: `test_no_module_enqueues_training` walks the AST of all five modules for `send_task`/`delay`/`apply_async`, `test_no_module_registers_a_schedule` scans for beat, cron and timer registrations, and `test_no_module_compares_a_figure_to_a_constant` asserts the accumulation-path modules contain no float literal and no multi-digit integer.
- [x] 5.2 Verify that run completion does not promote the version it produced, and that the previously promoted version continues serving until a human promotes. `test_completion_does_not_self_promote` and `test_promotion_requires_human_action`; `test_no_module_promotes` scans the five modules and the training worker for any promote call.
- [x] 5.3 Confirm no scheduler registration, cron entry, or timer capable of initiating a training job exists in this change's diff. Confirmed — see task 8.5 for the diff-wide grep.
- [x] 5.4 Add `tests/test_no_auto_retraining.py` with `test_large_accumulation_creates_no_job`, `test_completion_does_not_chain_run`, `test_completion_does_not_self_promote`, and `test_promotion_requires_human_action`, covering Spec Alignment rows 14-17. These MUST exercise the real paths, not stubs. All four present and passing against the real annotation and training apps, the real accumulation query and the real consumed-span writer, with `send_task` spied on both services' Celery apps. Each is paired with a source-level scan, because a config-gated trigger defaulting to off would pass every behavioural check.

## 6. Promotion Decision Evidence

- [x] 6.1 Add an endpoint returning a completed version's evaluation metrics alongside the currently promoted version's, with the confirmed span count each was trained on. `GET /api/v1/training-promotion-evidence/{version_number}` in `src/training_service/api/v1/promotion_evidence.py`. Span counts read from `span_training_consumption`, the table task 2.1 writes.
- [x] 6.2 Indicate when the two runs used materially different dataset sizes, per the definition decided in task 1.4. `comparable: false` plus a note naming both counts, at a relative difference of 25% or more. `comparable: null` when either size is unrecorded — unknown, not similar.
- [x] 6.3 Handle the case of no currently promoted version by returning the candidate's metrics and indicating there is nothing to compare against. `current: null`, `comparable: null`, and a note. Also covers a candidate that is itself the promoted version, which would otherwise produce a reassuring identical pair.
- [x] 6.4 Confirm the response contains no comparative verdict, ranking, or promotion recommendation (design.md Decision 4). `test_no_verdict_produced` gives the candidate strictly better metrics on every axis with similar dataset sizes, then asserts no verdict word appears anywhere in the serialized response and that the response has exactly four keys.
- [x] 6.5 Add `tests/test_promotion_evidence.py` with `test_candidate_and_current_metrics_presented`, `test_different_dataset_sizes_flagged`, `test_no_verdict_produced`, and `test_no_promoted_version_to_compare`, covering Spec Alignment rows 18-21. All four present and passing, plus the similar-sizes, unknown-size, threshold and 404 cases.

## 7. Frontend Surfaces

- [x] 7.1 Add the retraining decision surface showing the per-entity-type accumulation breakdown against the serving version, with a request-retrain action, and the distinct no-trained-model state. `src/portal/src/components/retraining/RetrainingDecisionPage.tsx`, routed at `/retraining` and on the Tenant Admin sidebar only. `RetrainingDecision` is a discriminated union, so the compiler refuses the `?? 0` that would conflate no-trained-model with zero.
- [x] 7.2 Add the promotion decision surface showing candidate and current metrics with their dataset sizes and the comparability indication, without any recommendation. Same page, second section. No promote control and no verdict — asserted by a test that renders a strictly better candidate.
- [x] 7.3 Confirm the existing promote control and the existing training jobs and approval screens are unchanged. Unchanged. The only edits outside new files are one routing rule in `auth-fetch.ts` and one nav entry in `nav-config.ts`. Full portal suite: 18 failures at HEAD, 18 after — the single new failure was `nav-config.test.ts`'s item count, updated as an added nav entry requires. `tsc --noEmit` reports 34 errors, all pre-existing in unrelated test files and none in any file this change touches.

## 8. Verification & Evidence

> **Archived 2026-09-08 with 8.7, 8.8 and 8.9 open**, on the user's explicit instruction. The Audit Record gate in verification.md § 6 was overridden rather than satisfied — see verification.md § 7 for what that leaves unconfirmed.

- [x] 8.1 Run all acceptance-criteria tests for every scenario in verification.md § Spec Alignment and confirm all pass. **40 passed** across the five backend files, and **11 passed** for `RetrainingDecisionPage.test.tsx`. Run in the `annotation_service` container against `ner_test` with both `NER_DATABASE_URL` and `NER_DATABASE_URL_SYNC` pointed at `postgres-test`. Change 5's own suites re-run clean alongside: 77 passed.
- [x] 8.2 Collect functional evidence (screenshot / test output / log) for each scenario — record one entry per row in verification.md § Evidence Log. verification.md § Evidence Log now has 25 entries: one per Spec Alignment row, plus the portal suite, the structural scan, and the two regression runs. Every Status cell in § Spec Alignment is ticked.
- [x] 8.3 Confirm every Hallucination Risk mitigation step in verification.md § Hallucination Risk Register. All seven confirmed, each recorded in a new *Agent Confirmation* column in § 2 saying how. Risks 1, 3 and 7 — the auto-trigger, the auto-promotion, and the verdict — are each confirmed structurally as well as behaviourally, since a config-gated version of any of them would pass a behavioural test with the flag off.
- [x] 8.4 Confirm all ADR compliance steps in verification.md § Pattern & ADR Compliance. All six confirmed, each recorded in a new *Agent Confirmation* column in § 3.
- [x] 8.5 Grep the full diff for scheduler registrations, cron entries, timers, and any comparison of an accumulation figure against a constant, and confirm there are none. None. Scoped to this change's own file set rather than `git diff`, because the working tree also carries uncommitted work from changes 3, 4 and 5 — for the two files this change edited that belong to change 5, only the added lines were scanned. No scheduler, cron or timer token anywhere; no `send_task`/`apply_async`/`.delay(` on any path this change adds; no float and no multi-digit integer literal in the three accumulation-path modules. The same scans run as tests, so they hold going forward rather than only today.
- [x] 8.6 Confirm `training-approval` and `model-registry` promote/demote code paths are unchanged in the diff. Confirmed: `git diff HEAD` reports `src/training_service/api/v1/training_jobs.py` and `src/training_service/api/v1/models.py` both unmodified, with `approve_training_job`, `reject_training_job`, `promote_model` and `demote_model` all intact. The retrain request and the promotion evidence surface each live in their own module precisely so neither file needed opening.
- [ ] 8.7 Run one full loop end to end on a tenant: accumulate reviewed spans, observe the figure, request a retrain, approve it, let it complete, confirm the figure resets against the new version, then promote using the evidence surface. **Not done — blocked.** Every step except one is covered by a passing test against the real endpoints, but the loop as written requires a training run to actually *complete*, which means fine-tuning BERT on a GPU worker and writing a real model version. That is not something this environment can do, and faking it would make the one step the loop exists to prove into a stub. The recording function itself is exercised directly by `test_consumed_spans.py`; what remains unproven is that `worker.fine_tune_model` reaches it on a real run. Needs a live run on a tenant with a GPU worker.
- [ ] 8.8 Complete Audit Record sign-off in verification.md § Audit Record (human reviewer required — this task cannot be marked complete by an agent). **Not done — human gate**, by the task's own terms. § 6's thirteen checkboxes are deliberately left unticked; they are the only unticked boxes left in verification.md.
- [ ] 8.9 Run `openspec validate human-gated-retraining --type change --strict` and confirm it exits clean before archive. **Not done — blocked.** The `openspec` CLI is a Node package and no Node runtime is installed on this machine or in any project container (`node.exe` not found; the portal image ships a production build with no toolchain). Needs to be run wherever Node is available.
