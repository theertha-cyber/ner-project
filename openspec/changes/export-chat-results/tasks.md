## 1. Database migration

- [x] 1.1 Add `alembic/versions/047_chat_messages_export_rows.py` following the exact `tenant_template` + `DO $$ ... FOR schema_name IN SELECT nspname FROM pg_namespace WHERE nspname LIKE 'tenant\_%'` loop pattern from `010_chatbot_infrastructure.py` (design.md Migration Plan, ADR-014): `ALTER TABLE ... ADD COLUMN export_rows JSONB, ADD COLUMN export_row_count INTEGER` applied to `tenant_template.chat_messages` and every existing `tenant_%` schema's `chat_messages`. `downgrade()` drops both columns via the same loop.
- [x] 1.2 Verifies Hallucination Risk 7 — test in `tests/test_migration_047_chat_messages_export_rows.py` that seeds a test database with at least one non-template tenant schema, runs the migration, and confirms `export_rows`/`export_row_count` exist in both `tenant_template.chat_messages` and the seeded tenant's `chat_messages`.
- [x] 1.3 **Found during implementation, not in the original task list:** this branch (`export-chat`) predates 9 migrations that already landed on `main` (038-046) — including two files both already numbered `038` on `main` itself (`038_document_provenance_and_retention.py`, `038_llm_prelabeling_columns.py`). Confirmed live: the shared `ner_dev` database (reachable at `postgres-test:5432` from the `chat_api` container) is at alembic revision `046`, not `037` as this branch's local history assumes. Renumbered the migration file from `038_chat_messages_export_rows.py` to `047_chat_messages_export_rows.py` (and its test file to match) to avoid an outright filename/revision-id collision with `main`. `down_revision` still reads `"037"` and is flagged with a prominent docstring in the migration file itself — it does NOT chain onto `main`'s real tip and MUST be corrected as part of rebasing this branch before merge. **`alembic upgrade head` must not be run against any shared database from this branch as-is** — this is a pre-existing branch-currency problem unrelated to the export feature itself, surfaced here because it directly blocks safely running/testing this migration.
- [x] 1.4 **Resolved.** The duplicate-`038`/`039` collision turned out to be a genuine bug already on `main` (both `038` files, and both `039` files, used identical revision IDs — `alembic history` failed outright with "Requested revision 039 overlaps with other requested revisions 038"), not just a rebase-timing question. Verified directly against `ner_dev` that both migrations' (additive) DDL had already been applied despite the ambiguity, so fixed it as a pure renumbering on `main` — `038_llm_prelabeling_columns.py` → `038b`, `039_tenant_integration_profiles.py` → `039b`, re-chained in sequence — leaving `037`-`046`'s revision IDs untouched. Verified with a real `alembic upgrade head` from scratch (001→046, zero errors) before touching this migration further. `047_chat_messages_export_rows.py`'s `down_revision` corrected from `"037"` to `"046"`, matching main's real, now-unambiguous head.

## 2. Row snapshot persistence

- [x] 2.1 Surface the full, pre-token-budget SQL result at the point `_persist_turn_and_respond` is called in `src/chat_api/api/v1/chat.py` — `RAGOrchestrator.execute_with_clarification`/`_stream` now return `result.get("sql_results")` (the graph's raw stage output, `ChatState["sql_results"]`) as a 7th tuple element, kept distinct from `AdmittedEvidence`/the prompt-assembly output. Updated the 3 existing test doubles (`test_chat_api_streaming.py`, `test_chat_api_retrieval_status.py`, `test_chat_api_conversations.py`) that mock this method to return the extended tuple.
- [x] 2.2 In `_persist_turn_and_respond`, write `export_rows`/`export_row_count` in the same transaction as the assistant message insert when structured retrieval matched at least one row (`if sql_results:` — truthy, a non-empty list); leave both `NULL` when it didn't. Capped via `export_rendering.cap_rows` (ties to `sql_generator.py`'s `DEFAULT_LIMIT`/1000-row clamp — design.md Risks; ADR-014 Negative consequence). **Revised live** (task 2.7): initially `sql_results is not None`, which also persisted a useless snapshot for a validated query that matched zero rows.
- [x] 2.3 Verifies scenario 7 — `tests/test_chat_api_export.py::TestRowSnapshotPersistence::test_snapshot_captures_the_full_sql_result_not_a_smaller_admitted_subset` (proxies "prompt-admitted subset" with a deliberately smaller `sources` list, since the orchestrator boundary this suite patches is upstream of `ContextAssembler`'s own token-budget truncation, covered separately in `test_context_assembler.py`).
- [x] 2.4 Verifies scenario 8 — `TestRowSnapshotPersistence::test_no_snapshot_created_when_structured_retrieval_unused`.
- [x] 2.5 Verifies Hallucination Risk 1 — `TestOrchestratorSurfacesFullSqlResults::test_returns_state_sql_results_not_admitted_evidence`, asserting the orchestrator's 7th return value tracks `state["sql_results"]` (250 rows) and not `admitted_evidence.rows` (40 rows) in a fixture where the two intentionally differ.
- [x] 2.6 Verifies Hallucination Risk 6 — `TestSnapshotRowCap` in `tests/test_chat_api_export.py`, asserting `cap_rows` truncates 1500 input rows to exactly `MAX_EXPORT_ROWS` (1000).
- [x] 2.7 **Found live, not in the original task list:** a real chat query ("how many candidates do we have?") got a validated, zero-row SQL query (the LLM targeted an empty entity relation — see verification.md Hallucination Risk 9) and the original `is not None` check persisted a useless "0 results" snapshot, which the portal then rendered as a downloadable-nothing card. Changed the condition to `if sql_results:` and added `TestRowSnapshotPersistence::test_no_snapshot_created_when_structured_retrieval_matched_zero_rows` (scenario 16, chat-export spec updated with a new scenario for this).

## 3. ChatResponse export field

- [x] 3.1 Add an `ExportAvailability` model (`message_id: str`, `row_count: int`, `formats: list[str]`) and an `export: ExportAvailability | None` field to `ChatResponse` in `src/chat_api/api/v1/schemas.py`.
- [x] 3.2 Populate `export` in `_persist_turn_and_respond`'s return: non-`None` with `formats=["csv", "xlsx"]` when `export_rows` was written, `None` otherwise; `_response_payload` excludes the key when `None`, matching the existing `pending_clarification`/`retrieval_status` additive convention (specs/chat-api/spec.md updated to say "omitted" rather than "null" to match).
- [x] 3.3 Verifies scenario 9 — `TestExportAvailabilityOnChatResponse::test_response_includes_export_metadata_when_structured_source_succeeded`.
- [x] 3.4 Verifies scenario 10 — `TestExportAvailabilityOnChatResponse::test_response_omits_export_key_when_no_structured_result_exists`.
- [x] 3.5 Verifies Hallucination Risk 2 — `TestExportRowDataNeverInlinedInChatResponse::test_response_body_contains_no_row_array`.
- [x] 3.6 Add `export: ExportAvailability | None = None` to `MessageResponse` in `schemas.py`. In `get_conversation`, select `m.export_row_count` (never `m.export_rows`) alongside the existing columns and populate `export` per message when non-`None` — found during implementation: without this, a message's file card would vanish on page reload/conversation switch, since `GET /conversations/{conv_id}` never carried export availability (chat-api spec updated with a new scenario for this).
- [x] 3.7 Verifies scenario 15 — `TestConversationHistoryRetainsExportAvailability` in `tests/test_chat_api_export.py`, asserting a reloaded conversation's message list carries `export.row_count` for a past snapshot and `export: null` for a message without one.

## 4. Export endpoint (CSV/XLSX)

- [x] 4.1 Add `openpyxl` to `pyproject.toml` (`>=3.1.5,<4.0.0`). Could not run `poetry lock` against the project's own lockfile in this environment (no local Python interpreter — per project memory), but `pip install openpyxl` succeeded and resolved to `3.1.5` inside the `ner-project-chat_api-1` container (task 6.1), and the full `TestFormulaInjectionMitigation`/XLSX-endpoint tests ran against it successfully. The user still needs to run `poetry lock` for the project's own lockfile before this is a clean, reproducible install.
- [x] 4.2 Add a shared row-serialization helper (`src/chat_api/services/export_rendering.py`) used by both the CSV and XLSX writers, that neutralizes any cell value beginning with `=`, `+`, `-`, or `@` (leading `'` prefix) before writing — per design.md Decision 4 / Risks (CSV/Excel formula injection). Also holds `cap_rows`/`MAX_EXPORT_ROWS` (Hallucination Risk 6).
- [x] 4.3 Add `GET /api/v1/chat/messages/{message_id}/export` to `src/chat_api/api/v1/chat.py`. Resolves `message_id → conversation_id → conversations.user_id` via a single JOIN query and returns 404 on any ownership mismatch or missing/absent `export_rows`, mirroring the existing `DELETE /conversations/{conv_id}` ownership pattern. Returns 422 for any `format` other than `csv`/`xlsx`.
- [x] 4.4 Implement CSV rendering via the stdlib `csv` module through the 4.2 helper: `Content-Type: text/csv`, `Content-Disposition` with a filename.
- [x] 4.5 Implement XLSX rendering via `openpyxl` through the 4.2 helper: single-sheet workbook, header row plus data rows, `Content-Type: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`, `Content-Disposition` with a filename.
- [x] 4.6 Verifies scenario 1 — `TestExportEndpoint::test_export_returns_valid_csv`.
- [x] 4.7 Verifies scenario 2 — `TestExportEndpoint::test_export_unavailable_for_message_with_no_structured_result`.
- [x] 4.8 Verifies scenario 3 — `TestExportEndpoint::test_export_rejects_request_from_non_owning_user`.
- [x] 4.9 Verifies scenario 4 — `TestExportEndpoint::test_export_without_authentication_returns_401`.
- [x] 4.10 Verifies scenario 5 — `TestExportEndpoint::test_export_returns_valid_xlsx`.
- [x] 4.11 Verifies scenario 6 — `TestExportEndpoint::test_unsupported_format_returns_422`.
- [x] 4.12 Verifies Hallucination Risk 3 — `TestExportOwnershipCheckIsStructural::test_export_query_joins_conversations_filtered_by_user_id`, a source-inspection test distinct from scenario 3's black-box HTTP test, confirming the ownership check is structurally a database-level JOIN, not an in-Python comparison applied after data is read.
- [x] 4.13 Verifies Hallucination Risk 4 — `TestConversationHistoryExcludesExportRows::test_get_conversation_query_does_not_select_export_rows_blob`. **Refined during implementation** (see task 3.6): `get_conversation` intentionally selects the small `export_row_count` integer for scenario 15, but the test still guards against the bulky `export_rows` JSONB snapshot ever being added to that query.
- [x] 4.14 Verifies Hallucination Risk 5 — `TestFormulaInjectionMitigation` (both CSV and XLSX).

## 5. Portal file card

- [x] 5.1 Added `export?: ExportAvailability | null` to the `Message` interfaces in both `page.tsx` and `MessageThread.tsx`, and populated it from `data.export` in both the streaming (`onDone`) and non-streaming chat-response handlers in `page.tsx`. `loadMessages` needed no change — it assigns `data.messages` from `GET /conversations/{conv_id}` directly, and `MessageResponse.export` (task 3.6) rides along automatically.
- [x] 5.2 Added `ExportCard.tsx` in `src/portal/src/components/chat/`: renders "Download CSV"/"Download XLSX" actions (both `aria-label`led and titled for exact accessible-name matching) with a row-count summary and a pending/error state. **Purely presentational as of the final design** — it no longer owns any show/hide threshold itself (see 5.3).
- [x] 5.3 **Redesigned twice, live, after direct product feedback and a bug report — see design.md Decision 3 for the full arc.** The original `EXPORT_INLINE_THRESHOLD = 20`/`shouldShowExportCard()` living in `ExportCard.tsx` (file-card show/hide only, no text truncation) was replaced by `PREVIEW_LINE_LIMIT = 5` and an `isTruncatable` computation inlined in `MessageThread.tsx`, which now also truncates the reply text itself to a 5-line preview with a "See more"/"See less" toggle. `shouldShowExportCard`/`EXPORT_INLINE_THRESHOLD` were deleted from `ExportCard.tsx` (and their tests from `ExportCard.test.tsx`) rather than left as dead code. Final condition requires **both** `export.row_count > PREVIEW_LINE_LIMIT` **and** the rendered reply's non-empty line count `> PREVIEW_LINE_LIMIT` — a live bug report ("give me one single candidate" wrongly showed "See more") showed that line count alone misfires on a verbose single-result answer.
- [x] 5.4 Download action uses `authFetch` (adds the Bearer token and resolves `/api/v1/chat/...` to the gateway, same as every other chat API call), converts the response to a `Blob`, creates an object URL, clicks a detached `<a download>` element, then revokes the URL — mirrors the existing `use-analytics-data.ts` export-download idiom already in the codebase.
- [x] 5.5 Wired the truncated/full reply text, `ExportCard`, and the "See more"/"See less" toggle into `MessageThread.tsx`'s assistant-message render block, in that order, ahead of the existing `CitationChips`/`MessageFeedback` trailers — all three gated on `showTrailers && isTruncatable`, so they're suppressed while a message is still streaming/thinking, same as citations and the rating control.
- [x] 5.6 Verifies scenario 11 — `MessageThread.test.tsx` ("previews only the first 5 lines, with an export card and a See more toggle...").
- [x] 5.6a Verifies scenario 17 — `MessageThread.test.tsx` ("reveals the full answer and switches the toggle to See less when clicked").
- [x] 5.6b Verifies scenario 20 — `MessageThread.test.tsx` ("hides truncation and the export card while the message is still streaming").
- [x] 5.7 Verifies scenario 19 — `MessageThread.test.tsx` ("does not truncate, and shows no export card or See more, for a short answer even with export data") — high row count, short reply.
- [x] 5.7a **Found live, not in the original task list:** verifies scenario 18 — `MessageThread.test.tsx` ("does not truncate a detailed single-result answer, even with many lines"), the regression test for the line-count-vs-row-count bug (Hallucination Risk 8).
- [x] 5.8 Verifies scenario 13 — `MessageThread.test.tsx` ("does not truncate a long answer that has no export/result-count data at all") — renamed from "shows no file card when export is absent" to reflect that the reply itself no longer truncates either.
- [x] 5.9 Verifies scenario 14 — `ExportCard.test.tsx::"clicking Download CSV fetches the export endpoint and saves via a blob URL"` (and the XLSX equivalent), asserting the exact URL/format passed to `authFetch` and that `URL.createObjectURL`/`revokeObjectURL` are used rather than a direct link.

## 6. Regression sweep

- [x] 6.1 Ran the Python suite for real: no local interpreter exists on this machine (per project memory), but the `ner-project-chat_api-1` Docker container has one — synced the modified/new source and test files into it via `docker cp`, installed `openpyxl`/`pytest`/`pytest-asyncio` (already-running container, network reachable), and pointed `NER_DATABASE_URL`/`_SYNC` at the real `ner_test` database on `postgres-test:5432` (the same Postgres instance the host's `ner_test` on port 55432 is, reached over the compose network instead). `pytest tests/test_chat_api_export.py -v` → **22/22 passed**. `pytest tests/test_chat_api_rag.py tests/test_chat_api_conversations.py tests/test_chat_api_guardrails.py tests/test_chat_api_streaming.py tests/test_chat_api_widget.py tests/test_chat_message_feedback_model.py tests/test_chat_api_retrieval_status.py tests/test_context_assembler.py -q` → **130 passed, 2 skipped, 5 failed** — all 5 failures are pre-existing and unrelated: 1 is a known disclaimer-text assertion mismatch (`test_chat_response_sources`, documented as pre-existing in this repo's own prior verification.md), and 4 are in `TestConfidenceHedgeCaveat` inside `test_context_assembler.py`, which belongs to a separate, already-uncommitted `confidence-hedged-sql-answers` change this session never touched (confirmed via `git status` — both files were already modified before this session started). `tests/test_migration_047_chat_messages_export_rows.py` could NOT be run — see task 6.5, this branch's alembic history doesn't chain onto the shared dev DB's actual revision.
- [x] 6.1a Fixed a real bug surfaced by that run: `chat.py`'s `json.dumps(export_rows)` had no `default=str`, unlike the existing `source_assembly_node`'s equivalent call — asyncpg can return non-JSON-native types (datetime, Decimal) in SQL rows, which would have crashed persistence for some queries. Added `default=str` to match the established pattern before running the suite (caught during code review, not by a failing test — no existing test constructs a row with such a type).
- [x] 6.2 Ran the portal chat test suite in the `ner-portal-test` Docker container (synced via `docker exec ner-portal-test sh -c "cp -r /src/src/portal/src/* /app/src/portal/src/ && ..."`): `CitationCard.test.tsx`, `ExportCard.test.tsx`, `ConversationList.test.tsx`, `MessageThread.test.tsx` (48/48 passed) and `chat/page.test.tsx` (4/4 passed). Also ran the full portal suite for breadth: 743 passed / 44 failed across 120 files — every failure is in an unrelated area (documents, training-jobs, annotation, entity-types, sidebar) that this change's `git status` shows untouched; none are chat-related.
- [x] 6.3 Confirmed `openpyxl` is the only new runtime dependency added (`git diff --stat -- pyproject.toml src/portal/package.json` shows only the `pyproject.toml` dependency-list line changed; `package.json` untouched — no new JS dependency needed since `ExportCard.tsx` uses the browser's native `fetch`/`Blob`/`URL.createObjectURL`).
- [x] 6.4 Confirmed the migration diff matches the `010_chatbot_infrastructure.py`/`032_chat_message_feedback.py` per-tenant-schema loop pattern (Hallucination Risk 7) — same `tenant_template` + `DO $$ ... FOR schema_name IN SELECT nspname FROM pg_namespace WHERE nspname LIKE 'tenant\_%'` structure, confirmed by direct comparison while authoring `047_chat_messages_export_rows.py`.
- [x] 6.5 **Resolved.** After task 1.4's `038b`/`039b` renumbering landed on `main` and `export-chat` merged cleanly into it, ran the complete migration chain from scratch (`alembic upgrade head`) against a fresh throwaway database: **001 through 047 applied with zero errors**, `alembic heads` reports a single unambiguous `047 (head)`. `tests/test_migration_047_chat_messages_export_rows.py` itself still targets `NER_DEV_DATABASE_URL` (the shared `ner_dev`, currently stamped at `046`) rather than a fresh database — running it for real against `ner_dev` requires actually executing `alembic upgrade head` there, which is a deployment action distinct from this merge and was deliberately not done unprompted; the fresh-database run above is the equivalent proof that the migration itself is correct and will apply cleanly whenever that deploy step happens.

## 7. Verification & Evidence

- [x] 7.1 Ran all 15 scenarios' acceptance-criteria tests for real (backend via `pytest` inside `ner-project-chat_api-1`, frontend via `vitest` inside `ner-portal-test`) — all 15 pass; verification.md § Spec Alignment Status column checked for all 15 rows.
- [x] 7.2 Collected functional evidence for every scenario — verification.md § Evidence Log rows 1-4 (test-run output/counts, container, and command for each).
- [x] 7.3 Confirmed all 7 Hallucination Risk mitigations with passing tests/verification. **Risk 7 (migration pattern)**: structural diff against `010_chatbot_infrastructure.py` confirmed (Evidence Log row 5), *and* now confirmed by actually running the full merged chain — `alembic upgrade head` from scratch, 001 through 047, zero errors, single head (task 6.5). Also surfaced and fixed a separate, pre-existing bug on `main` in the same area (duplicate `038`/`039` revision IDs — task 1.4), verified the same way.
- [x] 7.4 Confirmed all 3 ADR compliance steps (ADR-001, ADR-007, ADR-014) in verification.md § Pattern & ADR Compliance — Evidence Log row 6.
- [ ] 7.5 Complete Audit Record sign-off in verification.md § Audit Record (human reviewer required — this task cannot be marked complete by an agent).
- [x] 7.6 Ran `openspec validate export-chat-results --type change --strict` — exits clean.
