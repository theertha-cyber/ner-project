# Deployment: tenant-self-service-data-sources-20260909-2 — dev

- Target: docker-compose (local, developer machine)
- Environment: dev
- Ladder position: rung 1 of 1 — lowest and final environment; this is the most important environment
- Strategy: rolling (as named in `docs/design/deployment-plan.md`). Executed as rolling: additive/backward-compatible migrations first, then application services and workers recreated sequentially in batches (gateway → core services → workers + portal), each batch left running until the next was verified healthy. No router or traffic split exists on this target; on single-replica local services no zero-downtime claim is made.
- Base URL: http://localhost:8000
- Status: **deployed — migrations 038–042 applied, seed + schema verification passed, all application images rebuilt (CAP-2 through CAP-6) and all services recreated healthy**
- Timestamp: 2026-09-10 (UTC+05:30, developer machine) — retry dispatch over the prior BLOCKED record; both migration fixes verified in the working tree and inside the rebuilt image
- Git revision built: `74fe6d4` (HEAD; contains merged CAP-2 through CAP-6) plus uncommitted working-tree changes including the `041`/`042` one-line fixes, all included in the rebuilt images as-is; no code was modified by this deployment
- Image tag / revision deployed: `ner-project-db-init:latest` rebuilt this retry including both fixes (verified inside the image: `041_azure_blob_sync_ledger.py:100` reads `op.execute(ddl.replace("%I", "tenant_template"))` and `042_external_pg_contracts.py:78` reads `op.execute(TENANT_DDL.replace("%I", "tenant_template"))`; image id `sha256:923c980eb35196f4b75dd155dcdb17b181904f5499ed7e10f28e0ac3d84638de`, 3190163470 bytes). All application images rebuilt this retry from the same tree: `gateway`, `chat_api`, `document_service`, `extraction_service`, `model_serving`, `annotation_service`, `training_service`, `analytics_service`, `celery_worker`, `celery_worker_extraction`, `portal` (all `:latest`, all recreated — see batches below)
- Services: 20/20 long-running compose services `running` after recreation (`gateway`, `chat_api`, `document_service`, `extraction_service`, `model_serving`, `annotation_service`, `training_service`, `analytics_service`, `celery_worker`, `celery_worker_extraction`, `portal`, `mlflow`, `postgres-test` (healthy), `minio` (healthy), `redis` (healthy), `otel-collector`, `tempo`, `loki`, `prometheus`, `grafana`). `db-init` is a one-shot task container: exit 0 this run (expected — it is not a running service)
- Health checks: `GET http://localhost:8000/health` -> `{"status":"ok","dependencies":{"database":{"status":"healthy","detail":"reachable"}}}` (exit 0, verified after recreation); `GET http://localhost:8000/health/live` -> `{"status":"ok"}` (verified after recreation); portal root `http://localhost:3000/` -> HTTP 200 (verified after recreation)
- Compose file: `docker-compose.yml` (used as-is, no edits; plan/dry-run step `docker compose config --quiet` passed, exit 0 — the only diagnostic is the pre-existing obsolete-`version`-attribute warning, left untouched)
- CI pipeline generated: not requested — the deployment plan asks for no checked-in CI pipeline, so `ci-pipeline-generation` was not run

## What landed

1. **Image build (container-image-build): PASS.** The existing root `Dockerfile` was used as-is (not rewritten). `docker compose build db-init` rebuilt `ner-project-db-init:latest` from the working tree including both uncommitted migration fixes; both fixes confirmed present inside the built image via `docker run --rm ... grep`. `docker image inspect` succeeds.
2. **Migrations 038–042 via db-init sequencing: PASS (exit 0).** Starting stamp `037` on `ner_dev` (confirmed pre-run), ending stamp `042` (confirmed post-run). Log order: `037 -> 038`, `038 -> 039`, `039 -> 040`, `040 -> 041` (clean — the `ddl.replace` fix holds), `041 -> 042` (clean — the `TENANT_DDL.replace` fix holds), then `ensure_mlflow_db` (`Database 'ner_mlflow' already exists`), full reseed (demo users/documents/tasks/spans/jobs/extraction runs; analytics views refreshed), and `Schema verification passed: no drift detected`. Artifact probes post-run: 10/10 038 provenance/retention columns in `tenant_template.documents`; `public.tenant_integration_profiles` + `public.tenant_data_source_connections` + `public.tenant_data_source_idempotency` present (039/040); 4/4 041 ledger tables in `tenant_template`; `public.external_pg_contracts` + `tenant_template.external_pg_schema_index` + `tenant_demo_tenant.external_pg_schema_index` present (042). Correction to the two prior BLOCKED records stands: their "applied 038/039/040" lines described log execution order, not commits — transactional DDL rolled everything back to 037 on each failure, so this run applied 038–042 for the first time.
3. **Service recreation, rolling batches: PASS.** Batch 1 `gateway` recreated, `/health` ok before proceeding. Batch 2 `document_service`, `extraction_service`, `model_serving`, `annotation_service`, `training_service`, `analytics_service`, `chat_api` recreated together, all `Up`. Batch 3 `celery_worker`, `celery_worker_extraction`, `portal` recreated, full `docker compose ps` shows 20/20 `Up`. Infra services (`postgres-test`, `minio`, `redis`, `mlflow`, `otel-collector`, `tempo`, `loki`, `prometheus`, `grafana`) untouched — 11 hours uptime, still healthy.
4. **Health/readiness verification: PASS.** Gateway `/health` (ok, database reachable), `/health/live` (ok), and portal root (HTTP 200) all verified from outside the compose network after the final batch landed.

## Prior blocking finding — resolved

Migration `042_external_pg_contracts.py:78` failed in the previous attempt with `ValueError: unsupported format character 'I'` (same defect class as the 041 line-100 defect: Python `%`-formatting applied to a template whose `%I` is a PostgreSQL `format()` identifier placeholder). The owning change repaired it in the working tree as `op.execute(TENANT_DDL.replace("%I", "tenant_template"))`, mirroring the 041 fix; this dispatch verified the fix at the source line, inside the rebuilt image, and by the clean `041 -> 042` log transition. No deploy-time code change was made.

## Security review (baseline-policy `security.nonOverridable`)

Reviewed `baseline-policy.json` `security.nonOverridable` before writing this record. No deployment manifests or configs were generated or edited by this dispatch (compose file used as-is; root `Dockerfile` used as-is), so no secret value was embedded and no credential scope was widened beyond the target skill's own pattern (ambient local Docker Engine; no cloud credentials touched). Configuration below cites **names only**; secrets come from the developer-managed ignored `.env` (`env_file: .env`). The compose file's committed dev-only defaults (`ner`/`ner` Postgres, `minioadmin` MinIO) are pre-existing local-development placeholders, unchanged by this deploy. Seed-account credentials below are repeated solely because the build itself commits them in `src/gateway/seed.py` (stated as their source); this is a `dev` environment — the same pattern on `production` would be recorded as a blocking finding instead.

## Access

- Open it: http://localhost:8000
- Sign in: seeded demo accounts defined by the build in `src/gateway/seed.py` (re-seeded by this run's `db-init` seed step; reseed log confirms all roles): System Admin `admin@nerplatform.io` / `Admin123!`, Tenant Admin `admin@democorp.io` / `Demo123!`, Annotator `annotator@democorp.io` / `Demo123!`, Business User `bizuser@democorp.io` / `Demo123!`. Credentials are quoted here only because the build commits them in that seed script — that script is their source. Portal UI: http://localhost:3000/ (HTTP 200 verified post-deploy; login flow itself not re-exercised — unverified beyond the 200).
- Admin: System Admin (`admin@nerplatform.io`, seeded as above) for tenant administration; MinIO console http://localhost:9001 and Grafana http://localhost:3001 are local infrastructure surfaces — reachability of the consoles was not probed, recorded as unverified.
- Guide: `README.md` (exists, verified) and `docs/local-dev.md` (exists, verified) — the plan names the build's user guide as the source of the concrete localhost port; no new guide was produced by this deploy.
- Health: `GET http://localhost:8000/health` -> ok, database reachable (verified 2026-09-10 post-recreation); `GET http://localhost:8000/health/live` -> ok (verified 2026-09-10 post-recreation).
- Configuration: `DATABASE_URL`, `RABBITMQ_URL`, `REDIS_URL`, `MINIO_ENDPOINT`, `MINIO_ACCESS_KEY`, `MINIO_SECRET_KEY`, `JWT_SECRET`, `TELEMETRY_PEPPER`, local Azure Blob / Azure PostgreSQL secret-reference environment variable names, and existing model/LLM configuration names (per the plan's per-environment table) — sourced from the ignored developer `.env` supplied to Compose (`env_file: .env`, local `env://` only). Secret values are never handled by this pipeline; `.env` presence verified, contents not inspected.
